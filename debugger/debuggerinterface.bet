ORIGIN '~beta/mps/astlevel';
-- lib: Attributes --
debuggerInterface:
  (# debugProgram:<
       (# program:^text;
       enter program[]
       do inner
       #);
     debugProgramAs:<
       (# program:^text;
       enter program[]
       do inner
       #);
     stopDebugging:<Object; (* is not implementet *)
     isDebugging:<BooleanValue;
     onCodeViewOpen:<
       (# theCodeView: ^object (*codemoveableeditor*); 
       enter theCodeView[]
       do INNER
       #);
  #);
