ORIGIN '~beta/mps/astlevel';
-- lib: Attributes --
debuggerInterface:
  (# debugProgram:<
       (# program:^text;
       enter program[]
       do inner
       #);
     stopDebugging:<Object; (* is not implementet *)
     isDebugging:<Object;
  #);
