(* Valhalla test:
 *   Set cursor on '4->t.setpos'
 *   Go To Mark
 *   Step into setpos.
 * 
 * r5.0:
 *   sun4s: Fails to step correctly through reset 
 *          (terminates when going out of reset)
 *   sgi:   Fails to step correctly through reset 
 *          (failed to set breakpoint when going out of reset)
 * 
 *   >>>>> 20/10/99: both sgi and sun4s:
 *          No visialization of current imperative when stepping through
 *          reset?????
 *       
 *)
ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/basiclib/formatio';
-- program: Descriptor --
(# t: ^text; i: @integer; p,lgth: @integer; pos: @integer; 
do
   'Mjolner Informatics 1999'->t[];
   0->p;
   0->lgth;
   (if true
    // p < 0 then 0->pos // p > lgth then lgth->pos
    else
       42->putint
   if);
   0x1234->putint;
   0xab->putint;
   
#)  

