ORIGIN '~beta/basiclib/betaenv';
BODY 'leavebody';
-- PROGRAM: Descriptor --
(#
   a,b,c,d,e: @integer;
   myint1: @integerValue (#  do 42->value #);
   myint2: @integerValue (#  do 51->value #);
   pp: (#  do 23->a; leave pp; 32->a;  #);
   P:
     (# 
     do L: (# A: (#  do 21->c; leave L #);  do &a; 32->c;  #)
     #);
   dp: (#  do 32->a; dpp: (#  <<SLOT dpp:DoPart>> #);  #);
   dp2:
     (# 
     do 32->a; dpp2: (#  <<SLOT dpp2:DoPart>> #); 
     #);
   s: (#  do pp; 32->a #);
   ff: (#  do 32->a; leave ff #);
   
do
   23->a;
   ff;
   32->b;
   (* test pattern leave *)
   s;
   32->b;
   pp;
   423->a;
   (* test dobbelt leave *)
   p;
   (* test if leave *)
   l:
   (if a = 21 then
       362->b; leave l; 341->a; 
   if);
   (* test for leave *)
   l:
   (for i: 12 repeat
     42->a; leave l; 432->a; 
   for);
   (* test leave dp-slot *)
   dp;
   342->a;
   dp2;
   342->a;
   
#)  

-- dpp: DoPart --
do 423->c; leave dpp;   

