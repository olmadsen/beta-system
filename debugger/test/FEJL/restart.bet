ORIGIN '~beta/basiclib/betaenv';
BODY 'restartbody';
-- PROGRAM: Descriptor --
(# a,b,c,d,e: @integer; 
   myint1:@integerValue(# do 42->value #);
   myint2:@integerValue(# do 51->value #);
   pp:(# 
     do 23->a;
        restart pp;
        32->a;
     #);
   P:(# 
     do L:(# 
             A:(# do 21->c; restart L #);
          do &a; 32->c;
          #);
     #);
   dp:
     (# 
     do 32->a;
        dpp:(# <<SLOT dpp:dopart >>
            #);
     #);
   dp2:
     (# 
     do 32->a;
        dpp2:(# <<SLOT dpp2:dopart >>
            #);
     #);
do  (* alle udkommenteret pga. loop *)
   23->a;
   (* test pattern restart *)
   (* pp; *)
   423->a;
   (* test dobbelt restart *)
   (* p; *)
   
   (* test if restart *)
   21->a;
   (* l:(if a=21 then
         362->b;
         restart l;
         341->a;
    if);*)
   (* test for restart *)
   (* l:(for i:12 repeat
          42->a;
          restart l;
          432->a;
    for); 
    *)
   (* test restart dp-slot *)
   (* dp; *)
   342->a;
   dp2;
   342->a;
#)

-- dpp:dopart --
do 423->c;
   restart dpp;
   
