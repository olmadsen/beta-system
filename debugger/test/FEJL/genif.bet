ORIGIN '~beta/basiclib/betaenv';
-- PROGRAM: Descriptor --
(# a,b,c,d,e: @integer; 
   myint1:@integerValue(# do 42->value #);
   myint2:@integerValue(# do 51->value #);
do
   23->a;
   
   (* no one fits *)
   (if a 
    // 1 then 32->b 
    // 2 then 34->b 
    // 0 then 63->b 
   if);
   (* fit one *)
   (if a 
    // 1 then 32->b 
    // 23 then 34->b 
    // 0 then 63->b 
   if);
   (* fit two *)
   (if a 
    // 1 then 32->b 
    // 23 then 34->b 
    // 0 then 63->b 
    // 23 then 23->b
   if);

   
   (* fit else part *)
   (if a 
    // 1 then 32->b 
    // 223 then 34->b 
    // 0 then 63->b 
    else
       32->b;
   if);
   
   (* mult selections *)
   (if a 
    // 1 
    // 223 then 34->b 
    // 0 then 63->b 
    // 4
    // 23
    // 24 then 42->b
    else
       32->b;
   if);
   
   (* test evaluations *)
   (if myint1
    // myint2 then 32->b 
    // 42 then 34->b 
    // 0 then 63->b 
   if);
   (if myint1
    // myint2 then 32->b 
    // 42 then  myint1
    // 0 then myint1
   if);
   
   43->a;
   
#)
