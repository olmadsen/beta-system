ORIGIN '~beta/basiclib/betaenv';
-- PROGRAM: Descriptor --
(# a,b,c,d,e: @integer; 
   bvtrue:(# b:@boolean;
          do 42->a;
             true->b;
          exit b #);
   bvfalse:(# b:@boolean;
           do 42->a;
              false->b;
           exit b #);
do
   (* testing simple if *)
   23->a;
   (if a>b then
       423->a;
    else
       42->b
   if);
   (if a<b then
       423->a;
    else
       42->b
   if);
   
   (* boolean pattern Evaluations *)
   (if bvtrue then
       322->a;
   if);
   (if bvfalse then
       322->a;
    else
       42->a;
   if);
   321->b
#)
