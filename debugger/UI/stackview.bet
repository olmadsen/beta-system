ORIGIN '~beta/objectbrowser/v2.0/UI/moveable';
INCLUDE '~beta/objectbrowser/v2.0/UI/moveablelist';

--- windowlib:attributes ---

compstack:
  (# returnAdrs: [1]@Integer;
     returnObjs: [1]@Integer;
     last: @Integer;
  #);

stackview: moveablelist
  (# lastStack: ^compstack;
     theComp: ^
     contentsType::<
       (# listType::<
            (# 
            #);
       #);
     doUpdate::
       (# 
       #);
  #);
