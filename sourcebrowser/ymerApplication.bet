ORIGIN '~beta/basiclib/v1.4/basicsystemenv';

INCLUDE '~beta/guienv/v1.3.1/guienvsystemenv';

INCLUDE 'ymer';

INCLUDE 'toolsMenu';

--- program: descriptor ---
systemenv
(# fragmentBrowser: @ymerApplication
     (# ymerBrowserWindow::
          (# eventhandler::
               (# #);
             open::
               (# #);
             menubarType::
               (# open::
                    (# m: ^menu do &toolsMenu[]->m[]; m.open; m[]->append #)
               #)
          #);
        init::
          (#
          do  (*('Ymer: Short Manual(2)', '/users/jlk/ymer/v0.3/ymer.html')
               ->ymerBrowser.helpDocument;*)
          #);
     #);
   
   setWindowEnv:: (# do fragmentBrowser[]->theWindowEnv[] #);
     
do fragmentBrowser.init;
   (for i: noOfArguments-1 repeat
        i+1->arguments->fragmentbrowser.ymerbrowser.appendProject
   for);
   (if noOfArguments-1>0 then fragmentbrowser.ymerbrowser.appendDone if);
#)
