ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/toollibs/utils/mpsinterface'
        '~beta/objectbrowser/betagrammarinit'
        '../sourcebrowser';
BODY '~beta/editor/private/nouser'
(*
 * COPYRIGHT
 *   Copyright (C) Aarhus University
 *   All rights reserved.
 *)
---program:descriptor---
guienv
(# mps: @mpsinterface
     (# init::
          (#  
            do betagrammarinit
          #);  
     #);
   win: @window
     (# sb: @sourcebrowser
          (# eventhandler:: 
               (# onFatherFrameChanged:: 
                    (# do win.size->size #)
               #);
          #);
        eventhandler::
          (# onAboutToClose:: (# do terminate; true->okToClose #) #);
        open::
          (# 
          do sb.open;
             sb.size->size;
          #);
     #);
do mps.init;
   win.open;
   'version: (1.2)'->putline;
#)
