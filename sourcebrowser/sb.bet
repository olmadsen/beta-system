ORIGIN '~beta/basiclib/v1.4/betaenv';
INCLUDE 'directorybrowser';
---program:descriptor---
guienv
(# mps: @mpsinterface
     (# init::
          (# 
          do betagrammarinit
          #);
     #);
   win: @window
     (# db: @directorybrowser;
        open::
          (# 
          do (NONE,mps[])->db.open;
             db.size->size;
          #);
     #);
do mps.init;
   win.open;
#)
