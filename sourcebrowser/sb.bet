ORIGIN '~beta/basiclib/v1.4/betaenv';
INCLUDE '~beta/basiclib/v1.4/file';
INCLUDE 'directorybrowser';
---program:descriptor---
guienv
(# mps: mpsinterface
     (# init::
          (#  
          do betagrammarinit
          #);
     #);
   win: window
     (# db: @sourcebrowser;
        open::
          (# 
          do db.open;
             db.size->size;
          #);
     #);
do mps.init;
   win.open;
#)
