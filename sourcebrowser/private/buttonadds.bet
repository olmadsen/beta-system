ORIGIN '~beta/guienv/v1.3/controls';

INCLUDE '~beta/guienv/v1.3/private/controlsbody'

--- buttonlib: attributes ---
labelHeight: 
  (# ts: ^textStyle;
  do style->ts[];
  exit ts.lineHeight
  #);
labelWidth: 
  (# ts: ^textStyle;
  do style->ts[];
  exit (label->ts.widthOfText)
  #);
