#!/usr/local/bin/perl -s

$betalib = $ENV{'BETALIB'};
foreach $f (glob "java/*/*.bet"){
    $f =~ s/\.bet$//;
    next if ($f =~ m%/_[^/]+$%); # skip wrapper classes
    print "************ $f ************\n";
    system "cd $betalib/compiler/GENERATOR/JAVA; java2beta -F $f";
}
