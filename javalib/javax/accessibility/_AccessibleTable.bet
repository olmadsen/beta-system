ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java AccessibleTable class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AccessibleTable.bet for members.
 *)
_AccessibleTable: ExternalClass
  (#
  do '|javax/accessibility/AccessibleTable' -> className;
     INNER;
  #);

