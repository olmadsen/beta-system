ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java AccessibleIcon class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AccessibleIcon.bet for members.
 *)
_AccessibleIcon: ExternalClass
  (#
  do '|javax/accessibility/AccessibleIcon' -> className;
     INNER;
  #);

