ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java AccessibleText class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AccessibleText.bet for members.
 *)
_AccessibleText: ExternalClass
  (#
  do '|javax/accessibility/AccessibleText' -> className;
     INNER;
  #);

