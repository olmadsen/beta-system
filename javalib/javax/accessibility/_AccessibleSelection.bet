ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java AccessibleSelection class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AccessibleSelection.bet for members.
 *)
_AccessibleSelection: ExternalClass
  (#
  do '|javax/accessibility/AccessibleSelection' -> className;
     INNER;
  #);

