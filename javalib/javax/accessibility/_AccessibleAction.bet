ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java AccessibleAction class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AccessibleAction.bet for members.
 *)
_AccessibleAction: ExternalClass
  (#
  do '|javax/accessibility/AccessibleAction' -> className;
     INNER;
  #);

