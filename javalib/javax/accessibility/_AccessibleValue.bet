ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java AccessibleValue class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AccessibleValue.bet for members.
 *)
_AccessibleValue: ExternalClass
  (#
  do '|javax/accessibility/AccessibleValue' -> className;
     INNER;
  #);

