ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java PropertyChangeSupport class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See PropertyChangeSupport.bet for members.
 *)
_PropertyChangeSupport: ExternalClass
  (#
  do 'java/beans/PropertyChangeSupport' -> className;
     INNER;
  #);

