ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java PropertyChangeListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See PropertyChangeListener.bet for members.
 *)
_PropertyChangeListener: ExternalClass
  (#
  do '|java/beans/PropertyChangeListener' -> className;
     INNER;
  #);

