ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/util/EventObject';
--LIB: attributes--

(* Java AWTEvent class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AWTEvent.bet for members.
 *)
_AWTEvent: EventObject
  (#
  do 'java/awt/AWTEvent' -> className;
     INNER;
  #);

