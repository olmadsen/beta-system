ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ItemListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ItemListener.bet for members.
 *)
_ItemListener: ExternalClass
  (#
  do '|java/awt/event/ItemListener' -> className;
     INNER;
  #);

