ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java WindowStateListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See WindowStateListener.bet for members.
 *)
_WindowStateListener: ExternalClass
  (#
  do '|java/awt/event/WindowStateListener' -> className;
     INNER;
  #);

