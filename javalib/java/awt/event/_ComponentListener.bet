ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ComponentListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ComponentListener.bet for members.
 *)
_ComponentListener: ExternalClass
  (#
  do '|java/awt/event/ComponentListener' -> className;
     INNER;
  #);

