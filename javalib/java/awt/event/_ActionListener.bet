ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ActionListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ActionListener.bet for members.
 *)
_ActionListener: ExternalClass
  (#
  do '|java/awt/event/ActionListener' -> className;
     INNER;
  #);

