ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java FocusListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FocusListener.bet for members.
 *)
_FocusListener: ExternalClass
  (#
  do '|java/awt/event/FocusListener' -> className;
     INNER;
  #);

