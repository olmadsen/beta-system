ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java MouseListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See MouseListener.bet for members.
 *)
_MouseListener: ExternalClass
  (#
  do '|java/awt/event/MouseListener' -> className;
     INNER;
  #);

