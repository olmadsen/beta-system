ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java WindowFocusListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See WindowFocusListener.bet for members.
 *)
_WindowFocusListener: ExternalClass
  (#
  do '|java/awt/event/WindowFocusListener' -> className;
     INNER;
  #);

