ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java TextListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See TextListener.bet for members.
 *)
_TextListener: ExternalClass
  (#
  do '|java/awt/event/TextListener' -> className;
     INNER;
  #);

