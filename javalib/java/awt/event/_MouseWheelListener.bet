ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java MouseWheelListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See MouseWheelListener.bet for members.
 *)
_MouseWheelListener: ExternalClass
  (#
  do '|java/awt/event/MouseWheelListener' -> className;
     INNER;
  #);

