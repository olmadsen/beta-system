ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java MouseMotionListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See MouseMotionListener.bet for members.
 *)
_MouseMotionListener: ExternalClass
  (#
  do '|java/awt/event/MouseMotionListener' -> className;
     INNER;
  #);

