ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java KeyListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See KeyListener.bet for members.
 *)
_KeyListener: ExternalClass
  (#
  do '|java/awt/event/KeyListener' -> className;
     INNER;
  #);

