ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java WindowListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See WindowListener.bet for members.
 *)
_WindowListener: ExternalClass
  (#
  do '|java/awt/event/WindowListener' -> className;
     INNER;
  #);

