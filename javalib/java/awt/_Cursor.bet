ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Cursor class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Cursor.bet for members.
 *)
_Cursor: ExternalClass
  (#
  do 'java/awt/Cursor' -> className;
     INNER;
  #);

