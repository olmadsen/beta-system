ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ImageCapabilities class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ImageCapabilities.bet for members.
 *)
_ImageCapabilities: ExternalClass
  (#
  do 'java/awt/ImageCapabilities' -> className;
     INNER;
  #);

