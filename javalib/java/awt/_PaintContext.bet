ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java PaintContext class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See PaintContext.bet for members.
 *)
_PaintContext: ExternalClass
  (#
  do '|java/awt/PaintContext' -> className;
     INNER;
  #);

