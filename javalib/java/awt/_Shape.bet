ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Shape class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Shape.bet for members.
 *)
_Shape: ExternalClass
  (#
  do '|java/awt/Shape' -> className;
     INNER;
  #);

