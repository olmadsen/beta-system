ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java GraphicsConfigTemplate class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See GraphicsConfigTemplate.bet for members.
 *)
_GraphicsConfigTemplate: ExternalClass
  (#
  do 'java/awt/GraphicsConfigTemplate' -> className;
     INNER;
  #);

