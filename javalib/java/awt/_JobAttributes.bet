ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java JobAttributes class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See JobAttributes.bet for members.
 *)
_JobAttributes: ExternalClass
  (#
  do 'java/awt/JobAttributes' -> className;
     INNER;
  #);

