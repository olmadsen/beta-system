ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java DisplayMode class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See DisplayMode.bet for members.
 *)
_DisplayMode: ExternalClass
  (#
  do 'java/awt/DisplayMode' -> className;
     INNER;
  #);

