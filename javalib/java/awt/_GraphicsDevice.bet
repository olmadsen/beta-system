ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java GraphicsDevice class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See GraphicsDevice.bet for members.
 *)
_GraphicsDevice: ExternalClass
  (#
  do 'java/awt/GraphicsDevice' -> className;
     INNER;
  #);

