ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/awt/Container';
--LIB: attributes--

(* Java ScrollPane class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ScrollPane.bet for members.
 *)
_ScrollPane: Container
  (#
  do 'java/awt/ScrollPane' -> className;
     INNER;
  #);

