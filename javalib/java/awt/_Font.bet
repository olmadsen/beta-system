ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Font class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Font.bet for members.
 *)
_Font: ExternalClass
  (#
  do 'java/awt/Font' -> className;
     INNER;
  #);

