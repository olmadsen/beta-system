ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/awt/geom/Point2D';
--LIB: attributes--

(* Java Point class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Point.bet for members.
 *)
_Point: Point2D
  (#
  do 'java/awt/Point' -> className;
     INNER;
  #);

