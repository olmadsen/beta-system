ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/awt/Dialog';
--LIB: attributes--

(* Java FileDialog class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FileDialog.bet for members.
 *)
_FileDialog: Dialog
  (#
  do 'java/awt/FileDialog' -> className;
     INNER;
  #);

