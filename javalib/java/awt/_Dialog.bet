ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/awt/Window';
--LIB: attributes--

(* Java Dialog class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Dialog.bet for members.
 *)
_Dialog: Window
  (#
  do 'java/awt/Dialog' -> className;
     INNER;
  #);

