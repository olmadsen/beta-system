ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/awt/MenuItem';
--LIB: attributes--

(* Java CheckboxMenuItem class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See CheckboxMenuItem.bet for members.
 *)
_CheckboxMenuItem: MenuItem
  (#
  do 'java/awt/CheckboxMenuItem' -> className;
     INNER;
  #);

