ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/awt/Component';
--LIB: attributes--

(* Java Button class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Button.bet for members.
 *)
_Button: Component
  (#
  do 'java/awt/Button' -> className;
     INNER;
  #);

