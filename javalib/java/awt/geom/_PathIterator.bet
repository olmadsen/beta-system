ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java PathIterator class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See PathIterator.bet for members.
 *)
_PathIterator: ExternalClass
  (#
  do '|java/awt/geom/PathIterator' -> className;
     INNER;
  #);

