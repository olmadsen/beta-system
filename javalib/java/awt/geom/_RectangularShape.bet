ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java RectangularShape class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See RectangularShape.bet for members.
 *)
_RectangularShape: ExternalClass
  (#
  do 'java/awt/geom/RectangularShape' -> className;
     INNER;
  #);

