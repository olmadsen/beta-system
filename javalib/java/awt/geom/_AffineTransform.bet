ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java AffineTransform class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AffineTransform.bet for members.
 *)
_AffineTransform: ExternalClass
  (#
  do 'java/awt/geom/AffineTransform' -> className;
     INNER;
  #);

