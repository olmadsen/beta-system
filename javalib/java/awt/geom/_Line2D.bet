ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Line2D class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Line2D.bet for members.
 *)
_Line2D: ExternalClass
  (#
  do 'java/awt/geom/Line2D' -> className;
     INNER;
  #);

