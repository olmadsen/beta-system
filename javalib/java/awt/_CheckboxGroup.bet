ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java CheckboxGroup class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See CheckboxGroup.bet for members.
 *)
_CheckboxGroup: ExternalClass
  (#
  do 'java/awt/CheckboxGroup' -> className;
     INNER;
  #);

