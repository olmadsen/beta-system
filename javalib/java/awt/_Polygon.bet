ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Polygon class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Polygon.bet for members.
 *)
_Polygon: ExternalClass
  (#
  do 'java/awt/Polygon' -> className;
     INNER;
  #);

