ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Image class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Image.bet for members.
 *)
_Image: ExternalClass
  (#
  do 'java/awt/Image' -> className;
     INNER;
  #);

