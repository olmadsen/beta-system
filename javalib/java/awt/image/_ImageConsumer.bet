ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ImageConsumer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ImageConsumer.bet for members.
 *)
_ImageConsumer: ExternalClass
  (#
  do '|java/awt/image/ImageConsumer' -> className;
     INNER;
  #);

