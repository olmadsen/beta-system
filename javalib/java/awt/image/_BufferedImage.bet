ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/awt/Image';
--LIB: attributes--

(* Java BufferedImage class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See BufferedImage.bet for members.
 *)
_BufferedImage: Image
  (#
  do 'java/awt/image/BufferedImage' -> className;
     INNER;
  #);

