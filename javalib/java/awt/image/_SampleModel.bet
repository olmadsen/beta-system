ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java SampleModel class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See SampleModel.bet for members.
 *)
_SampleModel: ExternalClass
  (#
  do 'java/awt/image/SampleModel' -> className;
     INNER;
  #);

