ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java BufferStrategy class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See BufferStrategy.bet for members.
 *)
_BufferStrategy: ExternalClass
  (#
  do 'java/awt/image/BufferStrategy' -> className;
     INNER;
  #);

