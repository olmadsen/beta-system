ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java BufferedImageOp class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See BufferedImageOp.bet for members.
 *)
_BufferedImageOp: ExternalClass
  (#
  do '|java/awt/image/BufferedImageOp' -> className;
     INNER;
  #);

