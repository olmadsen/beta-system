ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ColorModel class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ColorModel.bet for members.
 *)
_ColorModel: ExternalClass
  (#
  do 'java/awt/image/ColorModel' -> className;
     INNER;
  #);

