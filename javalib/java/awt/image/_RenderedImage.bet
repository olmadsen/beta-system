ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java RenderedImage class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See RenderedImage.bet for members.
 *)
_RenderedImage: ExternalClass
  (#
  do '|java/awt/image/RenderedImage' -> className;
     INNER;
  #);

