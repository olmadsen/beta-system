ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/awt/image/Raster';
--LIB: attributes--

(* Java WritableRaster class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See WritableRaster.bet for members.
 *)
_WritableRaster: Raster
  (#
  do 'java/awt/image/WritableRaster' -> className;
     INNER;
  #);

