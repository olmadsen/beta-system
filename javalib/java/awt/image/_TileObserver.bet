ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java TileObserver class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See TileObserver.bet for members.
 *)
_TileObserver: ExternalClass
  (#
  do '|java/awt/image/TileObserver' -> className;
     INNER;
  #);

