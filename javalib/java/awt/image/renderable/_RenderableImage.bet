ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java RenderableImage class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See RenderableImage.bet for members.
 *)
_RenderableImage: ExternalClass
  (#
  do '|java/awt/image/renderable/RenderableImage' -> className;
     INNER;
  #);

