ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java RenderContext class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See RenderContext.bet for members.
 *)
_RenderContext: ExternalClass
  (#
  do 'java/awt/image/renderable/RenderContext' -> className;
     INNER;
  #);

