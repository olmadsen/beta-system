ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ImageObserver class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ImageObserver.bet for members.
 *)
_ImageObserver: ExternalClass
  (#
  do '|java/awt/image/ImageObserver' -> className;
     INNER;
  #);

