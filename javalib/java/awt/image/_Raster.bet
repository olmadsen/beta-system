ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Raster class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Raster.bet for members.
 *)
_Raster: ExternalClass
  (#
  do 'java/awt/image/Raster' -> className;
     INNER;
  #);

