ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java DataBuffer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See DataBuffer.bet for members.
 *)
_DataBuffer: ExternalClass
  (#
  do 'java/awt/image/DataBuffer' -> className;
     INNER;
  #);

