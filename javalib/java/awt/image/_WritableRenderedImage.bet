ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java WritableRenderedImage class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See WritableRenderedImage.bet for members.
 *)
_WritableRenderedImage: ExternalClass
  (#
  do '|java/awt/image/WritableRenderedImage' -> className;
     INNER;
  #);

