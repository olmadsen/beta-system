ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ImageProducer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ImageProducer.bet for members.
 *)
_ImageProducer: ExternalClass
  (#
  do '|java/awt/image/ImageProducer' -> className;
     INNER;
  #);

