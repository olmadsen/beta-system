ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java LayoutManager class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See LayoutManager.bet for members.
 *)
_LayoutManager: ExternalClass
  (#
  do '|java/awt/LayoutManager' -> className;
     INNER;
  #);

