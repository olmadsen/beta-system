ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/awt/Component';
--LIB: attributes--

(* Java Choice class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Choice.bet for members.
 *)
_Choice: Component
  (#
  do 'java/awt/Choice' -> className;
     INNER;
  #);

