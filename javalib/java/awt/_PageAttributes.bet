ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java PageAttributes class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See PageAttributes.bet for members.
 *)
_PageAttributes: ExternalClass
  (#
  do 'java/awt/PageAttributes' -> className;
     INNER;
  #);

