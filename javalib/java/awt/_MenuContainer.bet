ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java MenuContainer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See MenuContainer.bet for members.
 *)
_MenuContainer: ExternalClass
  (#
  do '|java/awt/MenuContainer' -> className;
     INNER;
  #);

