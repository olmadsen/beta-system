ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java FramePeer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FramePeer.bet for members.
 *)
_FramePeer: ExternalClass
  (#
  do '|java/awt/peer/FramePeer' -> className;
     INNER;
  #);

