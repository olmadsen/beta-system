ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java WindowPeer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See WindowPeer.bet for members.
 *)
_WindowPeer: ExternalClass
  (#
  do '|java/awt/peer/WindowPeer' -> className;
     INNER;
  #);

