ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java CanvasPeer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See CanvasPeer.bet for members.
 *)
_CanvasPeer: ExternalClass
  (#
  do '|java/awt/peer/CanvasPeer' -> className;
     INNER;
  #);

