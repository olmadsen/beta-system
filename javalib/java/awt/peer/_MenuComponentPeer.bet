ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java MenuComponentPeer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See MenuComponentPeer.bet for members.
 *)
_MenuComponentPeer: ExternalClass
  (#
  do '|java/awt/peer/MenuComponentPeer' -> className;
     INNER;
  #);

