ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java GlyphMetrics class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See GlyphMetrics.bet for members.
 *)
_GlyphMetrics: ExternalClass
  (#
  do 'java/awt/font/GlyphMetrics' -> className;
     INNER;
  #);

