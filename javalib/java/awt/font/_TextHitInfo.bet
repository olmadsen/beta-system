ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java TextHitInfo class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See TextHitInfo.bet for members.
 *)
_TextHitInfo: ExternalClass
  (#
  do 'java/awt/font/TextHitInfo' -> className;
     INNER;
  #);

