ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java LineMetrics class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See LineMetrics.bet for members.
 *)
_LineMetrics: ExternalClass
  (#
  do 'java/awt/font/LineMetrics' -> className;
     INNER;
  #);

