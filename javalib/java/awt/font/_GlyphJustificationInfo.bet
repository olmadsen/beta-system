ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java GlyphJustificationInfo class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See GlyphJustificationInfo.bet for members.
 *)
_GlyphJustificationInfo: ExternalClass
  (#
  do 'java/awt/font/GlyphJustificationInfo' -> className;
     INNER;
  #);

