ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/awt/Window';
--LIB: attributes--

(* Java Frame class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Frame.bet for members.
 *)
_Frame: Window
  (#
  do 'java/awt/Frame' -> className;
     INNER;
  #);

