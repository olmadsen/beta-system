ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Color class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Color.bet for members.
 *)
_Color: ExternalClass
  (#
  do 'java/awt/Color' -> className;
     INNER;
  #);

