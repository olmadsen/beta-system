ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/awt/Container';
--LIB: attributes--

(* Java Window class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Window.bet for members.
 *)
_Window: Container
  (#
  do 'java/awt/Window' -> className;
     INNER;
  #);

