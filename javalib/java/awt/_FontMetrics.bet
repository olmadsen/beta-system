ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java FontMetrics class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FontMetrics.bet for members.
 *)
_FontMetrics: ExternalClass
  (#
  do 'java/awt/FontMetrics' -> className;
     INNER;
  #);

