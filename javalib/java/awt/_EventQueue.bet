ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java EventQueue class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See EventQueue.bet for members.
 *)
_EventQueue: ExternalClass
  (#
  do 'java/awt/EventQueue' -> className;
     INNER;
  #);

