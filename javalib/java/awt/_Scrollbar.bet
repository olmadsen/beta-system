ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/awt/Component';
--LIB: attributes--

(* Java Scrollbar class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Scrollbar.bet for members.
 *)
_Scrollbar: Component
  (#
  do 'java/awt/Scrollbar' -> className;
     INNER;
  #);

