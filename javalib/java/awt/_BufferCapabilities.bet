ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java BufferCapabilities class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See BufferCapabilities.bet for members.
 *)
_BufferCapabilities: ExternalClass
  (#
  do 'java/awt/BufferCapabilities' -> className;
     INNER;
  #);

