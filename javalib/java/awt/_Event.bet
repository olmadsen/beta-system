ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Event class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Event.bet for members.
 *)
_Event: ExternalClass
  (#
  do 'java/awt/Event' -> className;
     INNER;
  #);

