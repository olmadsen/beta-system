ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Stroke class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Stroke.bet for members.
 *)
_Stroke: ExternalClass
  (#
  do '|java/awt/Stroke' -> className;
     INNER;
  #);

