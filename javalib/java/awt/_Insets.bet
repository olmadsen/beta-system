ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Insets class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Insets.bet for members.
 *)
_Insets: ExternalClass
  (#
  do 'java/awt/Insets' -> className;
     INNER;
  #);

