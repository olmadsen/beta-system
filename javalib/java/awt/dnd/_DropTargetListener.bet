ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java DropTargetListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See DropTargetListener.bet for members.
 *)
_DropTargetListener: ExternalClass
  (#
  do '|java/awt/dnd/DropTargetListener' -> className;
     INNER;
  #);

