ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java DropTarget class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See DropTarget.bet for members.
 *)
_DropTarget: ExternalClass
  (#
  do 'java/awt/dnd/DropTarget' -> className;
     INNER;
  #);

