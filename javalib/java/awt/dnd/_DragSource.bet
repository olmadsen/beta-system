ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java DragSource class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See DragSource.bet for members.
 *)
_DragSource: ExternalClass
  (#
  do 'java/awt/dnd/DragSource' -> className;
     INNER;
  #);

