ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java DragSourceListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See DragSourceListener.bet for members.
 *)
_DragSourceListener: ExternalClass
  (#
  do '|java/awt/dnd/DragSourceListener' -> className;
     INNER;
  #);

