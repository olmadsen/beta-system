ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Transferable class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Transferable.bet for members.
 *)
_Transferable: ExternalClass
  (#
  do '|java/awt/datatransfer/Transferable' -> className;
     INNER;
  #);

