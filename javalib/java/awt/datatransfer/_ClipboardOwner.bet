ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ClipboardOwner class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ClipboardOwner.bet for members.
 *)
_ClipboardOwner: ExternalClass
  (#
  do '|java/awt/datatransfer/ClipboardOwner' -> className;
     INNER;
  #);

