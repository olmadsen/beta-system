ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java FlavorMap class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FlavorMap.bet for members.
 *)
_FlavorMap: ExternalClass
  (#
  do '|java/awt/datatransfer/FlavorMap' -> className;
     INNER;
  #);

