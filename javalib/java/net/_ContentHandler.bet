ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ContentHandler class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ContentHandler.bet for members.
 *)
_ContentHandler: ExternalClass
  (#
  do 'java/net/ContentHandler' -> className;
     INNER;
  #);

