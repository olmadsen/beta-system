ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java URLConnection class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See URLConnection.bet for members.
 *)
_URLConnection: ExternalClass
  (#
  do 'java/net/URLConnection' -> className;
     INNER;
  #);

