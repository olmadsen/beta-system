ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java URL class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See URL.bet for members.
 *)
_URL: ExternalClass
  (#
  do 'java/net/URL' -> className;
     INNER;
  #);

