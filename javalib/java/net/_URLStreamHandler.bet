ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java URLStreamHandler class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See URLStreamHandler.bet for members.
 *)
_URLStreamHandler: ExternalClass
  (#
  do 'java/net/URLStreamHandler' -> className;
     INNER;
  #);

