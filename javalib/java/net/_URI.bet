ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java URI class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See URI.bet for members.
 *)
_URI: ExternalClass
  (#
  do 'java/net/URI' -> className;
     INNER;
  #);

