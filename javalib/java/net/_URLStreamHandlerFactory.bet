ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java URLStreamHandlerFactory class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See URLStreamHandlerFactory.bet for members.
 *)
_URLStreamHandlerFactory: ExternalClass
  (#
  do '|java/net/URLStreamHandlerFactory' -> className;
     INNER;
  #);

