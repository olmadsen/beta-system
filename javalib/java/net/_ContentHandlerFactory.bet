ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ContentHandlerFactory class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ContentHandlerFactory.bet for members.
 *)
_ContentHandlerFactory: ExternalClass
  (#
  do 'java/net/ContentHandlerFactory' -> className;
     INNER;
  #);

