ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java InetAddress class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See InetAddress.bet for members.
 *)
_InetAddress: ExternalClass
  (#
  do 'java/net/InetAddress' -> className;
     INNER;
  #);

