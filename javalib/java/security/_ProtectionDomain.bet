ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ProtectionDomain class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ProtectionDomain.bet for members.
 *)
_ProtectionDomain: ExternalClass
  (#
  do 'java/security/ProtectionDomain' -> className;
     INNER;
  #);

