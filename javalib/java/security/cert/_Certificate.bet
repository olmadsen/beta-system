ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Certificate class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Certificate.bet for members.
 *)
_Certificate: ExternalClass
  (#
  do 'java/security/cert/Certificate' -> className;
     INNER;
  #);

