ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java PermissionCollection class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See PermissionCollection.bet for members.
 *)
_PermissionCollection: ExternalClass
  (#
  do 'java/security/PermissionCollection' -> className;
     INNER;
  #);

