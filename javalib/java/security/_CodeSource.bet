ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java CodeSource class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See CodeSource.bet for members.
 *)
_CodeSource: ExternalClass
  (#
  do 'java/security/CodeSource' -> className;
     INNER;
  #);

