ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java PublicKey class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See PublicKey.bet for members.
 *)
_PublicKey: ExternalClass
  (#
  do 'java/security/PublicKey' -> className;
     INNER;
  #);

