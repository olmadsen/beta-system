ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Principal class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Principal.bet for members.
 *)
_Principal: ExternalClass
  (#
  do 'java/security/Principal' -> className;
     INNER;
  #);

