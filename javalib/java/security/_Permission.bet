ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Permission class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Permission.bet for members.
 *)
_Permission: ExternalClass
  (#
  do 'java/security/Permission' -> className;
     INNER;
  #);

