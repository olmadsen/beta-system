ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Collection class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Collection.bet for members.
 *)
_Collection: ExternalClass
  (#
  do '|java/util/Collection' -> className;
     INNER;
  #);

