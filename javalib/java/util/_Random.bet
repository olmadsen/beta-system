ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Random class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Random.bet for members.
 *)
_Random: ExternalClass
  (#
  do 'java/util/Random' -> className;
     INNER;
  #);

