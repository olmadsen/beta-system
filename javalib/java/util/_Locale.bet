ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Locale class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Locale.bet for members.
 *)
_Locale: ExternalClass
  (#
  do 'java/util/Locale' -> className;
     INNER;
  #);

