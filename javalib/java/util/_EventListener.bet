ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java EventListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See EventListener.bet for members.
 *)
_EventListener: ExternalClass
  (#
  do '|java/util/EventListener' -> className;
     INNER;
  #);

