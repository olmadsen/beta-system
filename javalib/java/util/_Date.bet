ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Date class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Date.bet for members.
 *)
_Date: ExternalClass
  (#
  do 'java/util/Date' -> className;
     INNER;
  #);

