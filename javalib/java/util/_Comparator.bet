ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Comparator class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Comparator.bet for members.
 *)
_Comparator: ExternalClass
  (#
  do 'java/util/Comparator' -> className;
     INNER;
  #);

