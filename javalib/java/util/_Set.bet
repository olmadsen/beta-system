ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Set class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Set.bet for members.
 *)
_Set: ExternalClass
  (#
  do 'java/util/Set' -> className;
     INNER;
  #);

