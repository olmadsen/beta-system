ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java AbstractCollection class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AbstractCollection.bet for members.
 *)
_AbstractCollection: ExternalClass
  (#
  do 'java/util/AbstractCollection' -> className;
     INNER;
  #);

