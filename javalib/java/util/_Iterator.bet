ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Iterator class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Iterator.bet for members.
 *)
_Iterator: ExternalClass
  (#
  do 'java/util/Iterator' -> className;
     INNER;
  #);

