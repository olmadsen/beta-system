ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Map class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Map.bet for members.
 *)
_Map: ExternalClass
  (#
  do 'java/util/Map' -> className;
     INNER;
  #);

