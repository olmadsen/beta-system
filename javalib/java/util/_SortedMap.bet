ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java SortedMap class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See SortedMap.bet for members.
 *)
_SortedMap: ExternalClass
  (#
  do '|java/util/SortedMap' -> className;
     INNER;
  #);

