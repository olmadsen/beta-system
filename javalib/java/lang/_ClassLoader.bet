ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ClassLoader class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ClassLoader.bet for members.
 *)
_ClassLoader: ExternalClass
  (#
  do 'java/lang/ClassLoader' -> className;
     INNER;
  #);

