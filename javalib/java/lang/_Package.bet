ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Package class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Package.bet for members.
 *)
_Package: ExternalClass
  (#
  do 'java/lang/Package' -> className;
     INNER;
  #);

