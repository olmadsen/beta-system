ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Runtime class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Runtime.bet for members.
 *)
_Runtime: ExternalClass
  (#
  do 'java/lang/Runtime' -> className;
     INNER;
  #);

