ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Math class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Math.bet for members.
 *)
_Math: ExternalClass
  (#
  do 'java/lang/Math' -> className;
     INNER;
  #);

