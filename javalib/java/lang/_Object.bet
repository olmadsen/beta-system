ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Object class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Object.bet for members.
 *)
_Object: ExternalClass
  (#
  do 'java/lang/Object' -> className;
     INNER;
  #);

