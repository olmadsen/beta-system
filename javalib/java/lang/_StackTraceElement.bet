ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java StackTraceElement class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See StackTraceElement.bet for members.
 *)
_StackTraceElement: ExternalClass
  (#
  do 'java/lang/StackTraceElement' -> className;
     INNER;
  #);

