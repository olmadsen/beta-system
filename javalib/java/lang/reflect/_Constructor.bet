ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/lang/reflect/AccessibleObject';
--LIB: attributes--

(* Java Constructor class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Constructor.bet for members.
 *)
_Constructor: AccessibleObject
  (#
  do 'java/lang/reflect/Constructor' -> className;
     INNER;
  #);

