ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java AccessibleObject class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AccessibleObject.bet for members.
 *)
_AccessibleObject: ExternalClass
  (#
  do 'java/lang/reflect/AccessibleObject' -> className;
     INNER;
  #);

