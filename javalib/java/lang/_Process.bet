ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Process class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Process.bet for members.
 *)
_Process: ExternalClass
  (#
  do 'java/lang/Process' -> className;
     INNER;
  #);

