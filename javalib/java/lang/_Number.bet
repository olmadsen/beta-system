ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Number class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Number.bet for members.
 *)
_Number: ExternalClass
  (#
  do 'java/lang/Number' -> className;
     INNER;
  #);

