ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java SecurityManager class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See SecurityManager.bet for members.
 *)
_SecurityManager: ExternalClass
  (#
  do 'java/lang/SecurityManager' -> className;
     INNER;
  #);

