ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java String class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See String.bet for members.
 *)
_String: ExternalClass
  (#
  do 'java/lang/String' -> className;
     INNER;
  #);

