ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Thread class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Thread.bet for members.
 *)
_Thread: ExternalClass
  (#
  do 'java/lang/Thread' -> className;
     INNER;
  #);

