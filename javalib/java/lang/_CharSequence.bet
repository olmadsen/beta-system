ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java CharSequence class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See CharSequence.bet for members.
 *)
_CharSequence: ExternalClass
  (#
  do 'java/lang/CharSequence' -> className;
     INNER;
  #);

