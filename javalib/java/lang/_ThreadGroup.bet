ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ThreadGroup class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ThreadGroup.bet for members.
 *)
_ThreadGroup: ExternalClass
  (#
  do 'java/lang/ThreadGroup' -> className;
     INNER;
  #);

