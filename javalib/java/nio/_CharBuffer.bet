ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/nio/Buffer';
--LIB: attributes--

(* Java CharBuffer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See CharBuffer.bet for members.
 *)
_CharBuffer: Buffer
  (#
  do 'java/nio/CharBuffer' -> className;
     INNER;
  #);

