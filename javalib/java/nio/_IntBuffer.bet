ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/nio/Buffer';
--LIB: attributes--

(* Java IntBuffer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IntBuffer.bet for members.
 *)
_IntBuffer: Buffer
  (#
  do 'java/nio/IntBuffer' -> className;
     INNER;
  #);

