ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/nio/Buffer';
--LIB: attributes--

(* Java ShortBuffer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ShortBuffer.bet for members.
 *)
_ShortBuffer: Buffer
  (#
  do 'java/nio/ShortBuffer' -> className;
     INNER;
  #);

