ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/nio/Buffer';
--LIB: attributes--

(* Java ByteBuffer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ByteBuffer.bet for members.
 *)
_ByteBuffer: Buffer
  (#
  do 'java/nio/ByteBuffer' -> className;
     INNER;
  #);

