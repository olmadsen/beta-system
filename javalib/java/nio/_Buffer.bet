ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Buffer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Buffer.bet for members.
 *)
_Buffer: ExternalClass
  (#
  do 'java/nio/Buffer' -> className;
     INNER;
  #);

