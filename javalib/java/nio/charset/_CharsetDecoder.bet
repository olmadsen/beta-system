ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java CharsetDecoder class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See CharsetDecoder.bet for members.
 *)
_CharsetDecoder: ExternalClass
  (#
  do 'java/nio/charset/CharsetDecoder' -> className;
     INNER;
  #);

