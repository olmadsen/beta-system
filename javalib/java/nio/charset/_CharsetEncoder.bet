ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java CharsetEncoder class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See CharsetEncoder.bet for members.
 *)
_CharsetEncoder: ExternalClass
  (#
  do 'java/nio/charset/CharsetEncoder' -> className;
     INNER;
  #);

