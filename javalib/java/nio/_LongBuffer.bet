ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/nio/Buffer';
--LIB: attributes--

(* Java LongBuffer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See LongBuffer.bet for members.
 *)
_LongBuffer: Buffer
  (#
  do 'java/nio/LongBuffer' -> className;
     INNER;
  #);

