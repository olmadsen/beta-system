ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ReadableByteChannel class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ReadableByteChannel.bet for members.
 *)
_ReadableByteChannel: ExternalClass
  (#
  do 'java/nio/channels/ReadableByteChannel' -> className;
     INNER;
  #);

