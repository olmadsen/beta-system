ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java WritableByteChannel class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See WritableByteChannel.bet for members.
 *)
_WritableByteChannel: ExternalClass
  (#
  do '|java/nio/channels/WritableByteChannel' -> className;
     INNER;
  #);

