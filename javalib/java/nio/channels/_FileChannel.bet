ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/nio/channels/spi/AbstractInterruptibleChannel';
--LIB: attributes--

(* Java FileChannel class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FileChannel.bet for members.
 *)
_FileChannel: AbstractInterruptibleChannel
  (#
  do 'java/nio/channels/FileChannel' -> className;
     INNER;
  #);

