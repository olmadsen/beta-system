ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java FileLock class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FileLock.bet for members.
 *)
_FileLock: ExternalClass
  (#
  do 'java/nio/channels/FileLock' -> className;
     INNER;
  #);

