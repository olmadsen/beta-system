ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java AbstractInterruptibleChannel class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AbstractInterruptibleChannel.bet for members.
 *)
_AbstractInterruptibleChannel: ExternalClass
  (#
  do 'java/nio/channels/spi/AbstractInterruptibleChannel' -> className;
     INNER;
  #);

