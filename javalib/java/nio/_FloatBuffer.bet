ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/nio/Buffer';
--LIB: attributes--

(* Java FloatBuffer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FloatBuffer.bet for members.
 *)
_FloatBuffer: Buffer
  (#
  do 'java/nio/FloatBuffer' -> className;
     INNER;
  #);

