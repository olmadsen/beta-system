ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java AppletContext class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AppletContext.bet for members.
 *)
_AppletContext: ExternalClass
  (#
  do '|java/applet/AppletContext' -> className;
     INNER;
  #);

