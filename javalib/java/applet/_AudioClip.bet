ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java AudioClip class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AudioClip.bet for members.
 *)
_AudioClip: ExternalClass
  (#
  do '|java/applet/AudioClip' -> className;
     INNER;
  #);

