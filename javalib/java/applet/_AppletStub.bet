ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java AppletStub class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AppletStub.bet for members.
 *)
_AppletStub: ExternalClass
  (#
  do '|java/applet/AppletStub' -> className;
     INNER;
  #);

