ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java AttributedCharacterIterator class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AttributedCharacterIterator.bet for members.
 *)
_AttributedCharacterIterator: ExternalClass
  (#
  do '|java/text/AttributedCharacterIterator' -> className;
     INNER;
  #);

