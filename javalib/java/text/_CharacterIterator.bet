ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java CharacterIterator class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See CharacterIterator.bet for members.
 *)
_CharacterIterator: ExternalClass
  (#
  do '|java/text/CharacterIterator' -> className;
     INNER;
  #);

