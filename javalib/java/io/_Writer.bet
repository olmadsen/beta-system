ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Writer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Writer.bet for members.
 *)
_Writer: ExternalClass
  (#
  do 'java/io/Writer' -> className;
     INNER;
  #);

