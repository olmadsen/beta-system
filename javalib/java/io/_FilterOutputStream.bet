ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/io/OutputStream';
--LIB: attributes--

(* Java FilterOutputStream class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FilterOutputStream.bet for members.
 *)
_FilterOutputStream: OutputStream
  (#
  do 'java/io/FilterOutputStream' -> className;
     INNER;
  #);

