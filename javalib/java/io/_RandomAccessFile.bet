ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java RandomAccessFile class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See RandomAccessFile.bet for members.
 *)
_RandomAccessFile: ExternalClass
  (#
  do 'java/io/RandomAccessFile' -> className;
     INNER;
  #);

