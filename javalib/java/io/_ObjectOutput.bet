ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ObjectOutput class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ObjectOutput.bet for members.
 *)
_ObjectOutput: ExternalClass
  (#
  do '|java/io/ObjectOutput' -> className;
     INNER;
  #);

