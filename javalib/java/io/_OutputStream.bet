ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java OutputStream class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See OutputStream.bet for members.
 *)
_OutputStream: ExternalClass
  (#
  do 'java/io/OutputStream' -> className;
     INNER;
  #);

