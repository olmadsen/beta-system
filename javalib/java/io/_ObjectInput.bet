ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java ObjectInput class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ObjectInput.bet for members.
 *)
_ObjectInput: ExternalClass
  (#
  do '|java/io/ObjectInput' -> className;
     INNER;
  #);

