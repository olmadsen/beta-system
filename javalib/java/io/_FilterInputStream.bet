ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/javalib/java/io/InputStream';
--LIB: attributes--

(* Java FilterInputStream class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FilterInputStream.bet for members.
 *)
_FilterInputStream: InputStream
  (#
  do 'java/io/FilterInputStream' -> className;
     INNER;
  #);

