ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java Reader class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Reader.bet for members.
 *)
_Reader: ExternalClass
  (#
  do 'java/io/Reader' -> className;
     INNER;
  #);

