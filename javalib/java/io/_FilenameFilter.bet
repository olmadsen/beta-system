ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java FilenameFilter class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FilenameFilter.bet for members.
 *)
_FilenameFilter: ExternalClass
  (#
  do '|java/io/FilenameFilter' -> className;
     INNER;
  #);

