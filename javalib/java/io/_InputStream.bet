ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java InputStream class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See InputStream.bet for members.
 *)
_InputStream: ExternalClass
  (#
  do 'java/io/InputStream' -> className;
     INNER;
  #);

