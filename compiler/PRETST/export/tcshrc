# UNIX tcsh PRE-BETA setup
# usage: source tcshrc
setenv BETALIB `pwd`
setenv PATH ${BETALIB}/bin:${PATH}
cd $BETALIB/examples
echo Working directory now $PWD
echo BETALIB and PATH set. 
echo Now start using pre-beta for compilation.
