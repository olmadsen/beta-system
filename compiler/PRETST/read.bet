origin 'minienv'
---program:descriptor---
(# ch: @char
do loop:
     (#
     do '?' -> out.put;
        in.get -> ch;
        ch -> out.put; out.newline;
        (if ch<>'!' then restart loop if);
     #)
#)
 
