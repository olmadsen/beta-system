ORIGIN 'minienvbody' ;
INCLUDE 'Queue';
BUILD pre
'pre/BetaComponentComponent.prc'
'BetaComponent.st'
'pre-st -o $0 $1';

BUILD pre
'pre/BetaStructureStructure.prc'
'BetaStructure.st'
'pre-st -o $0 $1';

---run:descriptor---
(# trace: 
     (# exit false #);
   out: @ stream
     (# put::<
          (# write: External  
               (# ch: @char
               enter ch  
               do '%primitive:put' -> callC 
               #);
          do ch -> write
          #) 
        
     #);
   in: @ stream
     (# get::<
          (# read: external 
               (# ch: @char
               do '%primitive:get' -> callC 
               exit ch
               #);
          do read -> ch
          #)
     #);
   
   
do (if trace then 'Start'->out.putline; if);
   (in[],out[],'') -> main_program;
   (if trace then '\nmain ended\n' -> out.puttext; if);
#)



