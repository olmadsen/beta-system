ORIGIN 'minienv' ;
BUILD pre
      'pre/BetaComponent.prc'
      'BetaComponent.st'
      'pre-st -o $0 $1';

BUILD pre
      'pre/BetaStructure.prc'
      'BetaStructure.st'
      'pre-st -o $0 $1';

---scheduler:descriptor---
(#
   stdOut: @ stream
     (# put::<
          (# write: External  
               (# ch: @char
               enter ch 
               do '%primitive:put' -> callC 
               #);
          do ch -> write
          #) 
        
     #);
   stdIn: @ stream
     (# get::<
          (# read: external
               (# ch: @char
               do '%primitive:get' -> callC 
               exit ch
               #);
          do read -> ch
          #)
     #)
do 
   (stdIn[],StdOut[],'') -> main_program   
#)



