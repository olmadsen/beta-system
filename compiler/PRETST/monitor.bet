ORIGIN 'minienv';
---lib:attributes---
monitor:
  (# entry:
       (#
       do mutex.wait; inner; mutex.signal
       #);
     init:<
       (# 
       do mutex.signal; inner
       #);
     mutex: @semaphore
  #)
