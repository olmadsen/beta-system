ORIGIN 'minienv';
INCLUDE 'IO'
(* default REQUIRES BETA 'minienv'*)
---lib:attributes---
(* OLM: 2021.10.19: the syntax in this comment is apparently an experiment
 * to support components. The MinySystem parser oes not hanlde it
 * so we have replaced it with another syntax
thomsen: @
 (# IO: @ ::beta.IO; ( * cannot be used before IO is a first class object * )
 *)
 thomsen: 
 (# IO: @ beta.IO;     
     TobClass:  
       (# out: ^stream;
          T: @text;    
          S: ^text; 
          init: (# enter out[] #);
          hello:  
            (#   
            do 'Thomsen' -> out.putline;
               &text[] -> S[];
               'How are you?' -> S.puttext;
               S[] -> out.putline
            #);
       #)
  #)



  

