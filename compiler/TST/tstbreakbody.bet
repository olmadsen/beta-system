origin 'tstbreak'      
---N:descriptor---
(#
do (if isByteCodeMode then
    else
       'f'->put; false -> b;
       restart K;
       error
   if);
#)
---NN:doPart---
do (if isByteCodeMode then
    else
       L: (if true then          
              <<SLOT koks:descriptor>>;
              (# x: @real do leave L #);
              '?'->put;
              leave L;
              error
          if);
       'g'->put;  
   if)
--koks:descriptor---  
(# #)
