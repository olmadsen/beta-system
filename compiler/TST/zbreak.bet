origin 'tstenv'    
---program:descriptor---
(# ch: @char;
   bb: @boolean;
   B: (# 
      do ch->put; 
         (if not bb then '*'->put; 
             true->bb;  
             ch+1 -> ch;
             restart B 
          else '+'->put;
             leave B;
         if)
      #);
do 'a'->ch;
   B
#)
 
   


 

