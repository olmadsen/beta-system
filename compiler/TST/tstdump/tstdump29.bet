origin '../tstenv';
INCLUDE 'tstdumplib';
(* Testing dump for INNER P *)

-- program: descriptor --
(# 
   a: @|(# pp: @r(# x:^object 
                 do x (* RefNoneErr: Reference is none *)
                 #);
        do pp
        #);
   b: @|(# qq: @q(# do a #);
        do qq
        #);
do b
#)
