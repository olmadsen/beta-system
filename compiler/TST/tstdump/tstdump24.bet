ORIGIN '../tstenv'; 

--PROGRAM:descriptor--
(# a: @|(# p: (# do inner #);
           pp: @p
             (# 
             do 'Now entering infinite loop - interrupt please!' -> putline;
                loop:
                  (# 
                  do restart loop; (* InteruptErr *)
                  #);
             #);
        do pp
        #);
   b: @|(# q: (# do inner #);
           qq: @q(# do a; #);
        do qq
        #);
do b;
#)

