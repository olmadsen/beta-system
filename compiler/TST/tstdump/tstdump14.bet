origin '../tstenv';
BETARUN default '~beta/betarun/v2.6/$/betarun.debug';
-- program: descriptor --
(# 
   a:  (# t: (# z: (# do 1 -> put #) #);
       do INNER
       #);
   
   b: a(# q: ^t;
          qq: @q.z; (* RefNoneErr: Reference is none *)
       do qq
       #);
do b
#)
