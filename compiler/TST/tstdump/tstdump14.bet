origin '../tstenv';
-- program: descriptor --
(# 
   a:  (# t: (# z: (# do 1 -> put #) #);
       do INNER
       #);
   
   b: a(# q: ^t;
          qq: @q.z; (* RefNoneErr: Reference is none (allocating b) *)
       do qq
       #);
do b
#)
