origin '../tstenv';
-- program: descriptor --
(# 
   a: @|(# p: (# do inner #);
           pp: @p
             (# r, s: [1]@integer
             do r[2:3]->s (* RepHighRangeErr: Repetition subrange out of range (high) *)
             #);
        do pp
        #);
   b: @|(# q: (# do inner #);
           qq: @q(# do a; a #);
        do qq
        #);
do b
#)

(* Correct dump:
 
 It is a RepHighRangeErr since the runtiem check is:
 if (low<LowBorder) then RepLowRangeErr
 if (high>Highorder) then RepHighRangeErr
 And low (2) is not less than LowBorder (1).
 This semantics has been requested since people want to
 be able to express an empty slice using low>high.
 
  item <pp#>p# in ~beta/betarun/sun4s/TST/tstdump/tstdump09
    -- a# in ~beta/betarun/sun4s/TST/tstdump/tstdump09
  item pp#<p#> in ~beta/betarun/sun4s/TST/tstdump/tstdump09
    -- a# in ~beta/betarun/sun4s/TST/tstdump/tstdump09
  comp <a#> in ~beta/betarun/sun4s/TST/tstdump/tstdump09
    -- PROGRAM-~ in ~beta/betarun/sun4s/TST/tstdump/tstdump09

  item <qq#>q# in ~beta/betarun/sun4s/TST/tstdump/tstdump09
    -- b# in ~beta/betarun/sun4s/TST/tstdump/tstdump09
  item qq#<q#> in ~beta/betarun/sun4s/TST/tstdump/tstdump09
    -- b# in ~beta/betarun/sun4s/TST/tstdump/tstdump09
  comp <b#> in ~beta/betarun/sun4s/TST/tstdump/tstdump09
    -- PROGRAM-~ in ~beta/betarun/sun4s/TST/tstdump/tstdump09

  item <PROGRAM-~> in ~beta/betarun/sun4s/TST/tstdump/tstdump09
    -- BETAENV-~ in ~beta/betarun/sun4s/TST/tstenv
  basic component in ~beta/betarun/sun4s/TST/tstenv
 
 *)
