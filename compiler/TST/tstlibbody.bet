ORIGIN 'tstlib'  ;
LIBRARY 'tstlib'
[[
---chk:descriptor---
(# 
do (if v//r then ch->put else '!'->put if)
#)
---fill:descriptor---   
(#
do  (if ('a'<=ch) and (ch<='z') //true then
	(for c: 'z'-ch+1 repeat  c+ch-1->put for);
	'0'->ch
    if);
    (for c: '9'-ch+1 repeat c+ch-1->put for)
#)
---]]

