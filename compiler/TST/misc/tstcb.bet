ORIGIN '../tstenv'; 
(* Test intensive immediate callback.
 * Should be able to run in IOA=1K.
 *)

OBJFILE default	'../$/ctstcproc.o';
BETARUN default '../betarun.debug';
MAKE    default '../tstMake.make';
[[
-----program:descriptor-----
(# pp: @put;
   doCallBar: External(# F: ##bar enter F## #);
   bar: External
     (# 
     do cExternalEntry;
        '7'->pp;
     #);
do
   loop: (# do bar##->doCallBar; '2'->pp; restart loop#)
#)
---]]
