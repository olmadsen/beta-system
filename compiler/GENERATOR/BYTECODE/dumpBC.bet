ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/basiclib/textmatch';
INCLUDE 'BCmachine';
-- program: Descriptor --
(# BC: @BCmachine;
   init:<
     (# fn: ^text
     enter fn[]
     do (if '.bin' -> fn.endsWith then
            
            fn[] -> BC.restore;
         else
            'Usage: dumpBC foo.bin' -> putline
        if)
     #)
do
   2->arguments->init; 
   
   (****** other alternatives include
    * BC.emitClr
    * BC.dump
    *)
   BC.emitJasmin;
   
#)  

