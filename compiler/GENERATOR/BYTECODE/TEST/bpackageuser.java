// see bpackage.bet
import foo.bar.*;
import beta.*;

public class bpackageuser
{
    public static void main(String args[])
    { bpackage BP = new bpackage(new tstenv(null));
      BP.go();
    }
}
 
