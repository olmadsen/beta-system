ORIGIN '~beta/basiclib/betaenv';   
MDBODY clr 'amazon_clrbody';

(* 
 *)
---LIB:attributes---
BetaToken: 
  (* The Amazon Developer Token for beta@daimi.au.dk.
   * Used by default below.
   * PLEASE REPLACE with your own key, if you want to use this service.
   *)
  (# exit 'D1C69HCQ94J0BU' #);

AmazonSearch:
  (# 
     private: @<<SLOT SearchAmazonPrivate:descriptor>>;
     SearchAuthor:
       (*  *)
       (# author: ^text;
          token: ^text;
       enter (author[], token[])
       <<SLOT DoSearchAuthor: dopart>>
       #);
  #);
   
--program:descriptor--
(# amazon: @AmazonSearch;
do ('Ole Lehrmann Madsen', BetaToken) -> amazon.SearchAuthor;
#)

