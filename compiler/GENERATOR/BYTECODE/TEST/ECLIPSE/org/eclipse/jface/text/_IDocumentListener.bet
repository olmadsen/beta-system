ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Java IDocumentListener class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IDocumentListener.bet for members.
 *)
_IDocumentListener: ExternalClass
  (#
  do '|org/eclipse/jface/text/IDocumentListener' -> className;
     INNER;
  #);

