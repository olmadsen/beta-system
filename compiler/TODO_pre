TODO list for BETA PRE implementation
-------------------------------------

1.	Leave/restart out of method-activations
	Branch instructions for leave/restart out of
	method activations, stack unwinding as in
   
        (#
 	do ...
	   L: (# foo: (# do ... leave L; ... #)
                 bar: (# do ... restart L; ... #)
              do ...; bar; ...; fo; ...
              #)
        #)

2.	Initialization of arrays revised - currently
	no init in case of R.new and R.extend
          
3.	Pattern variables: fixing leS, etc. - missing origin test

4.	Non-virtual fields and methods as in

	  myClass  :        (# X: @integer; foo: (# ... #); ... #);
	  yourClass: myClass(# X: @integer foo: (# ... # ... #);

        In myClass, we need a send.this method

5.	Qua-check (casts) as in:

	   aVechicle[] -> aBus[]

	is currently not implemented

6.	Byte- and bit operations 
           Shift-operations, ...
	   R.%getByte, ...
	   R.%putBits, ...
	   etc.

7.	Real/floating point

8.	Lots of clean-up:-)

--------------------------------------------------------

Wish list for VM:

Generelt

*	send.this #"foo"

	starter søgningen i this - analog til send og send.super

