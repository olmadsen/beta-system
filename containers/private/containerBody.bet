ORIGIN '../container';
LIB_ITEM 'container';
(*
 * COPYRIGHT
 *	 Copyright (C) Aarhus University
 *	 All rights reserved.
 *)


--- containertheScannerDopart : doPart ---
do
   true->s.first;
   s.start;
   INNER theScanner;
   s.end;
   false->s.first; (* enable static rescanning *)
   
--- containerScanDopart:DoPart ---
do
   (if not first then 
       (this(scan)[],&where[])->theScanner
    else
       INNER Scan;
   if);
   
--- containerFindDopart:DoPart ---
do
   start;
   INNER find;
   end;
   
   

--containerEnterDopart:dopart--
do
   clear;
   (if theOther[]<>none then
       Inner;
   if)
