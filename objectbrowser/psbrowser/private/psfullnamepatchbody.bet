ORIGIN '../psfullnamepatch';

INCLUDE '~beta/objectserver/v2.3/execGroupTable';
INCLUDE '~beta/sysutils/v1.4.2/objinterface';
INCLUDE '~beta/basiclib/v1.4/external';

--- lib:attributes ---

newExecGroupTable: ExecGroupTable
  (# newInit: 
       (# getGroups:
            (# new: ^standardEGTE;
               current: ^group_header;
            do 
               loop:
                 (if (current[] -> NextGroup -> current[])<>NONE then
                     &standardEGTE[] -> new[];
                     &Text[]->new.groupName[];
                     current[]->NameOfGroup->new.address->extGetCstring->new.groupName;
                     nextInx -> new.inx;
                     current.protoTableAdr -> tos'%adrGetLong' -> new.len;
                     current.protoTableAdr + 4 -> new.ptListAdr;
                     new[]->nameHash.insert;
                     (new[],new.address) -> adrHash.insert;
                     restart loop;
                 if);
            #);
       do nameHash.init; 
          adrHash.init;
          getGroups;
       #)
  #)

--- psfullnamepatchimpl:descriptor ---
(# egte: ^newExecGroupTable;
do &newExecGroupTable[]->egte[];
   egte.newInit;
   egte[]->objectPool.put;
#)
