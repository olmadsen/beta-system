ORIGIN 'moveable';
INCLUDE '~beta/guienv/v1.2/scrolllists';

--- windowLib: attributes ---

listmoveable: moveable
  (# contentstype::<
       (# list: @listType;
          listType:< TextScrollList
            (# open::<
                 (#
                 do INNER; true->bindBottom->bindRight;
                 #);
            #);
          open:: 
            (#
            do list.open; 
               list.size -> size; 
            #);
       #);
  #);
