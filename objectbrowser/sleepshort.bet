ORIGIN '~beta/basiclib/v1.4/basicsystemenv';
MDBODY mac 'private/sleepshort_macbody'
       nti 'private/sleepshort_ntibody'
       default 'private/sleepshort_x11body';

INCLUDE '~beta/containers/v1.4/list';

--- systemlib:attributes ---

shortsleeper:
  (# sleepers: @List (# element:: (# id: @Integer; sem: @semaphore; #)#);
     
     sleep:
       (# ticks: @Integer;
       enter ticks
       do <<SLOT sleepshortsleep:descriptor>>
       #);
     
     init:
       (# 
       do <<SLOT sleepshortinit:descriptor>>
       #);
     
     ssprivate: @<<SLOT sleepshortprivate:descriptor>>;
  #)
