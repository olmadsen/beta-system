ORIGIN 'Deep_Thought';
(*INCLUDE '~beta/win32lib/winerror';*)
--PROGRAM: descriptor--
(# ICalc_Impl: ICalc
     (# 
        Add::
          (# do (v1 + v2) -> res #);
        Subtract::
          (# do (v1 - v2) -> res #);
        Multiply::
          (# do (v1 * v2) -> res #);
        Divide::
          (# 
          do (if op2 = 0 then 
                 0 -> res;
                 (*E_FAIL -> result;*)
              else
                 (v1 div v2) -> res;
             if);
          #);
        Modulus::
          (# 
          do (if op2 = 0 then 
                 0 -> res;
                 (*E_FAIL -> result;*)
              else
                 (v1 div v2) -> res;
             if);
          #);
     #);
do 
#)

