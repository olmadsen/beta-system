VERSION 2.1.2
=============
With v2.1.2 the following changes were introduced:


frejaexternalinterfacebody.bet:
===============================
toby 040396:	Onfocuschanged checks if a document has been instantiated.

frejamenu.bet:
==============
toby 040396:	NewBETAProgram/NewBETALibrary lets Sif open dialog to prompt
		for name. Document is first initialized during newFragment
		event from Sif.

toby 080396:	Added check 'currentPage/currentObject is none'.

toby 140396:	'Edit Name' checks for case where title is name of singular
		object.
		'Search' checks if theSifEditor <> none.

freja.bet:
==========
toby 060396:	Added Freja FAQ as Ymer help document.

frejabody.bet:
==============
toby 140396:	onInit on connectors used in 'Relations' menu checks if ends
		are none (means other function was selected).
		Inheritance connector checks for singular as "subpattern".
