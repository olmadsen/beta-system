ORIGIN 'controlbody'

--- DblFileCaseErrorTestBody: descriptor ---
(#
do (if not (fullname[]->current.name.equal) then
       'Filename found which only differs in case.'->t[];
       '\n\tPreviously found: '->t.append; current.name[]->t.append;
       '\n\tNow found:        '->t.append; fullname[]->t.append;
       '\n\tFilenames must be unique regardless of case.'->t.append;
       t[]->DblFileCaseError;
   if);
#)

--- ControlMDInit: descriptor ---
(# do (*false->BinaryCodeGen *) #)

--getmodtimebody: descriptor --
(# getEntryModtime: External
     (# path: [1]@char;
        follow: @boolean;
        time: @integer; (* -1 => error *)
     enter (path,follow)
     exit time
     #);
   (*f: @file;*)
do (* (if switch[13] then
    *     t[]->f.name;
    *     (if f.entry.exists  then f.entry.modTime->m else -1->m if);
    * else
    *)
   (t,true) -> getEntryModtime -> m
   (* if) *)
#)
