ORIGIN '../dependency';
INCLUDE '~beta/betaast/v5.2/index'; (* for easy ast init in this example *)
---program: descriptor---

(# AST: @astinterface;
   dg: @AST.DependencyGraph;
do ('',true)->AST.init; (* Note: this is init in index.bet
                         * used here for easy ast init
						 *)
   dg.Init;
   'ppc'->dg.TargetMachine;
   'ppc'->dg.TargetDirectory;
   'Extent: '->screen.putline;
   'dep'->dg.scanextent(# do current.fullname->screen.putline #);
   'Domain: '->screen.putline;
   'dep'->dg.scandomain(# do current.fullname->screen.putline #);
#)

