ORIGIN '../dependency';
INCLUDE '~beta/betaast/v5.0.2/index'; (* for easy ast init in this example *)
---program: descriptor---

(# AST: @astinterface;
do ('',true)->AST.init; (* Note: this is init in index.bet
                         * used here for easy ast init
                         *)
   AST.DependencyGraph
   (# t: ^text
   do Init;
      'Enter fragment to print dependency graph for: '->puttext;
      getline->t[];
      'sun4s'->TargetMachine;
      'sun4s'->TargetDirectory;
(*
      'Extent: '->screen.putline;
      t[]->scanextent(# do current.fullname->screen.putline #);
 *)
      'Domain: '->screen.putline;
      t[]->scandomain(# do current.fullname->screen.putline #);
   #);
#)

