ORIGIN '~beta/mps/astlevel';
LIB_DEF 'betaastindex' '../lib';
BODY 'private/lexemEQbody'
-- astLib: Attributes --
less: (#  exit 0 #);
equal: (#  exit 1 #);
greater: (#  exit 2 #);

CMP:
  (* require: (this(AST)## = nameAppl##) AND (N2## = nameAppl##) *)
  (# N2: ^AST; res: @integer
  enter N2[]
  <<SLOT newIndexCMP:dopart>>
  exit res
  #);

getText:
  (# T: ^ text
  <<SLOT newIndexGetText:doPart>>
  exit T[]
  #)
