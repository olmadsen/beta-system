ORIGIN 'basicshell';

--- include '~beta/uienv/v1.1.8/uienvsystemenv'

(* UIENVSHELL
 * ==========
 * 
 * Use this fragment as origin for distributed BETA programs using
 * uienv. The program should look something like:
 * 
 * ORIGIN 'uienvshell';
 * [[
 * --- program:descriptor ---
 * shellEnv
 * (# setWindowEnv::< (# do myWindowEnv[] -> theWindowEnv[] #);
 *    myWindowEnv: @motifEnv (# ... #);
 *    shellType::< (# ... #)
 *    ...
 * #)
 * ---]]
 * 
 * The 'setWindowEnv' virtual and 'theWindowEnv' reference are declared in
 * BasicSystemEnv.
 * 
 * The uienv instance assigned to theWindowEnv is used for scheduling
 * purposes by UIenvSystemEnv to allow BETA coroutines to cooperate with
 * the event driven user interface.
 * 
 * For distribution specific details see e.g. basicShell. *)
