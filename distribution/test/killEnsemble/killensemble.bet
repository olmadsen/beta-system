ORIGIN '~beta/distribution/v1.1/shell';

--- program:descriptor ---
shellEnv 
(# 
do 
   'Ensemble to kill: ' -> screen.putText;
   ((ensemble##,getLine) -> theShell.myEnsemble.ns.get 
   (# notFound:: (# do 'ensemble not found' -> putLine; theShell.kill #)#)
     -> qua (# as:: ensemble #)
   ).kill;
   'killed' -> putLine;
   theShell.kill;
#)
