ORIGIN '~beta/distribution/v1.01/shell';

--- shellEnvLib:attributes ---
tstRep: shell
  (# 
     put: entry
       (# r0: ^RepetitionObject;
          r1: ^RepetitionObject;
       enter (r0[],r1[])
       do
          r0.reset; r0.getText -> screen.putLine;
          r1.reset; r1.getText -> screen.putLine;
          r0.reset; 'Dette er en aendret r0' -> r0.putText;
          r1.reset; 'Dette er en aendret r1' -> r1.putText;
       #);
  do (this(tstRep)[],'tstrep') -> myEnsemble.ns.put
     (# overWrite::< (# do true -> value #)#);
  #)
