ORIGIN '~beta/guienv/fields'

-- program: descriptor --

GUIenv 
(# 
   aWindow: @Window 
     (#
        aTextEditor: @TextEditor 
          (#
             open:: 
               (# 
               do THIS(window).size->size;
                  true->bindRight->bindBottom;
               #)
          #);
        open:: (# do aTextEditor.open #)
     #);
do aWindow.open;
#)
