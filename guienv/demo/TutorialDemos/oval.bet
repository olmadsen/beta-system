ORIGIN '~beta/guienv/v1.3/guienv';
INCLUDE '~beta/guienv/v1.3/figureitems' 
--- program: descriptor ---
guienv
(# aWindow: @window
     (# go: @oval
          (# open::
               (# 
               do (100, 100)->position;
                  (50, 100)->size;
                  7->pen.size
               #)
          #);
        open:: 
          (# 
          do (100, 100)->position;
             (300, 300)->size;
             'oval'->title;
             go.open
          #)
     #)
do aWindow.open
#)
