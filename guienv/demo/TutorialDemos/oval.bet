ORIGIN '~beta/guienv/v1.4/guienv';
INCLUDE '~beta/guienv/v1.4/figureitems' 
--- program: descriptor ---
guienv
(# aWindow: @window
     (# go: @oval
          (# open::
               (# 
               do (100, 100)->position;
                  (50, 100)->size;
                  7->pen.size
               #)
          #);
        eventhandler::
          (# onAboutToClose:: (# do terminate #) #);
        open:: 
          (# 
          do (100, 100)->position;
             (300, 300)->size;
             'oval'->title;
             go.open
          #)
     #)
do aWindow.open
#)
