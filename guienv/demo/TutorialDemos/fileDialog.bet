ORIGIN '~beta/guienv/v1.3.1/guienv';
INCLUDE '~beta/guienv/v1.3.1/stddialogs';
--- program: descriptor ---
guienv
(# name: ^text;
do fileSelectionDialog(# do 'fileDialog'->dialogTitle[] #)->name[];
   (if name[]=NONE then
       'Selected Cancel' -> putline;
    else
       name[] -> putline;
   if);
   terminate;
#)


