ORIGIN '~beta/guienv/guienv';
INCLUDE '~beta/guienv/utils/colorDialog'
--- program: descriptor ---
guienv
(# w: @colorDialog
     (# apply::
          (# 
          do '('->put;c.red->putint;
             ','->put;c.green->putint;
             ','->put;c.blue->putint;
             ')'->putline;
             (if closing then terminate if)
          #)
     #)
do w.open
#)
