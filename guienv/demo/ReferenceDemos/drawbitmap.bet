ORIGIN '~beta/guienv/v1.3/guienv';
INCLUDE '~beta/guienv/v1.3/graphics';
(* This demo is an example showing how read a bitmap from a file, and
 * using the graphics pattern to draw the bitmap on the screen.
 *)
--- program: descriptor ---
guienv
(# bm: @bitMap;
   theWindow: @window
     (# obj: @windowItem
          (# open::
               (# 
               do (50,50) -> position;
                  (bm.width,bm.height) -> size
               #);
             eventHandler::
               (# onRefresh::
                    (# 
                    do father.graphics
                       (# 
                       do (bm[], (0, 0), position, bm.width,bm.height)
                            -> drawRaster
                       #)
                    #);
                  onMouseDown::
                    (# do drag #)
               #)
          #);
        open::
          (# 
          do (400,400) -> size;
             obj.open
          #)
     #)
do 'picture.x11' -> bm.readFromX11File;
   theWindow.open
#)
