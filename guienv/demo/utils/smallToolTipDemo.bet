ORIGIN '~beta/guienv/guienv';
INCLUDE '~beta/guienv/utils/tooltip'
        '~beta/guienv/controls';
-- PROGRAM: Descriptor --
guienv
  (#
     aWindow: @window
       (#
          aButton: @pushButton
            (#
               open:: 
                 (# 
                 do (150,100)->size; (10,50)->position; 'Ok'->label; 
                 #);
               
            #);
          open::  (#  do (300,300)->size; aButton.open;  #);
          eventHandler:: 
            (# onAboutToClose::  (#  do terminate;  #);  #);
          
       #);
     
  do aWindow.open; 'a tooltip'->aWindow.aButton.addToolTip; 
  #)  

