ORIGIN '~beta/guienv/guienv';
BODY 'private/colorDialogBody'
-- guienvLib: Attributes --
colorDialog: window
  (* this pattern implements a color selection dialog in which you
   * can specify a color interactively in a dialog window.  You can
   * specify explicit color values in either RGB or HSV color values,
   * select colors from a set of predefined color names, or you can
   * select the color directly in a HSV color space.
   *)
  (# apply:<
       (* this is invoked each time either 'Apply' or 'Close' button
        * are pressed.  The 'Close' button also closes the color
        * dialog window.  When invoked, 'c' will contain the selected
        * color value
        *)
       (# c: @color
         <<SLOT colordialogApply: dopart>>
       #);
     custom:<
       (* when invoked, 'colors' contains the color values, which are
        * selected by the user as costum colors (visible in the
        * 'Costom' group in the dialog).  In INNER, you can read these
        * 'colors'.  If you change the values of any of the 'colors'
        * color values in INNER, these will be displayed as the new
        * custom color values in the 'Custom' group afterwards.
        * 
        * Interactively, a color is selected as a custom color value
        * by 'dragging' the color from the 'Color' area down into one
        * of the color spots in the 'Custom' group.
        *)
       (# colors: [12]@color
       <<SLOT colordialogCustom: dopart>>
       #);
     open::< 
       (# <<SLOT colordialogOpen: dopart>> #);
     private: @<<SLOT colordialogPrivate: descriptor>>
  #)

