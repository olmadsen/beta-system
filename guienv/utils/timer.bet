ORIGIN '../guienv';
BODY 'private/timerbody';

-- GUIenvLib: attributes --

timer:
  (# <<SLOT timerLib: attributes>>;
     activate:
       (# interval: @integer
       enter interval
       <<SLOT GUIenvTimerActivate: doPart>>
       #);
     pause:
       (# <<SLOT GUIenvTimerPause: doPart>> #);
     action:<
       object;
     private: @<<SLOT GUIenvTimerPrivate: descriptor>>;
  #);
