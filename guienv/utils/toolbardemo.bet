ORIGIN 'toolbar';
--program:descriptor--
guienv
(# 
   w:@window
     (# 
        tb:@toolbar
          (#             
             t1,t2,t3,t4:@pixmapTool;
             open::
               (# 
               do 'backbtn'->t1.init;
                  t1[]->addTool; 
                  'frwbtn'->t2.init;
                  t2[]->addTool;
                  'upbtn'->t3.init;
                  t3[]->addTool;
                  'downbtn'->t4.init;
                  t4[]->addTool
               #);
          #);
        open::
          (# do (300,300)->size;
             tb.open;
          #);
        close::
          (# do terminate #)
     #);
do w.open;   
#)
