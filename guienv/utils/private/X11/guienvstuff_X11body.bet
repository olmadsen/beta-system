ORIGIN '../guienvstuffbody';
INCLUDE '../../../private/X11/guienv_unixprivate';
INCLUDE '../../../private/X11/motifstuff';

-- GUIENVbuttonAlignment: doPart --
do (# xAlignment: @integer;
   do (if value 
       //center then XmALIGNMENT_CENTER -> xAlignment
       //right then XmALIGNMENT_END-> xAlignment
       //left then XmALIGNMENT_BEGINNING -> xAlignment;
      if);
      (XmNalignment, xAlignment) -> setIntegerResource;
   #);
