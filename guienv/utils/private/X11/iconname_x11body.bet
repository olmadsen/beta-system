ORIGIN '../iconnamebody';

-- iconButtonEnterIconName: doPart --
do l:
     (# pm: ^pixmap;
     do &pixmap[] -> pm[];
        (if value[] <> none then
            value[] -> pm.read
            (# 
               error::
                 (# 
                 do leave l;
                 #);
            #);
            pm[] -> icon;
        if);
     #);
   
-- iconButtonExitIconName: doPart --
do 
