ORIGIN '../guienv';
BODY 'private/trackbody';

-- windowItemLib: attributes --

track:
  (* Faster replacement for mouseTrack *)
  (# grabMouse:< booleanValue (# do true -> value; inner #);
     mousePress:< object;
     mouseMove:< 
       (# h, v: @integer;
       enter (h, v)
       do inner
       #);
     mouseRelease:< object;
     curPt, prevPt, startPt: @point;
  <<SLOT GUIENVwindowItemTrack: doPart>>
  #);
