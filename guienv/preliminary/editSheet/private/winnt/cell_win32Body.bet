ORIGIN '../cellbody';

INCLUDE 
       '~beta/guienv/private/winnt/figureitems_ntibody';

-- GUIENVcellOnRefresh: doPart --
do (if border.visible then
       drawWithPen
       (# 
       do frame -> drawRect; 
       #);
   if);
   inner onRefresh;
