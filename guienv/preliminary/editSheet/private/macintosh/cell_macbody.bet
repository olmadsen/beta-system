ORIGIN '../cellbody';
INCLUDE 
'~beta/guienv/v1.6/private/macintosh/figureitems_macbody';

-- GUIENVcellOnRefresh: doPart --
do (if border.visible then
       drawWithPen
       (# 
       do frame -> drawRect; 
       #);
   if);
   inner onRefresh;
