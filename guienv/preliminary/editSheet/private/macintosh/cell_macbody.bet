ORIGIN '../cellbody';
INCLUDE        '~beta/guienv/private/macintosh/figureitems_macbody';

-- GUIENVcellOnRefresh: doPart --
do (if border.visible then
       drawWithPen
       (# 
       do frame -> drawRect; 
       #);
   if);
   inner onRefresh;
