ORIGIN '~beta/guienv/v1.6/guienv';
BODY 'private/pixmapsBody';

-- guienvlib: attributes --


loadIconResource:
  (# name: ^text;     
     id: @integer;
     pm: ^pixmap;
  enter (name[],id,pm[])
  <<SLOT loadIconResourceImpl: doPart>>  
  #);

loadBitmapResource:
  (# name: ^text;     
     id: @integer;
     pm: ^pixmap;
  enter (name[],id,pm[])
  <<SLOT loadBitmapResourceImpl: doPart>>     
  #);
