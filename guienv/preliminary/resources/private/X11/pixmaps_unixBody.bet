ORIGIN '../pixmapsBody';

-- guienvlib: attributes --

-- loadIconResourceImpl: doPart --
do (if name[]<>NONE then
      l: name[] -> read
       (#
          error::
            (#
            do what[] -> this(loadIconResource).error;
               leave l;
            #);
       #);
   if);
   
   
-- loadBitmapResourceImpl: doPart --
do (if name[]<>NONE then
       l: name[] -> read
       (#
          error::
            (#
            do what[] -> this(loadBitmapResource).error;
               leave l;
            #);
       #);
   if);
   
