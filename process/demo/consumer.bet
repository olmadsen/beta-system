ORIGIN '~beta/basiclib/v1.4/betaenv';
[[
--- program:descriptor ---
(#
do 	 
   cycle 
   (# t: ^Text;
   do
      keyboard.getatom -> t[]; t[] -> screen.puttext; ' ' -> screen.put;
      t.reset;
      (if 'end.' -> t.equal //true then 
          screen.newline; 'Consumer stops' -> screen.putline;
          stop
      if)
   #)
#)
---]]
