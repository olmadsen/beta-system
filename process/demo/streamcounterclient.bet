ORIGIN '~beta/basiclib/systemenv';
INCLUDE '~beta/process/streamsocket';

--- program:descriptor ---
systemEnv
(# 
   port: (# exit 7887 #);
   host: (# exit 'localhost' #);
   
   s: @StreamSocket;

do s.init;
   (10, host, port) -> s.connect;
   s.getLine -> putText; putline;
   s.close;
#)
