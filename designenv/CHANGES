VERSION 2.1.2
=============
With v2.1.2 the following changes were introduced:

designenv.bet:
==============
toby 100596:	INNER is called before anything else happens in onRemove.

designobjectbody.bet:
=====================
toby 080396:	In nodedrag, onDrag is only called on region when region
		is a node (prevents qua error when dragging region with
		label).

toby 290396:	Errors corrected in 'NewPolygon' and PolyPoints'.