super2: @
  (# integer:  (# % simple, 32 % #);
     put: (# % prim % #);
     object:
       (# % simple %
          suspend: (# % prim % #);
       #);
       
     foo: (# ch: @integer do 'a' -> ch; ch -> put; inner; ch -> put #);
     bar: foo(# do ch + 1 -> ch; ch -> put; inner; ch -> put; ch + 1 -> ch #);
     snorf: bar(# do ch + 1 -> ch; ch -> put; ch + 1 -> ch #)
     
  do snorf
  #)
