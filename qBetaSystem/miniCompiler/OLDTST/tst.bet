tst: @
  (# integer:  (# % simple, 32 % #);
     put: (# % prim % #);
     object:
       (# % simple %
          suspend: (# % prim % #);
       #);
    
     R : @
       (# d: @integer;
          fisk: (# q: @integer enter q do q -> put #);
          sild:< (# do 'f' -> put #);
       enter d
       do 'c' -> put; d -> put
       #);
     torsk:
       (# haj:< (# do 'g' ->ch; inner ; ch -> put #);
          ch: @integer
       #);
     makrel: torsk(# haj::< (# do ch -> put; ch + 1 -> ch #) #);
     S: ^torsk;
     c, ch: @integer;
     chk:
       (# a,b,c: @integer
       enter(a,b,c)
       do (if a = b then
              c -> put
           else
              63 -> put
          if)
       #);
    
  do 'a' -> ch;
     ch -> put;
     ch + 1 -> c -> put;
     'd' -> R;
     'e' -> R.fisk;
     r.sild;
     &makrel[] -> S[];
     S.haj;
     (12,12,98) -> chk; '*'->put;
     1 + c -> put;
  #)
