File: 
  (# open:
       (# name: ^text
       enter name[]
       do (fileService,0,1,name[]) -> sendMsg -> handle
       #);
      puttext: 
       (# T: ^text
       enter T[]
       do (fileService,handle,2,T[]) -> sendMsg;
       #);
     close:
       (#
       do (fileService,handle,3, '') -> sendMsg
       #);
     handle: @integer; 
     fileService: (# exit 1 #)
  #)
