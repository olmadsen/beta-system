test: @
{ myClass: -- comment
   { put[x: integer %at y: integer] -> integer: 
      { a: integer
      do a := x + y;
         a := a * 100; 
      };
     foo[x: integer , y: boolean] -> boolean: {};
     [get pos: integer]: {};
     last: {};
     [if B: boolean %then thenP:< object %else elseP:< Object if]:     { };
     if[ B: boolean %then thenP:< object %else elseP:< Object if]:     { };
     x: integer;
     + V: integer: { };
     := V: integer: {};
     S: ^Person;
   do   
       x[17];
       [if x %then { do x[x + 1] } %else { do x[x - 2] } if];
       if[ x %then { do x[x + 1] } %else { do x[x - 2] } if];
       x := foo[12,x];
       x := foo[12,x];
   };
   R:  myClass
do
   R.put[22 %at 30];
   R.[get 21];
   R:= x + 1;
   R.S.age;
   'Hello'.print
}
