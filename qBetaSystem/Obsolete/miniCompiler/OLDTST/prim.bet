prim: @
  (# integer:  (# % simple, 32 % #);
     put: (# % prim % #);
     object:
       (# % simple %
          suspend: (# % prim % #);
       #);
     R: @ (# foo: (# do 98 -> put; R.suspend; 100 -> put; #)
          do foo
          #)
  do
     97 -> put;
     R;
     99 -> put;
     R;

  #)
