insItem: @
  (# integer:  (# % simple, 32 % #);
     put: (# % prim % #);
     object:
       (# % simple %
          suspend: (# % prim % #);
       #);
          
     R: @ 
       (# scan: (# do (for i: 3 repeat inner; zz -> put for) #)
          doIt: 
            (# a,b: @integer enter(a,b) do a -> put; b -> put; b+1  -> x; inner exit b+2 #)
       #);
     x,y,zz: @integer
  do 'a'->put;
     'a' -> zz;
     R.scan(# do '?'->put; zz + 1 -> zz #);
  #)
