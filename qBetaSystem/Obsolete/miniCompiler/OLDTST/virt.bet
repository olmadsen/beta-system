virt: @
  (# integer:  (# % simple, 32 % #);
     put: (# % prim % #);
     object:
       (# % simple %
          suspend: (# % prim % #);
       #);
     
     fisk: (# foo:< (# do'>'-> put; ch -> put; inner; ch -> put #)
           #);
     sild: fisk
       (# foo::< (# do ch+1 -> ch -> put; ch +1 -> ch #);
       #);
     ch: @integer;
     S: ^fisk
  do 'a' -> ch; '*'->put;
     &sild[] -> S[];
     '@' -> put;
     S.foo
  #)

          
