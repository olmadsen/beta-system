smallEx: @
  (# Core: 
       (# S: ^Object;
          attach:
            (#
            enter S[]
            do
            #);
       do loop:
            (if S[] <> none then
                S
            if)
       #);
     core1,core2: @Core;
     
     init:
       (#
       do core1[] -> fork; core2[] -> fork
       #);
     
     P1: @
       (#
       do
       #);
     
     P2: @
       (#
       do
       #)
     
       
  #)
