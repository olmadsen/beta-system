tinyZ: obj
   Record:
      in id: var integer
   Fool:
      cmp:<
         in x: ref Record
         in y: ref Record
         putint(x.id + y.id)
      inner(Fool)	 
   Bar: Fool
      cmp::
         ...
      a: obj Record(10)
      b: obj Record(11)
      if (true):then
         cmp(a,b)
   Bar      
   