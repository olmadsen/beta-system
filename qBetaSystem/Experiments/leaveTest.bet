ORIGIN '~beta/basiclib/betaenv'
---program:descriptor--
(# i: @integer
do L:
     cycle
     (#
     do i + 1 -> i;
        (if i > 5 then
            leave L
        if);
        'a' + i -> put
     #);
   'Q' -> put;
#)
