ORIGIN '~beta/basiclib/betaenv';

INCLUDE '~beta/containers/hashTable';
INCLUDE 'part';
--- lib: attributes ---

PartTable: HashTable
  (* a hashtable containing part objects *)
  (# element::< part;
     hashFunction::<(# do e.id->value #);
     rangeInitial::<(# do 2000->value#);
  #);

