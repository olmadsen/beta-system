ORIGIN 'psLocationBody';

--- psloreadswap:descriptor ---
(# 
do (for i: last repeat
        inxs[i].%byteSwapLong;
        lens[i].%byteSwapLong;
        protos[i].%byteSwapShort;
        groups[i].%byteSwapShort;
   for)
#)
--- pslowriteswap1:descriptor ---
(# 
do (for i: last repeat
        inxs[i].%byteSwapLong;
        lens[i].%byteSwapLong;
        protos[i].%byteSwapShort;
        groups[i].%byteSwapShort;
   for)
#)
--- pslowriteswap2:descriptor ---
(# 
do (for i: last repeat
        inxs[i].%byteSwapLong;
        lens[i].%byteSwapLong;
        protos[i].%byteSwapShort;
        groups[i].%byteSwapShort;
   for)
#)

--- psfreeListreadswap:descriptor ---
(# 
do (for i: size repeat
        inxs[i].%byteSwapLong;
        lens[i].%byteSwapLong;
   for)
#)
--- psfreeListwriteswap1:descriptor ---
(# 
do (for i: size repeat
        inxs[i].%byteSwapLong;
        lens[i].%byteSwapLong;
   for)
#)
--- psfreeListwriteswap2:descriptor ---
(# 
do (for i: size repeat
        inxs[i].%byteSwapLong;
        lens[i].%byteSwapLong;
   for)
#)

