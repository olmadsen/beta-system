ORIGIN 'objectcode'
---jmp:doPart--
do ' jmp L' -> out.puttext; lab -> out.putint; out.newline;
   codes.jmp -> emit;
   0 -> emit2;
   lab -> labs.mark;
---jmpFalse:descriptor--
(#
do codes.jmpFalse -> emit;
   0 -> emit2;
   Flab -> labs.mark
#)

   
