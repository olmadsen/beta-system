ORIGIN '~beta/guienv/guienvsystemenv';
INCLUDE '~beta/basiclib/iget';
---lib:attributes---
_kbhit: external(# b: @boolean do callC exit b #);
---program:descriptor---
(# i,j: @integer; ch: @char
do (for i: 10 repeat
        L:
          (if not _kbhit then 
              j + 1 -> j;
              (if (j mod 1000) = 0 then ','->put if);
              restart L if);
        iget -> ch;
        ch -> put
   for);
#)
