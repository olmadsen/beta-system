Persons: @
  (# 
     Person: 
       (# name: ^text;
          open:
            (# N: ^text
            enter N[]
            do N[] -> name[]
            #);
          theName: (# N: ^text do name[] -> N[] exit N[] #);
          notify:
            (# msg: ^text;
            enter msg[]
            do 
               (*name.print;
               ' got message: ' -> M;
               M.print;
               msg.print; msg.newline*)
            #)
       #);
  #)

