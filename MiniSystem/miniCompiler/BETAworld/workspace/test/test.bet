test: @
  (# Foo:
       (# a: @char
       do ch -> a;
          '!'-> put; a -> putint;
          a -> put;
          inner;
          a -> put
       #);     
     Fisk:
       (# put:
            (# x,y,z: @integer
            enter (x,y)
            do x + y -> z
            exit z
            #);
          Bar: Foo
            (# 
            do 'b' -> a;               
            #);
          snorf:< Foo
       #);
     hest: Fisk
       (# snorf:: (# do 'd' -> a #)
       #);
     ch: @char;
     R: @Fisk;
     S: ^Fisk;
     V: @integer
  do (11,22) -> R.put -> V;
     V -> putint; newline;
     'a' -> ch;
     R.bar;
     &hest[] -> S[];
     'c' -> ch;
     S.snorf
  #)
