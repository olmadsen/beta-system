myC: @
   a: @integer
   foo: 
      x: @integer
      y: @integer
      foo
      x:= x + y
      R.foo
      R.s.foo
      max(a,b)
      %if a + b %then a := b %else b:=b
      %while a - b %do a := a + b
      b := a
   bar:
      z: @integer
  


