ORIGIN '~beta/basiclib/betaenv';
INCLUDE 'qparseStdBeta';
---LIB:attributes---
qStdBetaChecker: parseStdBetaModule
  (# BetaDataItem::< 
       (# setUpOrigin::
            (#
            do (*'BetaDataItem:setUpOrigin'->dumpT; label -> dumpTN*)
            #)
          
       #);
     ObjectDesc::<
       (# setUpOrigin::
            (#
            do (*'ObjectDesc:setUpOrigin'->dumpT; label -> dumpTN; *)
               origin[] -> sup.setupOrigin; 
               this(ObjectDesc)[] -> att.setUpOrigin;
               this(ObjectDesc)[] -> acp.setUpOrigin;
               true -> done;
            #)
       #);
     BetaName::<
       (# setUpOrigin::
            (#
            do (if origin[] = none then
                   'Name:origin is none:'->dumpT; T[] -> dumpTN
               if)
            #)
       #)
     
  do '***** StdBetaChecker'->putline;
     
  #)
