Coroutines: obj
   test:
      TF: obj
         X: var integer
         TF.suspend;
         X := 97
         put(X);
         TF.suspend
         X := X + 1
         put(',')
         put(X);
         TF.suspend
      TF.resume
      TF.resume
      TF.resume
      TF.resume
      newline