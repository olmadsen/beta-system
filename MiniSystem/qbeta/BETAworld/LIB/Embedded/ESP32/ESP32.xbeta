ESP32: =
   pinMode(pn: ?integer,mode: ?Boolean):
      [external[1]]
   digitalWrite(pn: ?integer,state: ?Boolean): -- external function
      [external[2]]
   delay(t: ?integer):
      t := t - 1
      %if 0 < t %then%
          %restart delay

