ESP32Ex: =
    loop: 
       LIB.Embedded.ESP32.digitalWrite(0,0); -- arg 1 is not used
       sleep(100)
       LIB.Embedded.ESP32.digitalWrite(0,1); -- arg 1 is not used
       sleep(100)
       %restart loop
    "qBeta:ESP32:blink; ".print   
    putint(thisCoreId);
    newline
    LIB.Embedded.ESP32.pinMode(0,true) -- parameters are currently not used
    loop
