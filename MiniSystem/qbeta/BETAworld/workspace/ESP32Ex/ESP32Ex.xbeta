ESP32Ex: obj 
   loop: 
      LIB.Embedded.ESP32.digitalWrite(0,0)
      sleep(100) -- arg 1 is not used
      LIB.Embedded.ESP32.digitalWrite(0,1)
      sleep(100) -- arg 1 is not used
      restart(loop)
   "qBeta:ESP32:blink; ".print
   putint(thisCoreId)
   newline
   LIB.Embedded.ESP32.pinMode(0,true) -- parameters are currently not used
   loop