ImmutableUnique: obj
   Record:
      [unique]
      in id: ref String.String
      print:
         id.print      
   fool:
      @inner fool
   Message: --fool
      [immutable]
      in rec: ref String.string
      print:
         rec.print      
   R1: ref Record
   R2: ref Record
   S: ref Object
   M1: ref Message
   R1 := Record("First:")
   R1.print
   R2:= R1
   R2.print
   @if R1 = none :then
      " R1 is none\n".print
   :else
      R1.print
   -- S := R2 -- semantic error
   
   M1 := Message("Olsen:")
   M1.print
   