Coroutines: obj 
   do: 
      fool: obj Object
         one: 
            put('e')
            fool.suspend
            put('g')
         put('a')
         fool.suspend
         put('b')
         fool.suspend
         put('d')
         one
      coco: obj 
         go: 
            Loop: obj 
               i: var integer
               @if i <= 7 :then put('i' + i)
               i := i + 1
               %restart loop
         coco.suspend
         go
      choko: 
         in a: var integer
         in b: var integer
         doResume: 
            in q: var integer
            in w: var integer
            out V: var integer
            e: var integer
            (%this choko).resume
            V := 150
         x: var integer
         y: var integer
         INIT: obj 
            x := a + b
            y := a + b + 1
         (%this choko).suspend
         put(x)
         put(y)
         x := x + 2
         y := y + 2
         %restart choko
      runChoko: 
         VW: var integer
         dark: obj choko('q',1)
         VW := dark.doResume(12,3)
         dark.doResume(14,56)
      fool.resume
      put('c')
      fool.resume
      put('f')
      fool.resume
      put('h')
      coco.attach(200)
      put('q')
      runChoko
      fill('v')