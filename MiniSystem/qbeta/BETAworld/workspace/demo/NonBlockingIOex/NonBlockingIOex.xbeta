NonBlockingIOex: = LIB.NonBlockingIO.NonBlockingIOSystem
   ch := 'a'
   %for 1 %to% 27 %do%
        reader.resume
        %if (inx = 10) || (inx = 20) || (inx = 27) %then%
            %for 1 %to% 5 %do%
                 put(reader.get)
   newline
