Client: =
   S: = LIB.Socket.Socket  -- Ref is none without LIB - adr(Socket) is wrong
   T: ? String.String
   V: ?integer
   "Socket\n".print
   S.init(3000)
   S.connect("localhost")
   S.send("Hello world - I am here")
   T := S.receive -- need to conver C string to abeta String
   S.close;

