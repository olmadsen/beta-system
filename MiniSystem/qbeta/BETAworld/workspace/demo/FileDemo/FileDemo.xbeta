FileDemo: =
   %include FileSys
   read:
      %if F.eof.not %then%
         put(F.get) 
         %restart read         
   F: ? FileSys.File
   F:= open("Olsen") -- FileSys.open gives an index error on vcompiler
   read
   F.close
