testZ: obj
   doIT:
      Set:
         foolxx:
            out B: var boolean
            "foolxx:".print
            putint(X)
            put(',')
            @inner foolxx	    
         exists:
            in Fxx:<
               in RRR: ref Object
               out V: var Boolean
            out B: var Boolean  
            putint(X)
            B := Fxx(none)
            newline
         Y: var Boolean
      X: var integer
      Y: var integer
      Z: var integer
      SSS: obj Set    --off.5
      X:=117
      SSS.Y := true
      @if SSS.Y :then
         put('A')
         putint(X + 1)
         put(':')
         SSS.foolxx{putint(X + 2)}	 
      :else
         put('B')
      SSS.foolxx{qwe: var integer}
      put('%')
      @if SSS.foolxx{xyz: var integer} :then
          put('Q')
      :else
          put('W')
          putint(X + 17)	  
      newline
      SSS.exists({in a: ref object; out b: var Boolean; put('*'); putint(X + 3) })
      put('!')
      @if SSS.exists({in q: ref object; out w: var Boolean; put('+'); putint(X + 4)}) :then
          put('Q')
      :else
          put('W')
      newline

