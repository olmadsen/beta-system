NewStorage: =
   bar(v: ?integer):
      m:
         put(v)
   a: ? integer
   s: ? string.string
   b: ?integer
   r: = bar('c')
   foo1:
      b:= 8
      put(a + b)
      r.m
      s := "de"
      put(':')
      putint(s.length)
      s.print
   a := 90
   b:= 7
   put(a+b)
   foo1
