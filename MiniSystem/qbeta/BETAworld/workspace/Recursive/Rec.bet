ORIGIN '~beta/basiclib/betaenv'
---program:descriptor---
(# MyPtn:
     (# ch: @char
     enter ch
     do ch -> put;
        (if ch < 'g' then
            ch + 1 -> MyPtn;
            inner
        if)
     #);
   MySub: MyPtn
     (#
     do '!' -> put
     #)
do 'a' -> MySub;
   newline
#)
   
