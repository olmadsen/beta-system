tinyK: obj
   TTT:
      ...
   Sortable_range:
      TTT:< Object
      Scan:< { current: ref TTT; inner(scan) }

   sort(r: ref Sortable_range):
      ...

   MyType:
      Elm:< Object
      asSortable: obj Sortable_range
          TTT:< Elm
          scan::
              this(MyType).scan
                  this(asSortable.scan).current := current
                  inner(asSortable.scan)
      scan:
         current: ref Elm
         inner(scan)
   Q: obj MyType
   sort(Q.asSortable)
