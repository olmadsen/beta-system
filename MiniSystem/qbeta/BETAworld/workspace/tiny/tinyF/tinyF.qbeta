tinyF: obj
   condition-> BBB: var Boolean:
      innerBool-> B: var Boolean: inner
         ...	 
      BBB := innerBool
   while (cond:< condition):repeat{doPart:< Object}:
      loop: do
         if (cond):then
            doPart
            restart(loop)
   a: var char	     
   "Start\n".print
   a := 96
   while(a < 106):repeat
       a := a + 1
       put(a)       
         