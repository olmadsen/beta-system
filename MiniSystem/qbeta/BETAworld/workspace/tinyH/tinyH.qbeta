tinyH: obj
   %visible Stringlib
   ch: var Char
   ch := 'a'
   if ((ascii.isLetter(ch) || ascii.isDigit(ch) 
              || (ch = '_') || (ch = '%') || (ch = '@'))) :then
      put('Q')
   :else
      put('?')
