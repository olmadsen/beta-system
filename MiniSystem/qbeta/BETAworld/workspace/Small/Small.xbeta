Small: =
   foo_small:
      V: ? integer
      R: ? string.string
      S: = string.string
      U: ? integer
      T: ? foo_small
   tiny: =
      b:  ?integer
      kuk: ?string.string
   U: ? foo_small
   A: ?integer
   
