test: @
{ myClass: -- comment
   { put( x: integer ,at y: integer): {};
     (get pos: integer): {};
     last: {};
     (if B: boolean ,then thenP:< object ,else elseP:< Object if):
     { };
     x: integer;
     + V: integer: { };
   do   
       x(17);
       (if x ,then { do x(x + 1) } ,else { do x(x - 2) } if)
   };
   R:  myClass
do
   R.put(22 ,at 30);
}
