ORIGIN '~beta/basiclib/betaenv';

INCLUDE '~beta/persistentstore/persistentstore';
INCLUDE 'register';

-- program: descriptor --
(# ps: @PersistentStore;
	filler: @integer;
	reg: @Registry (# version:: (# do 13 -> value #) #);
do reg.init;
	ps.init;
	'oranges' -> ps.create;
	
	'Ole Lehrman Madsen' -> reg.register;
	'Michael Lassen' -> reg.register;
	'Tabita Enig' -> reg.register;
	
	reg.display;
	
	(reg.data[], 'root') -> ps.put;
	ps.checkpoint;
	ps.close;
#)
