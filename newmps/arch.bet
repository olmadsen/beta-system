ORIGIN '~beta/basiclib/betaenv';
MDBODY nti 'private/arch_ntibody'
       ppcmac 'private/arc_macbody';

-- lib: attributes --

current_machine:
  (# str: ^text;
  <<SLOT current_machine: doPart>>
  exit str[]
  #);
