ORIGIN '../betadbc';
-- program: Descriptor --
(# sqlCon: @connection; stmt: @sqlCon.directSqlStatement
do
   (2->arguments,3->arguments,4->arguments)->sqlCon.open;
   'SELECT COUNT(*) FROM MovieExec'->stmt->putline;
   (stmt.execute).scan
     (#  do current.i->putint; newline #);
   'SELECT COUNT(name) FROM MovieExec'->stmt->putline;
   (stmt.execute).scan
     (#  do current.i->putint; newline #);
   stmt.close;
   sqlCon.close
#)  

