ORIGIN '~beta/guienv/v1.2/guienv';
INCLUDE 'palette';

-- program: descriptor --
GUIenv
(#
   theWindow: @window
     (# thePalette: @controlPalette
          (# 
          #);
        
        open::
          (# 
          do contents -> thePalette.open;
          #);
        close::
          (# 
          do terminate;
          #);
     #);
do theWindow.open;
#)
