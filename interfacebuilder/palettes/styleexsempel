 (*(# open::<
               (# theStyle: ^textStyle;
               do &textStyle[] -> theStyle[];
                  'courier' -> theStyle.name;
                  12 -> theStyle.size;
                  textFaces.bold -> theStyle.face;
                  theStyle[] -> style;
                  (200,70) -> position;
                  (20,20) -> size;
                  ' '->contents;
               #);
          #);*)