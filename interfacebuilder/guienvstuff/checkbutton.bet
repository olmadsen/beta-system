ORIGIN '~beta/guienv/v1.6/controls';
BODY 'private/checkbuttonbody';

-- windowLib: attributes --
CheckButton: CheckBox
  (# EventHandler::<
       (# onMouseUp::< 
            (# 
            <<SLOT checkButtonOnMouseUp: doPart>>
            #);
       #);
     Open::< 
       (#
       do (80,15) -> Size;
          Inner Open;
       #);
  #);
