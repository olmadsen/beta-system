ORIGIN '../loadbitmap';
INCLUDE '~beta/guienv/stddialogs';
LIB_ITEM 'frigg_loadbit';


-- lib: attributes --

FriggWarningMessage:
  (#
  #);

GetFriggWarning: ObjectPool.Get
  (# Type:: FriggWarningMessage;
     DisplayMessage:< (# do INNER #);
     Init::
       (#
       do DisplayMessage;
       #);
  #);
-- GUIENVloadbitmap: doPart --
do (# failed: @boolean;
      message: ^Text;
   do &pixmap[] -> thePixmap[];
      name[] -> thePixmap.read;
   #);
   
