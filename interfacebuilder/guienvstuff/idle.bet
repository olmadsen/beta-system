ORIGIN '~beta/guienv/v1.2/guienv';
BODY 'private/idlebody';

-- GUIenvLib: attributes --

idle:
  (# <<SLOT idleLib: attributes>>;
     activate:
       (# interval: @integer
       enter interval
       <<SLOT GUIenvIdleActivate: doPart>>
       #);
     pause:
       (# <<SLOT GUIenvIdlePause: doPart>> #);
     action:<
       (# do inner #);
     private: @<<SLOT GUIenvIdlePrivate: descriptor>>;
  #);
