ORIGIN '~beta/guienv/v1.6/controls';

-- windowLib: attributes --

caption: staticText
  (# eventHandler::<
       (# onLabelChanged::<
            (#
            do fitToContents;
               INNER;
            #);
       #);
  #);
