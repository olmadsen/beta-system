ORIGIN '~beta/basiclib/v1.6/betaenv';
BODY 'private/classinfobody';

INCLUDE '~beta/containers/v1.6/list';

-- lib: attributes --

classInfo:
  (# <<SLOT classInfoLib: attributes>>;
     class:
       (# name: ^text;
          methods: @list
            (# element::
                 (# name: ^text;
                    event: @boolean;
                 #);
            #);
          type: @integer;
          supported: @boolean;
       #);
     lookup:
       (# name: ^text;
          theClass: ^class;
       enter name[]
       <<SLOT ClassInfoLookup: doPart>>
       exit theClass[]
       #);
     init:
       (# <<SLOT ClassInfoInit: doPart>> #);
     private: @<<SLOT ClassInfoPrivate: descriptor>>;
  #);
getClassInfo: objectPool.get
  (# type:: classInfo;
     exact:: 
       (# do true -> value #);
     init:: 
       (# do obj.init; #);
  #);
