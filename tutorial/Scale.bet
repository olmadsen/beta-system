ORIGIN '~beta/Xt/v1.8/motifenv';
INCLUDE '~beta/Xt/v1.8/motif/scale';
-- program: descriptor --

(* Example showing how to use a Scale. *)

MotifEnv
(# volume: @Scale
     (# init::<
          (# 
          do 0 -> minimum;
             100 -> maximum;
             XmVERTICAL -> orientation;
             'Volume' -> titleString;
             true -> showValue;
          #);
        valueChangedCallback::<
          (# 
          do 'New volume: ' -> screen.puttext;
             data.value -> screen.putint;
             screen.newline;
          #);
     #);
do volume.init;
#)
