ORIGIN '~beta/basiclib/directory';
INCLUDE '~beta/persistentstore/persistentstore';
INCLUDE 'DirList';
---program: descriptor---
(# (* Reading the file names saved in the persistent store *)
   dirList: ^directoryList;
   theStore: @persistentStore;
do 'fileStore'->theStore.openWrite;
     ('myList',directoryList##)->theStore.get->dirList[];
   dirList.scan
   (# (* current referns to the current text element *)
   do current[]->putline; (* print the text *)
   #);
   theStore.close
#)
