ORIGIN '../AgentSystem';
---program:descriptor---
World
(# Observer: @ Agent
     (# go: 
          (#
          do 5 -> create_turtles;
             (for i: 5 repeat
                  turtles.ask
                  (#
                  do (*current.print;*)
                     1 -> current.fd;
                     current.who + i -> random -> current.rd;
                     current.who + 17  * i -> random -> current.ld;
                     theWorldW.show;
                     10 -> guienvWait;
                  #);
                  newline
             for)
          #)
     #)
do observer.go
#)
