ORIGIN '../AgentSystem';
INCLUDE '~beta/basiclib/randomInteger';
---program:descriptor---
World
(# Observer: @ Agent
     (# go: 
          (#
          do 3 -> create_turtles;
             (for i: 5 repeat
                  turtles.ask
                  (#
                  do (*current.print;*)
                     1 -> current.fd;
                     (1,10) -> genunf -> current.rd;
                     (1,10) -> genunf -> current.ld;
                     newline;
                     theWorldW.show;
                     60 -> guienvWait;
                  #);
                  '--------'->putline
             for)
          #)
     #)
do (for i: 20 repeat
        (1,10) -> genunf -> putint; ' ' -> put
   for);
   newline;
   observer.go
#)
