
. ${BETALIB}/export/misc/echonl.sh

###
###if [ $PACKDISTRIBUTION = "yes" ]; then
### No need for this - PACKDISTRIBUTION is only used for standalone 
### packing of distribution

echo +distribution >/dev/tty

echonl "\
./demo/distribution/*.bet \
./demo/distribution/calculator/*.bet \
./demo/distribution/timeServer/*.bet \
./demo/distribution/xtalk/*.bet \
./distribution/v1.6/*.bet \
./distribution/v1.6/*.${AST} \
./distribution/v1.6/${CODEDIR}/ensembleDeamon${EXE} \
./distribution/v1.6/${CODEDIR}/*.${OBJ} \
./distribution/v1.6/private/*.bet \
./distribution/v1.6/private/*.${AST} \
./distribution/v1.6/private/${CODEDIR}/*.${OBJ} \
./bin/startensemble${BAT} \
./distribution/v1.6/private/external/*.c \
./distribution/v1.6/private/external/*.h \
./distribution/v1.6/private/external/*.make \
"
if [ "$TARGET" != "ppcmac" -a "$TARGET" != "nti" ] ; then
echonl "\
./distribution/v1.6/private/external/remoteStart \
./distribution/v1.6/private/${CODEDIR}/startAsDeamon \
"
fi

if [ "$PACKDOTDB" = "yes" ]
then
echo +.db >/dev/tty
echonl "\
./distribution/v1.6/${CODEDIR}/*.db \
./distribution/v1.6/private/${CODEDIR}/*.db \
"
fi

if [ "$TARGET" = "sgi" ]
then
echonl "\
./distribution/v1.6/${CODEDIR}/*.*so \
"
fi

if [ "$PACKOBJECTSERVER" = "yes" ]
then
	${BETALIB}/export/files/objectserver.files
fi

###fi
