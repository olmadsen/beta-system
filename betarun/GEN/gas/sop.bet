ORIGIN '../GENERATOR/asmfile'
[[
---- SingleOperatorLib:attributes ----
putInstr:
  (# instrName: @text
  enter instrName
  do instrName[] -> putText;
     (if op.size
      //1 then 'b' -> putText 
      //2 then 'w' -> putText 
      //4 then 'l' -> putText 
      else '<Unexpected size:' -> putText; op.size -> putInt; '>' -> putText;
     if);
     '\t' -> putText;
  #)

---- SingleOperatorBeforeInner:descriptor ----
(# do ascii.ht -> put #)

---- SingleOperatorAfterInner:descriptor ----
(# do newLine #)

----]]
