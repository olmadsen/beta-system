ORIGIN '~beta/basiclib/betaenv';
INCLUDE 'buttonobserver'
        'observergui'
-- program: Descriptor --
guienv (#
   theWindow: @MyWindow;
   MyTestObserver: @Observer
     (# update:: (#  do 'Observer 1: Update called'->putLine; #);  #);
   MyTestObserver2: @Observer
     (# update:: (# do 'Observer 2: Update called'->putLine; #); #);
   
do MyTestObserver[]->theWindow.pushbutton1.attach; 
   MyTestObserver2[]->theWindow.pushButton1.attach;
   theWindow.open;
#)  
