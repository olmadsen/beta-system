ORIGIN '~beta/guienv/guienv';
INCLUDE 'buttonsubject'
        '~beta/guienv/controls';

-- guienvLib: Attributes --
MyWindow: window
  (#
     open::< (#  do pushbutton1.open; INNER ;  #);
     pushbutton1: @Subject
       (# open::< (#  do (60,20)->size; (110,80)->position; 
	   'observer'->label;
           INNER #)
       #);
     eventHandler::< (# 
       onAboutToClose::< (# do terminate #);
    #);
  #)  

