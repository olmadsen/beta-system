editorenv  interface

 Cursor Support: 

     setWaitCursor()
     setStructureCursor()
     setRightButtonCursor()
     setTextCursor()
     
     Bruges af sifbrowserWindowExt mm i ymersiflib.bet

 Interne:

     setGlobalWriteProtection()  
     nextEditorId()
     fragmentDefaulName()
     showEditors()
     findFormEditorId()
     createGroupEditor()
     createFormEditor()
     nonterminalsExist()


 History support

     history: @
       (# h: ^historyList;
          currentCell: ^h.theCellType;

          init()
          current()
          add()
          remove()
          backPossible()
          back()
          forwardPossible()
          forward()
       #);

     Bruges af sourcebrowser i ymerBody.bet

Fil  support

     new()
     newBetaProgram()
     newBetaLibrary()
     newBetaBody()
     openForm()
     saveAll()
     makeNewTextFile()

     Eksporteres af externalinterface

     
Søgning

     searchTextDialog()
     replaceTextDialog()
     searchText()
     replaceText()
     inLexem()
     hasSemanticLink()
     isApplicationOf()
     scanGroupForText()

     Bruges af sifBrowserWindowExt

Editor håndtering     
     
     findOrCreateGroupEditor()
     findOrCreateFormEditor()

bruges af sourcebrowser


     findEditorForForm()

Eksporteres i external interface

     wriggleWindows: @boolean;
 

Bruges af sourcebrowser
