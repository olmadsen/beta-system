Konstaterede fejl/uhensigtsmaessigheder i r4.2.1.dsa (eller r5.0)
==============================================================================
M.r5.0
------------------------------------------------------------------------------
B	. 'extendedbackup', i kaldet af g.savebackup
	  glemmer at viderebinde exceptions. error report 122. (jlk)
B	. denne fejl opstaar naar man undo'er 'beyond' en save
	  slet; check (semantisk fejl) OK
	  undo; check:		       OK(pga.fejlen,touched gaar fra 0 til 1)
	  slet noget; check:	       not OK (touched gaar fra -1 til 0)
	  undo - check:		       not OK
	  insert after; slet - check   OK
B	. textedit i separate window; ctrl w => editering er laast paa
	  den fragmentform og mjolner maa quittes (gammelby 990414)
B DONE	. rettet i r5.0
	  To gange hurtig Ctrl-g efter hinanden giver en Quit foo dialog
	  som faar mjolner til at crashe ved yes og no.
	  	**** Exception processing
	  	interfaceobject-exception for *
	 	Attempt to use '*' before calling 'open'
	  rettet ved at close kald til hide call i promptfortext-bindingerne.
	  Det betoed ogsaa at recompileAndExecute crashede hvis mjolner
	  oversat af en (anden) mjolner, efter genoversaettelsen af mjolner,
	  men den ny mjolner blev startet. 
B DONE	. rettet i r5.0
	  Edit Includes etc. enables kun hvis man selekterer en fragmentform
	  rettet allowOnForm til allowCommand i onStatus
B DONE	. rettet i r5.0 
	  ref is none fejl i iCompileAndExecute.onStatus, 
	  tester nu paa om cge.fg og cge.fg.fragmentlist er none
	  men kunne ikke reproducere fejlen
B DONE	. rettet i r5.0
	  Hvis man har     origin '...'; include '...'
	  og markerer det sidste '...' og vælger Cut (Ctrl-x), så går mjolner
	  ned. Det er kun tilfældet, når man sletter det sidste fra include.
          Det skyldes at findSuperSyncatNo returnerede none (getSon1) 
	  i listElement parameteren, den skulle istedet returnere listeKnuden.
B DONE	. rettet i r5.0
	  Med SLOT->CreateImplementationFile bliver origin/body-filnavne sat op
	  med backslash (\). Når man tekstediterer, bliver dette backslash
	  således fjernet.
	  Rettet ved at konvertere backslash til slash i disse to operationer,
	  men det er en generel problemstilling: hvis brugere taster backslash
	  saa forsvinder det i propertylisten. Man skal kunne haandtere dette?
IB	. Copy/paste imellem i mellem sif og andre editorer, samt i 
	  teksteditmode
	  På PC virker det sådan set fint nok, men man vil måske gerne kunne
	  kopiere noget struktur og paste det som tekst i teksteditringmode?
	  Paa Unix: Blot man selekterer noget i sif kan det pastes i emacs. 
	  Det er ssv. grunden til at strukturel copy også kan  pastes i emacs.
	  Og måske er det grunden til at tekstuel copy/paste ikke
	  virker fra en tekstediteringssession til en anden. Copy/paste
	  i samme session er OK.
	  Copy i een session og paste i en anden: 
	  Det ser ud som om hele vinduets indhold (på copy-tidspunktet!) 
	  bliver pastet. Men det ser også ud som om tidligere kopier, 
	  akkumuleres i klippebordet?
I	. da browseren ikke saetter focus, man man stadig komme til at
	  teksteditere et forkert sted
I	. Editering under linkning? Kan man ikke mere.
i	. Fra debug til edit, enablet teksteditløsningen igen

Disse fejl er kun i r5.0:
B	. r5.0: Teksteditering til fra 'hello world' til 'hello' 'world' 
	  gaar helt i skoven. Det samme gælder hvis man åbner en fil
	  med gammel syntaks dvs. ''.
B DONE	. r5.0: recompile crashede pga. at translate blev kaldt med foo.exe
	  efter 1. kompilering.

S.r5.0
-------------------------------------------------------------------------------
	
I DONE	. dobbelt-klik paa .bet og aaben i mjolner 
	  (ikke ny hvis allerede startet), kan laves med en socket loesning.
i	. expander ... hvs man  vaelger texteditering paa dem alene (gram)
i	. Antag flg.: cursor: ...; pixmap: ... osv.
	  Hvis man soeger efter pixmap bliver pixmap markeret.
	  Kunne man ikke lave det saadan at hvis man trykker ctrl-d paa pixmap
	  (altsaa nameDcl) bliver hoejre siden ogsaa expanded.
	  Det mangler jeg tit. (gram)



Nedenstaaende blev ikke naaet foer dsa (correctionTODO)
===============================================================================

M.r5.0
-------------------------------------------------------------------------------
B	. editor/test/texteditgarb:
	  ved texteditering af do-parten i: 
	  (# fg2: ^astInterface.fragmentGroup; found: @boolean;  do ... #)
	  ekspanderes til: 
	  [[ (# fg2: ^astInterface.fragmentGroup; found: @boolean; 
          do <<@188:Imperatives>>
	  #) ]](# fg2: ^astInterface.fragmentGroup; found: @boolean; 
          do <<@188:Imperatives>#)
	  det har formentlig noget at goere med den contraction der bliver
	  ekspanderet.
B	. ved teksteditering i af (# #) i en subeditor gaar der kludder i de
	  firkantede paranteser.
b	. saveAs skal teste om filen eksisterer
	  genbrug fra new...
	  Den her er et andet eksempel på mgl. binding af mps-exception.  
          Denne gang i SaveAs. error report 120 (jlk)
b	. hvis dir er skrivebeskyttet men filen ikke doer saveas
	  se koden i backupTextFile
I	. copy/paste mainPart/descriptor slot
I	. search i separate editor og browser! gram
I	. set current as impl. file på group niveau, kan jeg ikke reproducere?
I	. Hvis man vælger hele den knude som bliver editeret i en
	  subeditor og paster noget ind istedet for, bliver subeditoren
	  lukket af Sif.
I	. rename fragment form name dialogen er alt for lille!
	. binde otherError i comp.translate kaldet, check!
I	. guienv: promptfortext, hvorfor scroller den teksten saa 
	  den ryger til venstre for starten at texteditfeltet?

S.r5.0
-------------------------------------------------------------------------------
I	. browsning i Search hit browser giver noget "flimmer" og er specielt
	  generende langsom hvis man koerer over ISDN.
i	. enable Close i File menuen
i	. search replace: replace all
i	. search i cs *DONE i browser men ikke Next

C.r5.0
-------------------------------------------------------------------------------
I	. watch cursor og maaske tager meget lang tid, proev at slaa det fra?
i	. selektionen bør skifte v. højreklik (som v. venstreklik)

W.r5.0
-------------------------------------------------------------------------------
I	. Tab ved textediting skal gaa hen under foerste tegn paa linien over
i	. external textediting: ved parse errors og no changes
	  cleares infoview efter at de er blevet skrevet

Andre ting: ppspec paa beta

