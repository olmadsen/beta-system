VERSION 4.0
=============
With v4.0 the following changes were introduced:
(MAJOR: Designenv is now bifrost-based instead of DesignOA-based!)

toby 240899:	. bifrostSavePage writes data in two steps: first to text object
		  then in one go to file (trying to avoid inkonsistent diagram-
		  files).
toby 110899:	. Tried to fix obsolete autosave dialog when showing diagram
		  after edit in sif (not tested yet!).
toby 300799:	. Abstract kan gå ned, hvis der en aggregeringspil fra en
		  attribut til sig selv.

toby 300799:	. "Show Diagram" i Diagram menu virker ikke (popup-menuens gør).
		. Hvis der er blevet indsat en ff uden at diagrammet var åbnet,
		  så opdaterer freja ikke tilsvarende næste gang diagram åbnes.
		. AggregationList elementer med prefix, som er et subpattern af
		  en container, bliver ikke loaded.
		. GetValuesFromCode søger ikke op gennem prefix-kæden efter
		  container-prefix (som SetupRelations gør).
		. Aggregation diamant bliver ikke altid sort, som den skal, når
		  det er by-value.
		. Der kommer ikke nogen aggregeringspil til statiske instanser
		  af singulære

toby 280799:	. changed div. switch tests into trace statements
		. removed the IndexIDList (it sometimes caused problems when
		  deleting or abstracting and it was actually no longer of
		  any use!)
		.. "..." på singulære instanser
		. Problem med opdatering ved editering af root via sif
		  (Skyldtes manglende opdat. af astIndex på fragmentnode i
		   parseDiagram)
		. WorkSheet og Group Page vises ikke i Windows menu.
		  (rettet i sourcebrowser/ymer.bet: manglede binding af
		   WindowsMenuInsertMenu)
		. "Show Diagram" på gruppe med mere en en fragmentform detailer
		  ikke til astknude, som er valgt i Sif.
		. ff slettet fra fg medfører "Select new location of file..."
		  dialog. ("Yes" med udpegning af samme fg som allerede var der
		  medfører "Select new..." dialog igen og "yes" efterflgt. af
		  "Cancel" medfører segmentation fault)

toby 040299:	(for once in Danish - sorry :-)
		. crash i fb. m.: 1) edit i freja 2) undo i sif =>
		  diag# fjernes ikke
		. crash pga. remapping af ID'er i fb. m. load af "graphics"
		. qua-fejl i updatePrefixInfo i fb. m. parseDiagram
		. recover => inkonsitens dialog - det burde som regel ikke ske!
		. klik på CommentMark => scroll til bund af diag samt
		  scroll til top/bund v. åbning af kommentar
		. fjerne foranstående blanke i kommentarer
		. layout (nye klasser placeres længere og længere væk...suk!!)
		. sæt fokus i dialoger og bind "Return" til "OK"
		. manglende "Save" dialog efter udelukkende layoutændringer
		. Object Diagrams fjernes i denne version
		. "Textediting on this level may imply..." dialog går
		  i teksteditering selvom der trykkes "Cancel"
		. "Diagrams->New Diagram" efterfulgt af "Cancel" =>  
		  "Could not create FormEditor for " i konsol
		. "New Class" dialog har title "Operation"
		. disable ikke-relevante menuer (disable debug menu entries
		  i View menu)
		. "ryd op" i "New Diagram", "Show Class Diagram" etc.
		. "..." efter menu items, der foresager dialog
		. "false -> displayWarnings" i guienv
		. test udskrifter fjernes (ja ok, naaede ikke dem alle!)
		. Layout Preferences... bør flyttes over i option databasen 
		  under en Freja Options.
		. kommentar box bør have hvid istedet for gennemsigtig
		  baggrund

toby 080199:	. Fixed problem with empty Windows menu in Freja.
		. Fixed missing update of CommentNode after edit.

toby 070199:	. Implemented "Load Graphics Pages".
		. Corrected error in onActivate on bifrostPage
		  (currentPage was not set correctly).
		. If you answer "No" to update diagram dialog conversion
		  of diagram to graphics is now offered (using above
		  mentioned load graphics stuff).
		. Corrected error in bifrost/private/ps/EPSRead in
		  bifrostLoadPicture i connection w. load of multiline
		  (crashed because of missing check for negative numbers
		  in regexp-match).
		. fixed NoSuchFileError in connection w. print
		. fixed ref. none in removeDiagram (I think!).
		. Removed not implemented stuff from menues and dialogs.
		. Made Freja pages appear in editor's Windows menu.
		. Inserted editor's Windows menu on Freja pages - but
		  needs fixing: menu is sometimes empty!.
		. Added "Scroll Selection Into View" entry in ViewMenu

toby 191198:	. Show Class Diagram: Selection of current node.
		. ParseDiagram: updateRelations call in openFormsInSif
		  moved out (if more than one fg it resulted in call
		  to updateRel. before all fg's were checked for
		  consistency)
		. Fixed ess error report: "no" to update dialog resulted
		  in ref. is none.
		. Fixed ref. is none in OpenFragmentForm (I think!).
		. Aggregations: multiplicity+attachRight is preserved
		  after check.
		. Split up frejabody into frejabody, dialogbody,
		  aggregationdialogbody, associationdialogbody.
		. Fixed size of box arround diagram when unchecking
		  "Show Attributes".

toby 181198:	Split up gppinterfacebody to make compilation on HP
		possible.
		If diag# existed but not diag existed and
                one answered "no" to recover dialog, it resulted in
		"Could not load diagram". Fixed so that it opens
		ast/bet file instead.
		Tried to fix selection of current node in connection
		w. "Show Class Diagram" but it doesn't work yet.

toby 010998:	Some more work on "round-trip" stuff.
		Diverse right-click-popup-menus implemented.

toby 040898:	Implemented "round-trip" stuff.
		Fixed old ref. none bug in SimpleNode.DisplayReferences.

toby 300798:	Problems with focus fixed.
		Implemented restore of attributes, operations and
		localClasses lists.
		Removed notifyFreja stuff in editor and found out
		that freja works without problems :-)

toby 280798:	New notification system (in mps) working, except minor
		problems with focus events.

toby 180698:	Most part of ast-index independency implemented.

toby 120598:	Restructured freja so that frejalib now only contains
		fragmentBrowser dependent code and frejaenv now contains
		the rest. Futhermore fragmentBrowser instance is created
		dynamically in private/frejaapplicationbody if standalone
		freja is generated and in private/frejafriggapplicationbody
		if linked together freja/frigg application is genrated.

toby 050598:	removed old revert textedit code from edit menu

toby 230498:	minor changes because of restructuring in gpp's
		patternDiagrams.

toby 090298:	Corrected: Save crashed when refering to
		 uninitialized theOADDocument.

toby 090298:	Read-only mode is set when diagram is loaded where
		one of the .diag or .freja files are read-only.

toby 050298:	Restructured RE in SimpleAttributeDecl.display to
		use new setupRelations that is also used from
		redisplayRelations (fixes bug regarding RE on
		embeded associations).
		Safer filehandling when saving and opening diagrams
		Fixed problem with selecting same radiobutton in
		"name", "name and type" etc. in layout preferences.

toby 030298:	Removed debug putformats that caused bool->int
		warnings.

toby 290198:	Corrected error in getParent that made clear on
		specializations dump.
		Made popup work properly for aggregations.

toby 290198:	Added new popup menu on associations and aggregations.
		Made sub region of super when check draws prefixconn.
		Corrected error in multiplicity fields in assocation
		dialog.

toby 210198:	A number errors corrected.
		Page "File" menu now basically equals Sif's "Freja" menu

toby 190198:	Corrected errors related to file open/save and recovery

toby		ExternalInterface.groupChecked: checks whether or not
		there is a Document.

toby 161297:	Corrected:
		Doing RE of embeded associations in SimplAttrDeclDisplay:
		If class in other end of assoc. had other embeded
		associations arbitrary other end was inserted in
		patternDiagrams.associationList.

		gpp	M private/simplenodebody.bet

toby 151297:	Corrected container library version used in freja
		code generation from v1.5 to v1.6.

toby 151297:	Added edit item to Relations menu. Can be used
		to edit a selected aggregation or association.
		(i.e. brings up aggregation-/associationdialog).

		Added "Implementation" and "Display Preferences"
		buttons to Aggregation- and associationdialog.
		Implementation button brings up implementation
		canvas with choices of f.ex. which container class
		to use in one-to-many associations.
		Display Preferences button brings up choices of f.ex.
		whether to show role name or not.

		Added edit virtual to deletableConnectors - virtual
		is bound in aggregation and association connector.

		Added AttachRight to Role in AssociationConnector (used
		to set/get whether role is physically attached to right
		or left side of class).

		Added display preferences enter parameters to interactiveNew
		and Redisplay in AssociationConnector and its Role.

		Corrected error in aggregation role visible set function.

		Corrected association library version used by freja
		from v3.0 to v4.0.
		
		Corrected error in getParent in designenv.
	
		freja		M frejalib.bet
				M private/frejabody.bet
				M private/frejamenu.bet

		gpp		M gppinterface.bet
				M private/abstractnodebody.bet
				M private/gppinterfacebody.bet
				M private/simplenodebody.bet

		designenv	M private/designobjectbody.bet

toby 051297:	Turned off repair (update of screen) during all compound
		operations - for instance in Detail Recursively.

		Replaced search for text '/' with platform independent
		DirectoryChar.

		Files changed:
		freja	M frejalib.bet
			M private/frejabody.bet
			M private/frejamenu.bet

		gpp	M gppinterface.bet
			M private/designinterfacebody.bet
			M private/fragmentnodebody.bet
			M private/gppinterfacebody.bet
			M private/menubody.bet
			M private/objectprivate.bet
			M private/propertynodebody.bet


VERSION 3.0
=============
With v3.0 the following changes were introduced:

Linux final to virtual binding does not work - large hack to
work arount needed Type::Role final binding.
Involved files:

private/frejabody.bet;
  new revision: 1.89.2.1; previous revision: 1.89



VERSION 2.2
=============
With v2.2 the following changes were introduced:


freja.bet:
==========
toby 280596: 	Update to basiclib/v1.5

toby 140696:	Menu bodies and external Sif event bodies handles all
		types of pages (OADPage, ObjectPage, Page), preventing
		qua-error.
		Search moved from Edit menu to View menu to resemble Sif.
		Cut, copy and paste works on all types of pages (except
		for semantic copy of objects in object diagrams) and
		between the different types of pages. This enables user
		to copy e.g. pattern diagram to (newly implemented) graphics
		page and edit it as graphics only. Notice: pattern diagrams
		can not directly be pasted as graphics to Work sheets. This
		is to prevent user confusing semantic with non-semantic
		objects.

toby 150806:	On Freja startup Sif is Hidden.
		Almost all dialogs are now GuiEnv dialogs (also before
		document initialization) - not designOA dialogs.
		All settings regarding layout are now controlled via
		a layout preferences dialog.


frejamenu.bet:
==============
toby 280596: 	Update to basiclib/v1.5

toby 140696:	Menu bodies handles all	types of pages (OADPage, ObjectPage,
		Page), preventing qua-error.
		Arbitrary hierarchies of regions can be cut, copy and pasted
		(necessary to enable copying of object diagrams to graphics
		pages).
		Implemented cut, copy and paste of connectors in arbitrary
		combinations of groups and regions.

toby 150896:	If a connector only is on the clipboard paste is now
		possible.
		Almost all dialogs are now GuiEnv dialogs (also before
		document initialization) - not designOA dialogs.
		On 'New Pattern' or 'local Pattern' 'Edit Name' is
		automatically invoked.


frejabody.bet:
==============
toby 280596: 	Update to basiclib/v1.5

toby 140696:	External Sif event bodies handles all types of pages (OADPage,
		ObjectPage, Page), preventing qua-error.

toby 150896:	Added promptForBoolean pattern that calls GuiEnv prompt-
		ForBoolean. Includes hack that makes sure window is resized
		properly when using multi-line messages.
		On creation of specializations subclass is moved to new
		position determined via function in new treeprettyprint
		library.
		MouseDown callback is prepared for manual positioning of
		diagrams.
		Implementation of layout preferences dialog (see freja.bet).


frejaexternalinterfacebody.bet:
===============================
toby 280596: 	Update to basiclib/v1.5

toby 140696:	External Sif event bodies handles all types of pages (OADPage,
		ObjectPage, Page), preventing qua-error.


sdecommunication.bet:
=====================
toby 280596: 	Update to basiclib/v1.5
