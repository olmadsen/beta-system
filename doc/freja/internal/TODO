Konstaterede fejl/uhensigtsmaessigheder i r4.2.1.dsa (eller r5.0)
==============================================================================

X/Y = X prioritet, Y kompleksitet, hvor H = Høj, M = Mellem, L = Lav.
KIG = Kan Ikke Genskabes.

-------------------------------------------------------------------------------
	H/?	. Abstract og Detail tager ofte meget lang tid, bør optimeres
		  (timeglas gentagne gange tager kan f.eks. tage lang tid)
	H/?	. flersidet udskrift
	H/?	. preview bitmap i eps-fil
	H/L	. Når man trykker en gang på scrollbaren ned ad, rykker
                  den mere end et vinduets højde ned.
                  Kommentar: proev at saette defaultvaerdier
	H/L	. kommentar-"boller" kan ikke flyttes over paa venstre side
		  * lav en pop up menu ved højre klik på "bollen" eller linien.
	H/L	. popup Edit:Description texteditoren skal have samme bredde
		  som et normalt prettyprint, dvs. 80 tegn. 
		  Maaske skal hoejden ogsaa vaere stoerre.
	H/L	. timeglas
	H/L	. statusbar: mere sigende informationer
	H/L	. bør ikke være rammer om SLOT
	H/L	. nogle gange nødvendig med manuel refresh
	H/L	. "semantisk browsning": f.eks. hvis "com:@commands" vælges
		  skal det være muligt via højreklik e.l. at få vist "commands"
		  pattern'et (Eller: Find og vis sub-/super-patterns i nuvaerende
		  aabne fragmenter.
	H/M	. Ændres navnet på en klasse, ændres navnet på de tilsvarende
		  associationsklasser ikke.
	H/M	. Hvis man laver 2 klasser og en association mellem dem, og
		  dernaest sletter den ene klasse, vil associationen stadig
		  vaere i koden. Denne refererer til den slettede klasse og
		  koden kan ikke checkes.
	H/H 	. fillMultiLine [ref. none] - KIG!
	H/H	. edgeSpecializations [ref. none] - KIG!
	H/H	. updateAggregationInfo [qua error] - KIG!
	H/H	. freja: layout:
		    - benytte marius' implementation af automatisk
		    layout-algoritme
		    - "manuel" placering
	H/H	. klasser etc. bør ikke være transperante
	H/H	. Farver (problem m. "send-to-back" og ps-fil gemme-format)
	H/H	. undo
	H/H	. treeview: istedet for "lib" og "program" kasser i diagram
		  (erstatter "Group Page"). I første omgang laves
		  specialiserede treeviews for hvert tool (freja, frigg,
		  browser) istedet for et integreret
	H/H	. vise diagram for del-ast (så man f.eks. ikke får omgivende
		  klasser for lokal klasse med i diagram)
	H/H	. show/hide af vilkårlige elementer (klasse, attribut etc.)
	H/H	. "inspector" (á la friggs "object inspector"): kan både have
		  "code inspector" (i.e. subeditor som skifter indhold i fht.
		  hvad der er valgt i freja) og "freja specific inspector"
		  (i.e. svarende til dialoger, som bruges v. popmenuens
		  "Edit...")
	H/H	. Evt. bruger valg mellem alternative implementationer af
		  abstrakte associationsklasser (brug for nyt design af
		  associationbiblioteket!).
	H/H	. Evt. palette, hvorfra de forskellige relationer kan vælges.
	H/H	. Forslag: 'Unified method' notation udvides med notation for
		  pattern referencer og nestede patterns.
	H/H	. visualisering af nestede patterns som ved objekt-diagrammer?
	H/H	. haandtering af mere end 2 sider.
	M/L	. I fb. m. kodegen. af association kan der blive genereret kode
		  som f.eks. 'rightType::< A.localToA', hvor A er et pattern
		  (og det er jo en semantisk fejl...)
		  Mulig løsn.: 'rightType::< <<NameAppl>>.localToA'
	M/L	. horisontal visning af specialiseringshierarki som
		  alternativ til nuværende vertikal prettyprint ("layout
		  subpatterns")
	M/M	. genkendelse af "virtuelt kvalificerede referencer" (se
		  email fra miksen fra 090299)
	L/L	. Ved RE på repetition vises "*" istedet for repetitionindex.
	L/L	. associations biblioteket bør nok have et interface til at
		  slette elementer fra associationen.
	L/L	. selektionen bør skifte v. højreklik (som v. venstreklik)
	L/L	. Skal være muligt at udpege enkelte attributter som
		  endepunkter for association. Disse attributter definerer så
		  referencen til association objektet (og bevarer deres
		  oprindelige navn).
	L/M	. kan ikke kende forskel på operationer og klasser
		  (generelt: bedre visualisering af forskel på attributer,
		   operationer etc.)
	L/H	. Kunne være rart, hvis man kunne undgå at få vist association
		  attributterne
	L/H	. editering i erklæringer, hvor der er mere end et element i
		  nameslisten (e.g. a,b:@integer), giver (bl.a.) forkert
		  localNodes liste
	L/H	. Menuer "flimrer" ved opstart.
	L/H	. Objektdiagrammer bringes i stand, så de kan re-enables.
	L/H	. ville være rart hvis man på vilkårligt tidspkt. kunne
		  fortælle et vindue, at det skal være "floating" (i fb. m.
		  freja f.eks. ved sub-/separate editorer)
	L/H	. "klippemærker" på flersidede ("tiled") udskrifter af
		  diagrammer (á la QualiWare)
	L/H	. Skal vaere muligt at saette tekstattributter saa som
		  fontsize og alignment.
	L/H	. Association patterns har ikke interface, der sikrer, at
		  relationerne er konsistente.
		. Funktioner som er i "Relations" menuen bruger man en gang,
		  hvorefter man er tilbage i "standard" mode, mens
		  funktionerne i "Create" menuen kan bruges flere gange efter
		  hinanden, uden at der skal vælges hver gang.
	L/H	. bagudkompatibilitet af .diag filer, f.eks. ved at tilfoeje
		  versionsnr og tillade read af de seneste versioner.


DONE.r5.0
-------------------------------------------------------------------------------
toby	DONE	. Selv-aggregering/associering
		  (Quick fix: kode bliver genereret, men relation tegnes ikke)
pahe/toby DONE	. Multipliciteter fjernes ikke ved hide, et generelt problem 
          	  med labels
mm/pahe DONE	. Fontload problemer
mg	DONE	. zoom paa pc (check om det virker)
hml	DONE	. idleEvent paa mac (i fb. pahe impl. af zoom)
toby	DONE	. Klassenavn centreres ikke og kan forveksles med en attribut.
datpete	DONE	. Bifrost: Floating Point division by zero.
		  (datpete kommentar: Jeg har nu indsat og committet hacket
		  for liniebredder 0,1,2 i bifrost der forhåbentlig gør at
		  freja ikke foresager division by zero/floating point
		  exception.)

mm	DONE	. centrering/fokus af diagram første gang Worksheet vises
		  * placer venstre øverste klasse i Ø.V. hjørne
toby	DONE	. Kommentarer på localNodes bør også forsvinde når 'Show
		  Attributes' er slået fra.
		  * commentmark og selve kommentaren skal også hides
toby	DONE	. View menuens layout preferences skal bruge samme interface
		  som popup-menu i fb. m. "Show Name" etc.
		  * dialogen fjernes fra menu indgangen
		  Show all attributes ...
		  Hide all attributes ...
		  * erstattes med globale operationer under view-menuen
toby	DONE	. overflødig autosaveautosave dialog ved "Show Diagram" efter
		  edit i sif.
pahe	DONE	. zoom ind/ud
toby	DONE	. bedre enable/disable af menuindgange i fht. current selection
		  * fx: cut rectangle
		      clear bl.a. lib
		  * lav et interface til at kunne disable menu indgange, når
		    guienv skal vise menuen. 
toby	DONE	. bifrostSavePage should write data in two steps: first to text
		  object then in one go to file (trying to avoid inkonsistent
		  diagramfiles).
mm 	DONE	. "New Class" med alle attributter slettet fra fragmentform
		  [ref. none]
mm 	DONE	. slet alt via codeeditor
toby	DONE?	. ListDiagramNode.onRemove i fb. m "Abstract" [ref. none]
		  (ikke 100% sikker på den er rettet, men fejlen opstod i fb. m.
		   sletning af fragmenter i parseDiagram og strategien herfor er
		   nu lavet temmelig meget om)
toby	DONE	. RemoveScan i fb. m. DeleteDiagramNode [ref. none]
		 (rettet ved at fjerne IndexIDList, som var årsag til problem,
		  men ikke længere blev brugt)
mm 	DONE	. går ned, hvis "Move Up/Down" er enablet, når de ikke burde
		  være det
mm	DONE	. fryser ved forsøg på: A arver fra B og B arver fra A

mm	DONE	. klasser kan blive placeret udenfor scroll-området og kan
		  dermed ikke nåes igen
mm	DONE	. Freja's editerings operationer bør være disablede ligesom
		  Sif's, når Sif er i tekstediteringsmode.
		  * få event fra sif. Der findes en generel metode i freja.
mm	DONE	. Ref none ved: aggregering fra klasse til lib
toby	DONE	. Der kommer ikke nogen aggregeringspil til statiske instanser
		  af singulære
toby	DONE	. "..." på singulære instanser
mm	DONE	. Radiobtn's i operation/class dialog: skal være muligt at
		  de-selektere virtuel (alternativt indfør "standard"
		  radiobtn)
		  * indføre et "Normal" button og overskriften ændres fra
		    Virtual til Type.

toby    DONE	. Problem med opdatering ved editering af root via sif
		  (Skyldtes manglende opdat. af astIndex på fragmentnode i
		   parseDiagram)
mm	DONE	. getInt exception i fb. m. åbning af ethvert diagram
toby	DONE	. WorkSheet og Group Page vises ikke i Windows menu.
		  (rettet i sourcebrowser/ymer.bet: manglede binding af
		   WindowsMenuInsertMenu)
toby	DONE	. "Show Diagram" på gruppe med mere en en fragmentform detailer
		  ikke til astknude, som er valgt i Sif.
toby	DONE	. ff slettet fra fg medfører "Select new location of file..."
		  dialog. ("Yes" med udpegning af samme fg som allerede var der
		  medfører "Select new..." dialog igen og "yes" efterflgt. af
		  "Cancel" medfører segmentation fault)
toby	DONE	. "Show Diagram" i Diagram menu virker ikke (popup-menuens gør).
toby	DONE	. Hvis der er blevet indsat en ff uden at diagrammet var åbnet,
		  så opdaterer freja ikke tilsvarende næste gang diagram åbnes.
toby	DONE	. AggregationList elementer med prefix, som er et subpattern af
		  en container, bliver ikke loaded.
toby	DONE	. GetValuesFromCode søger ikke op gennem prefix-kæden efter
		  container-prefix (som SetupRelations gør).
toby	DONE	. Aggregation diamant bliver ikke altid sort, som den skal, når
		  det er by-value.
toby	DONE	. Abstract kan gå ned, hvis der en aggregeringspil fra en
		  attribut til sig selv.
fg	DONE	. preferences via preferences-DB
	DONE	. der kommer en milliard:
		   CreatePolygonRgn failed in shapeMakeRegion. ErrorCode= 8
