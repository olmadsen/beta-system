proto.scanRefs
  (# 
  do (if isStatic then
         '\tStatic reference at offset  ' -> puttext
      else
         '\tDynamic reference at offset ' -> puttext
     if);
     thisOffset -> putint; newline
  #);
