ORIGIN '../ntdirectory';
INCLUDE '~beta/basiclib/v1.6/private/directory_ntbody';

---- NtDirFindDispatch:dopart ----
do INNER;
   (if selectedInInner // false then
       (if 1
	// private.statBuf[8] then whensocket;
	// private.statBuf[7] then whensymboliclink;
	// private.statBuf[4] then whencharspecial;
	// private.statBuf[5] then whenblockspecial;
	// private.statBuf[3] then whendir;
	// private.statBuf[6] then whenfile;
	else whenother;
       if);
       true -> selectedInInner;
   if)

---- NtDirScanDispatch:dopart ----
do INNER;
   (if selectedInInner // false then
       (if 1
       	// private.statBuf[8] then whensocket;
       	// private.statBuf[7] then whensymboliclink;
       	// private.statBuf[4] then whencharspecial;
       	// private.statBuf[5] then whenblockspecial;
       	// private.statBuf[3] then whendir;
       	// private.statBuf[6] then whenfile;
	else whenother;
       if); 
       true -> selectedInInner;
   if)
