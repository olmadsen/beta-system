ORIGIN '~beta/basiclib/directory';
--PROGRAM: descriptor--
(# d: @directory;
do (for i:NoOfArguments-1 repeat
        i+1 -> arguments -> d.name;
        (if d.entry.exists then
            d.name -> puttext; ': exists.'->putline;
         else
            d.touch;
        if)
   for);
#)
