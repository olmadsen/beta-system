ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/basiclib/private/systemExceptionHandler';
INCLUDE 'tstexceptlib';
-- program: descriptor --
(# a: @|(# pp: @p(# do Object.%break #);
        do pp
        #);
   b: @|(# qq: @q(# do a #);
        do qq
        #);
do runtimeExceptionHandler##->InstallSystemExceptionHandler;
   try
   (#
      name:: (# do 'emulatorTry'->n[] #);
      handler::
        (# 
        do when (# type:: emulatorTrapException
                do 'emulatorTry handler...aborting'->putline;
                   abort;
                #);
        #);
   do b
   #);
#)

