ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/basiclib/private/systemExceptionHandler';
INCLUDE 'tstexceptlib';
-- program: descriptor --
(# a: @|(# pp: @p(# r: @real do r/0.0->r; (* FlZeroDivErr: Floating Point Division By Zero *) #);
        do pp
        #);
   b: @|(# qq: @q(# do a #);
        do qq
        #);
do runtimeExceptionHandler##->InstallSystemExceptionHandler;
   try
   (#
      name:: (# do 'fpTry'->n[] #);
      handler::
        (# 
        do when (# type:: fpExceptException
                do 'fpZeroTry handler...aborting'->putline;
                   abort;
                #);
        #);
   do b 
   #);
#)

