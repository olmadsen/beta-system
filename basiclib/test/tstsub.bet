origin '../betaenv'
-- program: descriptor --

(# name: ^text;
do 'DoLoad#' -> name[];
   (if (name.length -> name.inxGet)
    //'#' then (1,name.length-1) -> name.sub -> name[]
   if);
   'if done' -> putline;
   name[] -> putline;
#)
