ORIGIN '../betaenv'
    --- program: descriptor ---
    (# (* Random?! *)

       l:[2] @integer;

    do -4 -> l.extend;
       l[-2] -> putint; newline;
       l[-1] -> putint; newline;
       l[0] -> putint; newline;
       l[1] -> putint; newline;
    #)
