ORIGIN 'betaenv';
(*
 * COPYRIGHT
 *       Copyright (C) Mjolner Informatics, 1984-98
 *       All rights reserved.
 *)
-- LIB: attributes --
mbs_version:
  (* Defines the current version of the Mjolner BETA System 
   * in various ways. Typical usage:
   * 
   *   (if (mbs_version).major >= 4 then
   *       ...
   *   if);
   *)
  (# major:    (# exit 5 #);
     minor:    (# exit 0 #);
     revision: (# exit 0 #);
     release:  (# exit 050000 #);
     desc:     (# exit '5.0' #);
  exit THIS(mbs_version)[]
  #);
 
