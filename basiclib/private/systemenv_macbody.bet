ORIGIN 'basicsystemenv_macbody';
-- BasicScheduler: Descriptor --
(# do
   initBeforeScheduler;
   run: cycle
     (# value: @Integer; 
     do
        (if private.attNext->value
         // 0 then (* Ok *)  // 1 then deadLocked;  // 2 then leave run; 
         else
        (* -value is the time in seconds until some component 
         * is interested in continuing. We need some general
         * mechanism for dispatching events to threads before
         * we are able to handle this properly. Currently we'll simply
         * continue. In the current way of using macenv and systemenv
         * together, this situation will never occur. *)
            
        if);
        
     #);
   
#)  

-- pioEnsure: Descriptor --
(# #)  

-- forksecond: Descriptor --
(# #)  

