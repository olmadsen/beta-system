ORIGIN '../fileSetExe';
INCLUDE '~beta/javalib/betahelpers';

---file_setExe:doPart---
do (* Cannot use class FilePermission: only allows *reading* permission.
    * Setting execution permission is not possible using Java class
    * libraries, since this is not considered platform independent.
    * So we have to use a native method implemented in BetaHelpers.
    *)
   BetaHelpers.loadJNILibraries;
   name ->  BetaHelpers.setFileExePermission;

   
