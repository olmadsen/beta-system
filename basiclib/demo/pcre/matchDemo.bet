ORIGIN '~beta/basiclib/pcre';
--- program: descriptor ---
(#
do loop:
     (# regexstring: ^text;
        regex: @Pcre;
     do 'Match: ' -> puttext; getLine -> regexstring[];
        (if regexstring.length = 0 then leave loop if);
        regexstring[] -> regex;
        loop:
          (# string: ^text;
          do 'Match: '->puttext; regexstring[]->puttext;
             ' against: ' -> puttext; getline -> string[];
             (if string.length = 0 then leave loop if);
             (if string[] -> regex.match then
	         'Matches!' -> putline;
	     else
	         'Doesn\'t match!' -> putline;
	     if);
             restart loop
          #);
        restart loop
     #)
#)
