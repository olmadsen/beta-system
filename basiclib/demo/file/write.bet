ORIGIN '~beta/basiclib/file';

(* Simple wrting of a file *)

--PROGRAM: descriptor--
(# outFile: @file;
do 'output' -> outFile.name;
   outFile.OpenWrite;
   'One line of text' -> outFile.putline;
   'Another line of text, with a number: ' -> outFile.puttext;
   10 -> outFile.putInt;
   outfile.newLine;
   outFile.close;
#)
