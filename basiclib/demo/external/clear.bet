ORIGIN '~beta/basiclib/v1.5/betaenv';
(* This small program illustrates how to invoke an external
 * C-function.  In this case the UNIX 'system' function.  'system'
 * takes a text string and executes it as a UNIX shell command.  In
 * this program, 'system' is used to invoke the 'clear' commend, which
 * will erase the contents of the screen terminal.
 *)
--PROGRAM: descriptor--
(# system: external(# cmd: [0]@char enter cmd #);
do 'clear' -> system;
#)
