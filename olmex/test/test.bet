ORIGIN '~beta/basiclib/betaenv';
---lib:attributes---
foo:
  (# fisk: @ integer;
     snorf: ^Bar;
     V:< Bar;
     W:< (# do inner  W#);
     Q:< bar(#do inner #)
  enter x
  do 117 -> x; x + 118 -> x
  #);
bar:
  (# VV:: Olsen;
     WW::< Jensen;
     QQ:: bar (# exit 122 #);
     F: ##Foo;
     A: [12] @integer
     B: [33]  ^Bar;
  do V * V + 88 -> W;
     S[] -> R[];
     S## -> R##;
     &Foo[] -> R[];
     &Foo;
     (for i: 3 repeat i + 1 -> x for);
     (if a = b then a + 1 -> b if);
     (if true
      // B then
         11 -> a
      // C then
         22 -> a
      else
         33 -> c
     if);
     LLL: 
       (if a then 
           0 -> a;
           R.foo;
           (R[]->S[]).bar;
           leave LLL
           restart LLL
       if)
  #)
