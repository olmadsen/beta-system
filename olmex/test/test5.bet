ORIGIN '~beta/basiclib/betaenv';
---program:descriptor---
(#
do
#)
---lib:attributes---
BreakEx:
  (#
  do LLL:
       (#
       do 'Q'->put;
          (if true then
              leave LLL;
           else
          if);
          '?'->put
       #);
     'W' -> put; 

     LccLab: 
       (# i: @integer 
       do 'y' -> put;
          (if (i + 1 -> i) > 2 then 
              '!'-> put; 
              leave LccLab; 
              '+'->put
           else
              'y' + i -> put;
              restart LccLab
          if);
          '?'->put;
       #);
  #)

