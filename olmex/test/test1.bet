ORIGIN '~beta/basiclib/betaenv';
INCLUDE 'test3';
---program:descriptor---
(#
do
#)
---lib:attributes---
BreakEx:
  (#
  do 'a' -> put;
    
     LLL: cycle
       (# n: @integer 
       do (if (n + 1 -> n) > 2 then 
              '%'->put;
              leave LLL 
           else
              'a' + n -> put 
          if);
          '/'->put
       #)
  #)
