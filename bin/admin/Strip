#!/bin/sh

# script used by ~beta/system/vx.x/update


case "`uname -m`" in
  sun4*)
    case "`uname -r`" in
      5.*) PATH=/usr/ccs/bin:$PATH; export PATH
           ;;
    esac
esac


echo `which strip` $1
strip $1
