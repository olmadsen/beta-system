#!/bin/sh
# pwd must be STATLOCATION
# argument 1 must be APPLICATION
# REPORTDIR is 'reports'

mkdir reports
chmod a+w reports
ls -l
cd reports/
touch index.html
cat >$1.counter <<EOF
1
EOF
chmod a+w index.html $1.counter
ls -l
head $1.counter
