#!/usr/bin/env perl
# prints ALL versions in your PATH,

push(@INC, $ENV{'BETALIB'} . "/bin/admin");

require "utils.perl";

if ($#ARGV != 0){
    print "usage: Which [-v] cmd\n";
    exit (0);
}

#$verbose = 1;
print &which($ARGV[0], 1);

