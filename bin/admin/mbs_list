#!/usr/local/bin/perl5 -s
#-*-Perl-*-

require "dirlist";

foreach $dir (@Dirs) {
    $basedir = "$betalib/$dir";
    next if (!-d &path($basedir));
    print "------- $betalib/$dir ------- ";
    chdir &path($basedir) or die "Unable to chdir: $!";
    system 'ls -F -R';
    exit(1) if ($?);
}
