#!/usr/local/bin/perl

use Cwd;

$minorupdate = 0;
if ($ARGV[0] eq "+") {
    $minorupdate = 1;
    shift @ARGV;
}

if ($ARGV[0] eq "-m") {
    $message = $ARGV[1];
    shift @ARGV;
    shift @ARGV;
}

$cwd = cwd;

print "CWD is '$cwd'\n";

#while (! -f "version.c") {
    
