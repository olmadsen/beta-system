# List directories/files to skip in mbs_export.
# Use paths relative to BETALIB being packed
# Built-in rules in mbs_export already make the following
# files/directories be skipped:
#   emacs tilde files: *~
#   entries with suffixes: .lst, .pdb, .sln, .duo, .suo, .dln
#   CVS directories
#   entries with names TODO or test
#
# FIXME: Allow for regexp matching too, to avoid */*/... below.

.cvsignore
*/demo/reference
*/*/demo/reference
*/demo/{nti_ms,nti_gnu,linux,sun4s,macosx,clr,jvm}
*/*/demo/{nti_ms,nti_gnu,linux,sun4s,macosx,clr,jvm}
*/demo/*.ast
*/*/demo/*.ast
betarun
bin/admin
bin/*/beta-compiled-by-boot-compiler
boot
comlib
compiler
debugger
designenv
doc/admin
doc/bin
doc/Makefile
doc/build
doc/compiler/build
doc/*/.cvsignore
doc/*/Makefile
doc/internal
doc/*/internal
doc/*/src
doc/manualerror
doc/templates
dotnetlib # skipping dotnetlib for now...
editor
export
freja
gpp
interfacebuilder
javalib   # skipping javalib for now...
mjolnertool
olmex
sourcebrowser
# FIXME: skip (parts of) toollibs?
# FIXME: Skip maclib? and doc/maclib?
