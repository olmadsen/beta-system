ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Array class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Array.bet for members.
 *)
_Array: ExternalClass (* , ICloneable, IList, ICollection, IEnumerable *)
  (#
  do '[mscorlib]System.Array' -> className;
  INNER;
  #);

