ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Encoding class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Encoding.bet for members.
 *)
_Encoding: ExternalClass
  (#
  do '[mscorlib]System.Text.Encoding' -> className;
  INNER;
  #);

