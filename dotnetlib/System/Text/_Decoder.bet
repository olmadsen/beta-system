ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Decoder class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Decoder.bet for members.
 *)
_Decoder: ExternalClass
  (#
  do '[mscorlib]System.Text.Decoder' -> className;
  INNER;
  #);

