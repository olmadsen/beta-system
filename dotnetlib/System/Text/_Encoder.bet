ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Encoder class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Encoder.bet for members.
 *)
_Encoder: ExternalClass
  (#
  do '[mscorlib]System.Text.Encoder' -> className;
  INNER;
  #);

