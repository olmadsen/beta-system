ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/Enum';
--LIB: attributes--

(* Dotnet DesignerSerializationVisibility class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See DesignerSerializationVisibility.bet for members.
 *)
_DesignerSerializationVisibility: Enum
  (#
  do '@valuetype [System]System.ComponentModel.DesignerSerializationVisibility' -> className;
  INNER;
  #);

