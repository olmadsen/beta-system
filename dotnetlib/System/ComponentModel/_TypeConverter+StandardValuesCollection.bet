ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet TypeConverter+StandardValuesCollection class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See TypeConverter+StandardValuesCollection.bet for members.
 *)
_TypeConverter+StandardValuesCollection: ExternalClass
  (#
  do '[system]System/ComponentModel.TypeConverter+StandardValuesCollection' -> className;
  INNER;
  #);

