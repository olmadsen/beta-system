ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IComponent class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IComponent.bet for members.
 *)
_IComponent: ExternalClass
  (#
  do '[system]System/ComponentModel.IComponent' -> className;
  INNER;
  #);

