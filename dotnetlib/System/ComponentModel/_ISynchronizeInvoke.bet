ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet ISynchronizeInvoke class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ISynchronizeInvoke.bet for members.
 *)
_ISynchronizeInvoke: ExternalClass
  (#
  do '[System]System.ComponentModel.ISynchronizeInvoke' -> className;
  INNER;
  #);

