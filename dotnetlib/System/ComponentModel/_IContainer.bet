ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IContainer class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IContainer.bet for members.
 *)
_IContainer: ExternalClass
  (#
  do '[system]System/ComponentModel.IContainer' -> className;
  INNER;
  #);

