ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/Enum';
--LIB: attributes--

(* Dotnet CollectionChangeAction class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See CollectionChangeAction.bet for members.
 *)
_CollectionChangeAction: Enum
  (#
  do '[system]System.ComponentModel.CollectionChangeAction' -> className;
  INNER;
  #);

