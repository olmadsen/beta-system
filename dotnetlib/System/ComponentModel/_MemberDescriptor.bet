ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet MemberDescriptor class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See MemberDescriptor.bet for members.
 *)
_MemberDescriptor: ExternalClass
  (#
  do '[System]System.ComponentModel.MemberDescriptor' -> className;
  INNER;
  #);

