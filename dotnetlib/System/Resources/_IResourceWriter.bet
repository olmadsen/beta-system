ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IResourceWriter class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IResourceWriter.bet for members.
 *)
_IResourceWriter: ExternalClass (* , IDisposable *)
  (#
  do '[mscorlib]System.Resources.IResourceWriter' -> className;
  INNER;
  #);

