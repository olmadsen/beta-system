ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System.Drawing/Image';
--LIB: attributes--

(* Dotnet Bitmap class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Bitmap.bet for members.
 *)
_Bitmap: Image
  (#
  do '[system.drawing]System.Drawing.Bitmap' -> className;
  INNER;
  #);

