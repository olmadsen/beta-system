ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/_MarshalByRefObject' (* Cannot use non-wrapper *);
--LIB: attributes--

(* Dotnet Image class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Image.bet for members.
 *)
_Image: _MarshalByRefObject
  (#
  do '[System.Drawing]System.Drawing.Image' -> className;
  INNER;
  #);

