ORIGIN '_CoordinateSpace';
ON 19 (* Disable warnings about runtime qualification tests *);
INCLUDE '~beta/dotnetlib/System/_Enum';
--LIB: attributes--

(* .NET CoordinateSpace class. *)
CoordinateSpace: _CoordinateSpace
  (#
     (* Enum Literals *)

     World:
       (# exit 0 #);
     Page:
       (# exit 1 #);
     Device:
       (# exit 2 #);
  do 'valuetype [System.Drawing]System.Drawing.Drawing2D.CoordinateSpace' -> className;
  INNER;
  #);

