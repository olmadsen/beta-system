ORIGIN '_CompositingQuality';
ON 19 (* Disable warnings about runtime qualification tests *);
INCLUDE '~beta/dotnetlib/System/_Enum';
--LIB: attributes--

(* .NET CompositingQuality class. *)
CompositingQuality: _CompositingQuality
  (#
     (* Enum Literals *)

     Default:
       (# exit 0 #);
     HighSpeed:
       (# exit 1 #);
     HighQuality:
       (# exit 2 #);
     GammaCorrected:
       (# exit 3 #);
     AssumeLinear:
       (# exit 4 #);
     Invalid:
       (# exit -1 #);
  do '@valuetype [System.Drawing]System.Drawing.Drawing2D.CompositingQuality' -> className;
  INNER;
  #);

(* Utility function to ease casting to fully specified class. *)
asCompositingQuality:
  (# _C: ^_CompositingQuality;
     C: ^CompositingQuality;
  enter _C[]
  do _C[]->C[];
  exit C[]
  #);
