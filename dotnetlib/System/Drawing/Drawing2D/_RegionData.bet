ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet RegionData class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See RegionData.bet for members.
 *)
_RegionData: ExternalClass
  (#
  do '[System.Drawing]System.Drawing.Drawing2D.RegionData' -> className;
  INNER;
  #);

