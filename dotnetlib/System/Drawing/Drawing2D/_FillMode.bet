ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/Enum';
--LIB: attributes--

(* Dotnet FillMode class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FillMode.bet for members.
 *)
_FillMode: Enum
  (#
  do '[system.drawing]System.Drawing.Drawing2D.FillMode' -> className;
  INNER;
  #);

