ORIGIN '_CombineMode';
ON 19 (* Disable warnings about runtime qualification tests *);
INCLUDE '~beta/dotnetlib/System/_Enum';
--LIB: attributes--

(* .NET CombineMode class. *)
CombineMode: _CombineMode
  (#
     (* Enum Literals *)

     Replace:
       (# exit 0 #);
     Intersect:
       (# exit 1 #);
     Union:
       (# exit 2 #);
     DotnetXor:
       (# exit 3 #);
     Exclude:
       (# exit 4 #);
     Complement:
       (# exit 5 #);
  do '@valuetype [System.Drawing]System.Drawing.Drawing2D.CombineMode' -> className;
  INNER;
  #);

(* Utility function to ease casting to fully specified class. *)
asCombineMode:
  (# _C: ^_CombineMode;
     C: ^CombineMode;
  enter _C[]
  do _C[]->C[];
  exit C[]
  #);
