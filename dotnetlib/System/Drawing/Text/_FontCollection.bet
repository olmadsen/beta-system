ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet FontCollection class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FontCollection.bet for members.
 *)
_FontCollection: ExternalClass
  (#
  do '[system.drawing]System.Drawing.Text.FontCollection' -> className;
  INNER;
  #);

