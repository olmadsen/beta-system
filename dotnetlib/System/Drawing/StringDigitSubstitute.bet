ORIGIN '_StringDigitSubstitute';
ON 19 (* Disable warnings about runtime qualification tests *);
INCLUDE '~beta/dotnetlib/System/_Enum';
--LIB: attributes--

(* .NET StringDigitSubstitute class. *)
StringDigitSubstitute: _StringDigitSubstitute
  (#
     (* Enum Literals *)

     User:
       (# exit 0 #);
     DotnetNone:
       (# exit 1 #);
     National:
       (# exit 2 #);
     Traditional:
       (# exit 3 #);
  do '@valuetype [System.Drawing]System.Drawing.StringDigitSubstitute' -> className;
  INNER;
  #);

(* Utility function to ease casting to fully specified class. *)
asStringDigitSubstitute:
  (# _S: ^_StringDigitSubstitute;
     S: ^StringDigitSubstitute;
  enter _S[]
  do _S[]->S[];
  exit S[]
  #);
