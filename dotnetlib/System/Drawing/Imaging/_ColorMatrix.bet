ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet ColorMatrix class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ColorMatrix.bet for members.
 *)
_ColorMatrix: ExternalClass
  (#
  do '[System.Drawing]System.Drawing.Imaging.ColorMatrix' -> className;
  INNER;
  #);

