ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet ColorMap class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ColorMap.bet for members.
 *)
_ColorMap: ExternalClass
  (#
  do '[System.Drawing]System.Drawing.Imaging.ColorMap' -> className;
  INNER;
  #);

