ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet BitmapData class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See BitmapData.bet for members.
 *)
_BitmapData: ExternalClass
  (#
  do '[System.Drawing]System.Drawing.Imaging.BitmapData' -> className;
  INNER;
  #);

