ORIGIN '_ImageLockMode';
ON 19 (* Disable warnings about runtime qualification tests *);
INCLUDE '~beta/dotnetlib/System/_Enum';
--LIB: attributes--

(* .NET ImageLockMode class. *)
ImageLockMode: _ImageLockMode
  (#
     (* Enum Literals *)

     ReadOnly:
       (# exit 1 #);
     WriteOnly:
       (# exit 2 #);
     ReadWrite:
       (# exit 3 #);
     UserInputBuffer:
       (# exit 4 #);
  do 'valuetype [System.Drawing]System.Drawing.Imaging.ImageLockMode' -> className;
  INNER;
  #);

