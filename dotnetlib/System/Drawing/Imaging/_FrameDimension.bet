ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet FrameDimension class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FrameDimension.bet for members.
 *)
_FrameDimension: ExternalClass
  (#
  do '[System.Drawing]System.Drawing.Imaging.FrameDimension' -> className;
  INNER;
  #);

