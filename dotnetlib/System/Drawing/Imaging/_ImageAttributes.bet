ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet ImageAttributes class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ImageAttributes.bet for members.
 *)
_ImageAttributes: ExternalClass
  (#
  do '[system.drawing]System.Drawing.Imaging.ImageAttributes' -> className;
  INNER;
  #);

