ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet ImageFormat class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ImageFormat.bet for members.
 *)
_ImageFormat: ExternalClass
  (#
  do '[System.Drawing]System.Drawing.Imaging.ImageFormat' -> className;
  INNER;
  #);

