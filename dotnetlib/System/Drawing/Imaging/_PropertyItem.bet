ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet PropertyItem class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See PropertyItem.bet for members.
 *)
_PropertyItem: ExternalClass
  (#
  do '[System.Drawing]System.Drawing.Imaging.PropertyItem' -> className;
  INNER;
  #);

