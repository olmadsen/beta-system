ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet WmfPlaceableFileHeader class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See WmfPlaceableFileHeader.bet for members.
 *)
_WmfPlaceableFileHeader: ExternalClass
  (#
  do '[System.Drawing]System.Drawing.Imaging.WmfPlaceableFileHeader' -> className;
  INNER;
  #);

