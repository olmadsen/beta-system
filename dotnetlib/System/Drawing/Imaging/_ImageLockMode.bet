ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/Enum';
--LIB: attributes--

(* Dotnet ImageLockMode class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ImageLockMode.bet for members.
 *)
_ImageLockMode: Enum
  (#
  do '[system.drawing]System.Drawing.Imaging.ImageLockMode' -> className;
  INNER;
  #);

