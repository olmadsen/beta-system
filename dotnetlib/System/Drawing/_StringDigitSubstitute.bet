ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/Enum';
--LIB: attributes--

(* Dotnet StringDigitSubstitute class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See StringDigitSubstitute.bet for members.
 *)
_StringDigitSubstitute: Enum
  (#
  do '[system.drawing]System.Drawing.StringDigitSubstitute' -> className;
  INNER;
  #);

