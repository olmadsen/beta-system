ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Delegate class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Delegate.bet for members.
 *)
_Delegate: ExternalClass (* , ICloneable, ISerializable *)
  (#
  do '[mscorlib]System.Delegate' -> className;
  INNER;
  #);

