ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Attribute class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Attribute.bet for members.
 *)
_Attribute: ExternalClass
  (#
  do '[mscorlib]System.Attribute' -> className;
  INNER;
  #);

