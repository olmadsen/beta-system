ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IAsyncResult class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IAsyncResult.bet for members.
 *)
_IAsyncResult: ExternalClass
  (#
  do '[mscorlib]System.IAsyncResult' -> className;
  INNER;
  #);

