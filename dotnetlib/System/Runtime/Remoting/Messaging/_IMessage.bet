ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IMessage class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IMessage.bet for members.
 *)
_IMessage: ExternalClass
  (#
  do '[mscorlib]System.Runtime.Remoting.Messaging.IMessage' -> className;
  INNER;
  #);

