ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IMessageCtrl class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IMessageCtrl.bet for members.
 *)
_IMessageCtrl: ExternalClass
  (#
  do '[mscorlib]System/Runtime/Remoting/Messaging.IMessageCtrl' -> className;
  INNER;
  #);

