ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IMessageSink class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IMessageSink.bet for members.
 *)
_IMessageSink: ExternalClass
  (#
  do '[mscorlib]System.Runtime.Remoting.Messaging.IMessageSink' -> className;
  INNER;
  #);

