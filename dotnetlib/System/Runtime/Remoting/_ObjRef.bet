ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet ObjRef class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ObjRef.bet for members.
 *)
_ObjRef: ExternalClass
  (#
  do '[mscorlib]System.Runtime.Remoting.ObjRef' -> className;
  INNER;
  #);

