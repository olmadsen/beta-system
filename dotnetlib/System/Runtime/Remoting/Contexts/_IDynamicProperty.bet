ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IDynamicProperty class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IDynamicProperty.bet for members.
 *)
_IDynamicProperty: ExternalClass
  (#
  do '[mscorlib]System.Runtime.Remoting.Contexts.IDynamicProperty' -> className;
  INNER;
  #);

