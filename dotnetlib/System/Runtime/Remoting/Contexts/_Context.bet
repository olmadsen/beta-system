ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Context class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Context.bet for members.
 *)
_Context: ExternalClass
  (#
  do '[mscorlib]System.Runtime.Remoting.Contexts.Context' -> className;
  INNER;
  #);

