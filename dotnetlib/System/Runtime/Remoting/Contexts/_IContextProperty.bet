ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IContextProperty class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IContextProperty.bet for members.
 *)
_IContextProperty: ExternalClass
  (#
  do '[mscorlib]System.Runtime.Remoting.Contexts.IContextProperty' -> className;
  INNER;
  #);

