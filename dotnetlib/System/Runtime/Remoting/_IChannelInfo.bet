ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IChannelInfo class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IChannelInfo.bet for members.
 *)
_IChannelInfo: ExternalClass
  (#
  do '[mscorlib]System/Runtime/Remoting.IChannelInfo' -> className;
  INNER;
  #);

