ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet ICollection class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ICollection.bet for members.
 *)
_ICollection: ExternalClass
  (#
  do '[mscorlib]System.Collections.ICollection' -> className;
  INNER;
  #);

