ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IHashCodeProvider class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IHashCodeProvider.bet for members.
 *)
_IHashCodeProvider: ExternalClass
  (#
  do '[mscorlib]System.Collections.IHashCodeProvider' -> className;
  INNER;
  #);

