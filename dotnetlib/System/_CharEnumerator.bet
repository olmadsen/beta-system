ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet CharEnumerator class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See CharEnumerator.bet for members.
 *)
_CharEnumerator: ExternalClass
  (#
  do '[mscorlib]System.CharEnumerator' -> className;
  INNER;
  #);

