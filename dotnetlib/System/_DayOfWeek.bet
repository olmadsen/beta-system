ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/Enum';
--LIB: attributes--

(* Dotnet DayOfWeek class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See DayOfWeek.bet for members.
 *)
_DayOfWeek: Enum
  (#
  do 'valuetype [mscorlib]System.DayOfWeek' -> className;
  INNER;
  #);

