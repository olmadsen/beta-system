ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Directory class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Directory.bet for members.
 *)
_Directory: ExternalClass
  (#
  do '[mscorlib]System.IO.Directory' -> className;
  INNER;
  #);

