ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet File class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See File.bet for members.
 *)
_File: ExternalClass
  (#
  do '[mscorlib]System.IO.File' -> className;
  INNER;
  #);

