ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet BinaryReader class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See BinaryReader.bet for members.
 *)
_BinaryReader: ExternalClass (* , IDisposable *)
  (#
  do '[mscorlib]System.IO.BinaryReader' -> className;
  INNER;
  #);

