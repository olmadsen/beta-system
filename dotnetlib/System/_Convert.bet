ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Convert class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Convert.bet for members.
 *)
_Convert: ExternalClass
  (#
  do '[mscorlib]System.Convert' -> className;
  INNER;
  #);

