ORIGIN '_AccessibleRole';
ON 19 (* Disable warnings about runtime qualification tests *);
INCLUDE '~beta/dotnetlib/System/_Enum';
--LIB: attributes--

(* .NET AccessibleRole class. *)
AccessibleRole: _AccessibleRole
  (#
     (* Public/family CLS compliant fields *)

     value__: @int32;

     (* Enum Literals *)

     DotnetNone:
       (# exit 0 #);
     TitleBar:
       (# exit 1 #);
     MenuBar:
       (# exit 2 #);
     ScrollBar:
       (# exit 3 #);
     Grip:
       (# exit 4 #);
     Sound:
       (# exit 5 #);
     Cursor:
       (# exit 6 #);
     Caret:
       (# exit 7 #);
     Alert:
       (# exit 8 #);
     Window:
       (# exit 9 #);
     Client:
       (# exit 10 #);
     MenuPopup:
       (# exit 11 #);
     MenuItem:
       (# exit 12 #);
     ToolTip:
       (# exit 13 #);
     Application:
       (# exit 14 #);
     Document:
       (# exit 15 #);
     Pane:
       (# exit 16 #);
     Chart:
       (# exit 17 #);
     Dialog:
       (# exit 18 #);
     Border:
       (# exit 19 #);
     Grouping:
       (# exit 20 #);
     Separator:
       (# exit 21 #);
     ToolBar:
       (# exit 22 #);
     StatusBar:
       (# exit 23 #);
     Table:
       (# exit 24 #);
     ColumnHeader:
       (# exit 25 #);
     RowHeader:
       (# exit 26 #);
     Column:
       (# exit 27 #);
     Row:
       (# exit 28 #);
     Cell:
       (# exit 29 #);
     Link:
       (# exit 30 #);
     HelpBalloon:
       (# exit 31 #);
     Character:
       (# exit 32 #);
     List:
       (# exit 33 #);
     ListItem:
       (# exit 34 #);
     Outline:
       (# exit 35 #);
     OutlineItem:
       (# exit 36 #);
     PageTab:
       (# exit 37 #);
     PropertyPage:
       (# exit 38 #);
     Indicator:
       (# exit 39 #);
     Graphic:
       (# exit 40 #);
     StaticText:
       (# exit 41 #);
     Text:
       (# exit 42 #);
     PushButton:
       (# exit 43 #);
     CheckButton:
       (# exit 44 #);
     RadioButton:
       (# exit 45 #);
     ComboBox:
       (# exit 46 #);
     DropList:
       (# exit 47 #);
     ProgressBar:
       (# exit 48 #);
     Dial:
       (# exit 49 #);
     HotkeyField:
       (# exit 50 #);
     Slider:
       (# exit 51 #);
     SpinButton:
       (# exit 52 #);
     Diagram:
       (# exit 53 #);
     Animation:
       (# exit 54 #);
     Equation:
       (# exit 55 #);
     ButtonDropDown:
       (# exit 56 #);
     ButtonMenu:
       (# exit 57 #);
     ButtonDropDownGrid:
       (# exit 58 #);
     WhiteSpace:
       (# exit 59 #);
     PageTabList:
       (# exit 60 #);
     Clock:
       (# exit 61 #);
     Default:
       (# exit -1 #);
  do 'valuetype [System.Windows.Forms]System.Windows.Forms.AccessibleRole' -> className;
  INNER;
  #);

(* Utility function to ease casting to fully specified class. *)
asAccessibleRole:
  (# _A: ^_AccessibleRole;
     A: ^AccessibleRole;
  enter _A[]
  do _A[]->A[];
  exit A[]
  #);
