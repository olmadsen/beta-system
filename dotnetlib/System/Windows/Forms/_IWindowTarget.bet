ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IWindowTarget class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IWindowTarget.bet for members.
 *)
_IWindowTarget: ExternalClass
  (#
  do '[system.windows.forms]System.Windows.Forms.IWindowTarget' -> className;
  INNER;
  #);

