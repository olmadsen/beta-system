ORIGIN '_DrawItemState';
ON 19 (* Disable warnings about runtime qualification tests *);
INCLUDE '~beta/dotnetlib/System/_Enum';
--LIB: attributes--

(* .NET DrawItemState class. *)
DrawItemState: _DrawItemState
  (#
     (* Enum Literals *)

     DotnetNone:
       (# exit 0 #);
     Selected:
       (# exit 1 #);
     Grayed:
       (# exit 2 #);
     Disabled:
       (# exit 4 #);
     Checked:
       (# exit 8 #);
     Focus:
       (# exit 16 #);
     Default:
       (# exit 32 #);
     HotLight:
       (# exit 64 #);
     Inactive:
       (# exit 128 #);
     NoAccelerator:
       (# exit 256 #);
     NoFocusRect:
       (# exit 512 #);
     ComboBoxEdit:
       (# exit 4096 #);
  do 'valuetype [System.Windows.Forms]System.Windows.Forms.DrawItemState' -> className;
  INNER;
  #);

(* Utility function to ease casting to fully specified class. *)
asDrawItemState:
  (# _D: ^_DrawItemState;
     D: ^DrawItemState;
  enter _D[]
  do _D[]->D[];
  exit D[]
  #);
