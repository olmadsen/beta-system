ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/Enum';
--LIB: attributes--

(* Dotnet AccessibleSelection class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AccessibleSelection.bet for members.
 *)
_AccessibleSelection: Enum
  (#
  do '[system.windows.forms]System/Windows/Forms.AccessibleSelection' -> className;
  INNER;
  #);

