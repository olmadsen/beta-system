ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Binding class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Binding.bet for members.
 *)
_Binding: ExternalClass
  (#
  do '[system.windows.forms]System/Windows/Forms.Binding' -> className;
  INNER;
  #);

