ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IWin32Window class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IWin32Window.bet for members.
 *)
_IWin32Window: ExternalClass
  (#
  do '[system.windows.forms]System.Windows.Forms.IWin32Window' -> className;
  INNER;
  #);

