ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet MessageBox class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See MessageBox.bet for members.
 *)
_MessageBox: ExternalClass
  (#
  do '[System.Windows.Forms]System.Windows.Forms.MessageBox' -> className;
  INNER;
  #);

