ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Menu+MenuItemCollection class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Menu+MenuItemCollection.bet for members.
 *)
_Menu+MenuItemCollection: ExternalClass
  (#
  do '[system.windows.forms]System/Windows/Forms.Menu+MenuItemCollection' -> className;
  INNER;
  #);

