ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IMessageFilter class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IMessageFilter.bet for members.
 *)
_IMessageFilter: ExternalClass
  (#
  do '[system.windows.forms]System.Windows.Forms.IMessageFilter' -> className;
  INNER;
  #);

