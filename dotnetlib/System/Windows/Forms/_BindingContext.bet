ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet BindingContext class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See BindingContext.bet for members.
 *)
_BindingContext: ExternalClass
  (#
  do '[system.windows.forms]System/Windows/Forms.BindingContext' -> className;
  INNER;
  #);

