ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet ScrollableControl+DockPaddingEdges class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ScrollableControl+DockPaddingEdges.bet for members.
 *)
_ScrollableControl+DockPaddingEdges: ExternalClass
  (#
  do '[system.windows.forms]System.Windows.Forms.ScrollableControl+DockPaddingEdges' -> className;
  INNER;
  #);

