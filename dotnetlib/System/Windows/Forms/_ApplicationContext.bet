ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet ApplicationContext class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ApplicationContext.bet for members.
 *)
_ApplicationContext: ExternalClass
  (#
  do '[system.windows.forms]System.Windows.Forms.ApplicationContext' -> className;
  INNER;
  #);

