ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IDataObject class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IDataObject.bet for members.
 *)
_IDataObject: ExternalClass
  (#
  do '[System.Windows.Forms]System.Windows.Forms.IDataObject' -> className;
  INNER;
  #);

