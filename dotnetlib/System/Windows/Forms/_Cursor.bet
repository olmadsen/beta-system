ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Cursor class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Cursor.bet for members.
 *)
_Cursor: ExternalClass (* , IDisposable, ISerializable *)
  (#
  do '[System.Windows.Forms]System.Windows.Forms.Cursor' -> className;
  INNER;
  #);

