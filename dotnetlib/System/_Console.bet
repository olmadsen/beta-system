ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Console class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Console.bet for members.
 *)
_Console: ExternalClass
  (#
  do '[mscorlib]System.Console' -> className;
  INNER;
  #);

