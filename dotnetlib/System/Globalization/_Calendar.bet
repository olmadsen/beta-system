ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Calendar class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Calendar.bet for members.
 *)
_Calendar: ExternalClass
  (#
  do '[mscorlib]System.Globalization.Calendar' -> className;
  INNER;
  #);

