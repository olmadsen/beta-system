ORIGIN '_CalendarWeekRule';
ON 19 (* Disable warnings about runtime qualification tests *);
INCLUDE '~beta/dotnetlib/System/_Enum';
--LIB: attributes--

(* .NET CalendarWeekRule class. *)
CalendarWeekRule: _CalendarWeekRule
  (#
     (* Enum Literals *)

     FirstDay:
       (# exit 0 #);
     FirstFullWeek:
       (# exit 1 #);
     FirstFourDayWeek:
       (# exit 2 #);
  do '@valuetype [mscorlib]System.Globalization.CalendarWeekRule' -> className;
  INNER;
  #);

(* Utility function to ease casting to fully specified class. *)
asCalendarWeekRule:
  (# _C: ^_CalendarWeekRule;
     C: ^CalendarWeekRule;
  enter _C[]
  do _C[]->C[];
  exit C[]
  #);
