ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet TextInfo class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See TextInfo.bet for members.
 *)
_TextInfo: ExternalClass
  (#
  do '[mscorlib]System.Globalization.TextInfo' -> className;
  INNER;
  #);

