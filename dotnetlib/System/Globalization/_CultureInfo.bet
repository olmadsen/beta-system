ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet CultureInfo class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See CultureInfo.bet for members.
 *)
_CultureInfo: ExternalClass
  (#
  do '[mscorlib]System/Globalization.CultureInfo' -> className;
  INNER;
  #);

