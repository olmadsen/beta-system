ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet NumberFormatInfo class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See NumberFormatInfo.bet for members.
 *)
_NumberFormatInfo: ExternalClass
  (#
  do '[mscorlib]System/Globalization.NumberFormatInfo' -> className;
  INNER;
  #);

