ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet SortKey class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See SortKey.bet for members.
 *)
_SortKey: ExternalClass
  (#
  do '[mscorlib]System.Globalization.SortKey' -> className;
  INNER;
  #);

