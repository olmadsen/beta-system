ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/Enum';
--LIB: attributes--

(* Dotnet AssemblyVersionCompatibility class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AssemblyVersionCompatibility.bet for members.
 *)
_AssemblyVersionCompatibility: Enum
  (#
  do '[mscorlib]System.Configuration.Assemblies.AssemblyVersionCompatibility' -> className;
  INNER;
  #);

