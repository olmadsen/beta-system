ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet ValueType class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ValueType.bet for members.
 *)
_ValueType: ExternalClass
  (#
  do '[mscorlib]System.ValueType' -> className;
  INNER;
  #);

