ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Evidence class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Evidence.bet for members.
 *)
_Evidence: ExternalClass (* , ICollection, IEnumerable *)
  (#
  do '[mscorlib]System.Security.Policy.Evidence' -> className;
  INNER;
  #);

