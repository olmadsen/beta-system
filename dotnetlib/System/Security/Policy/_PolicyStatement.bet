ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet PolicyStatement class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See PolicyStatement.bet for members.
 *)
_PolicyStatement: ExternalClass
  (#
  do '[mscorlib]System.Security.Policy.PolicyStatement' -> className;
  INNER;
  #);

