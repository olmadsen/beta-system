ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet StrongName class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See StrongName.bet for members.
 *)
_StrongName: ExternalClass
  (#
  do '[mscorlib]System.Security.Policy.StrongName' -> className;
  INNER;
  #);

