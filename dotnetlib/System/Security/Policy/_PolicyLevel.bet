ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet PolicyLevel class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See PolicyLevel.bet for members.
 *)
_PolicyLevel: ExternalClass
  (#
  do '[mscorlib]System.Security.Policy.PolicyLevel' -> className;
  INNER;
  #);

