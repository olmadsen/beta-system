ORIGIN '_PolicyStatementAttribute';
ON 19 (* Disable warnings about runtime qualification tests *);
INCLUDE '~beta/dotnetlib/System/_Enum';
--LIB: attributes--

(* .NET PolicyStatementAttribute class. *)
PolicyStatementAttribute: _PolicyStatementAttribute
  (#
     (* Public/family CLS compliant fields *)

     value__: @int32;

     (* Enum Literals *)

     Nothing:
       (# exit 0 #);
     Exclusive:
       (# exit 1 #);
     LevelFinal:
       (# exit 2 #);
     All:
       (# exit 3 #);
  do 'valuetype [mscorlib]System.Security.Policy.PolicyStatementAttribute' -> className;
  INNER;
  #);

(* Utility function to ease casting to fully specified class. *)
asPolicyStatementAttribute:
  (# _P: ^_PolicyStatementAttribute;
     P: ^PolicyStatementAttribute;
  enter _P[]
  do _P[]->P[];
  exit P[]
  #);
