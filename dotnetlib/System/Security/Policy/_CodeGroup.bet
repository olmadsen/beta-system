ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet CodeGroup class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See CodeGroup.bet for members.
 *)
_CodeGroup: ExternalClass
  (#
  do '[mscorlib]System.Security.Policy.CodeGroup' -> className;
  INNER;
  #);

