ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet PermissionSet class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See PermissionSet.bet for members.
 *)
_PermissionSet: ExternalClass
  (#
  do '[mscorlib]System.Security.PermissionSet' -> className;
  INNER;
  #);

