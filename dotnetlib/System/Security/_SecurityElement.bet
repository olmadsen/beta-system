ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet SecurityElement class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See SecurityElement.bet for members.
 *)
_SecurityElement: ExternalClass
  (#
  do '[mscorlib]System.Security.SecurityElement' -> className;
  INNER;
  #);

