ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IIdentity class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IIdentity.bet for members.
 *)
_IIdentity: ExternalClass
  (#
  do '[mscorlib]System.Security.Principal.IIdentity' -> className;
  INNER;
  #);

