ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IPrincipal class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IPrincipal.bet for members.
 *)
_IPrincipal: ExternalClass
  (#
  do '[mscorlib]System.Security.Principal.IPrincipal' -> className;
  INNER;
  #);

