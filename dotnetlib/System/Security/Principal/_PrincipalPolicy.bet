ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/Enum';
--LIB: attributes--

(* Dotnet PrincipalPolicy class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See PrincipalPolicy.bet for members.
 *)
_PrincipalPolicy: Enum
  (#
  do 'valuetype [mscorlib]System.Security.Principal.PrincipalPolicy' -> className;
  INNER;
  #);

