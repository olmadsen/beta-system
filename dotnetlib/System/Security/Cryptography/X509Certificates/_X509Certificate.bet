ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet X509Certificate class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See X509Certificate.bet for members.
 *)
_X509Certificate: ExternalClass
  (#
  do '[mscorlib]System.Security.Cryptography.X509Certificates.X509Certificate' -> className;
  INNER;
  #);

