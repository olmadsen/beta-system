ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet IPermission class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See IPermission.bet for members.
 *)
_IPermission: ExternalClass (* , ISecurityEncodable *)
  (#
  do '[mscorlib]System.Security.IPermission' -> className;
  INNER;
  #);

