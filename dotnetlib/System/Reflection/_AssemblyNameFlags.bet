ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/Enum';
--LIB: attributes--

(* Dotnet AssemblyNameFlags class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AssemblyNameFlags.bet for members.
 *)
_AssemblyNameFlags: Enum
  (#
  do '[mscorlib]System.Reflection.AssemblyNameFlags' -> className;
  INNER;
  #);

