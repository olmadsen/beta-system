ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet ManifestResourceInfo class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ManifestResourceInfo.bet for members.
 *)
_ManifestResourceInfo: ExternalClass
  (#
  do '[mscorlib]System/Reflection.ManifestResourceInfo' -> className;
  INNER;
  #);

