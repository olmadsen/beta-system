ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet AssemblyName class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See AssemblyName.bet for members.
 *)
_AssemblyName: ExternalClass (* , ICloneable, ISerializable, IDeserializationCallback *)
  (#
  do '[mscorlib]System.Reflection.AssemblyName' -> className;
  INNER;
  #);

