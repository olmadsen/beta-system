ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Module class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Module.bet for members.
 *)
_Module: ExternalClass
  (#
  do '[mscorlib]System.Reflection.Module' -> className;
  INNER;
  #);

