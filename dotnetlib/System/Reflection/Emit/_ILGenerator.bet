ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet ILGenerator class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ILGenerator.bet for members.
 *)
_ILGenerator: ExternalClass
  (#
  do '[mscorlib]System.Reflection.Emit.ILGenerator' -> className;
  INNER;
  #);

