ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet EventBuilder class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See EventBuilder.bet for members.
 *)
_EventBuilder: ExternalClass
  (#
  do '[mscorlib]System.Reflection.Emit.EventBuilder' -> className;
  INNER;
  #);

