ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet SignatureHelper class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See SignatureHelper.bet for members.
 *)
_SignatureHelper: ExternalClass
  (#
  do '[mscorlib]System.Reflection.Emit.SignatureHelper' -> className;
  INNER;
  #);

