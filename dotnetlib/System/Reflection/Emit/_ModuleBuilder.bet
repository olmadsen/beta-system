ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/Reflection/Module';
--LIB: attributes--

(* Dotnet ModuleBuilder class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ModuleBuilder.bet for members.
 *)
_ModuleBuilder: Module (* , ISerializable, ICustomAttributeProvider *)
  (#
  do '[mscorlib]System.Reflection.Emit.ModuleBuilder' -> className;
  INNER;
  #);

