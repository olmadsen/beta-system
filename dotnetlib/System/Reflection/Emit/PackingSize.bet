ORIGIN '_PackingSize';
ON 19 (* Disable warnings about runtime qualification tests *);
INCLUDE '~beta/dotnetlib/System/_Enum';
--LIB: attributes--

(* .NET PackingSize class. *)
PackingSize: _PackingSize
  (#
     (* Enum Literals *)

     Unspecified:
       (# exit 0 #);
     Size1:
       (# exit 1 #);
     Size2:
       (# exit 2 #);
     Size4:
       (# exit 4 #);
     Size8:
       (# exit 8 #);
     Size16:
       (# exit 16 #);
  do 'valuetype [mscorlib]System.Reflection.Emit.PackingSize' -> className;
  INNER;
  #);

(* Utility function to ease casting to fully specified class. *)
asPackingSize:
  (# _P: ^_PackingSize;
     P: ^PackingSize;
  enter _P[]
  do _P[]->P[];
  exit P[]
  #);
