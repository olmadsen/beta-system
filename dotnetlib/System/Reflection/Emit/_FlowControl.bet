ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/Enum';
--LIB: attributes--

(* Dotnet FlowControl class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FlowControl.bet for members.
 *)
_FlowControl: Enum
  (#
  do 'valuetype [mscorlib]System.Reflection.Emit.FlowControl' -> className;
  INNER;
  #);

