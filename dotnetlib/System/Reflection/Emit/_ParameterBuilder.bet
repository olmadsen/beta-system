ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet ParameterBuilder class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ParameterBuilder.bet for members.
 *)
_ParameterBuilder: ExternalClass
  (#
  do '[mscorlib]System.Reflection.Emit.ParameterBuilder' -> className;
  INNER;
  #);

