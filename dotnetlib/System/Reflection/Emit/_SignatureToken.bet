ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/ValueType';
--LIB: attributes--

(* Dotnet SignatureToken class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See SignatureToken.bet for members.
 *)
_SignatureToken: ValueType
  (#
  do 'valuetype [mscorlib]System.Reflection.Emit.SignatureToken' -> className;
  INNER;
  #);

