ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet StrongNameKeyPair class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See StrongNameKeyPair.bet for members.
 *)
_StrongNameKeyPair: ExternalClass
  (#
  do '[mscorlib]System/Reflection.StrongNameKeyPair' -> className;
  INNER;
  #);

