ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Binder class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Binder.bet for members.
 *)
_Binder: ExternalClass
  (#
  do '[mscorlib]System/Reflection.Binder' -> className;
  INNER;
  #);

