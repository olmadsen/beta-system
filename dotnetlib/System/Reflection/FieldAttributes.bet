ORIGIN '_FieldAttributes';
ON 19 (* Disable warnings about runtime qualification tests *);
INCLUDE '~beta/dotnetlib/System/_Enum';
--LIB: attributes--

(* .NET FieldAttributes class. *)
FieldAttributes: _FieldAttributes
  (#
     (* Enum Literals *)

     PrivateScope:
       (# exit 0 #);
     Private:
       (# exit 1 #);
     FamANDAssem:
       (# exit 2 #);
     Assembly:
       (# exit 3 #);
     Family:
       (# exit 4 #);
     FamORAssem:
       (# exit 5 #);
     Public:
       (# exit 6 #);
     FieldAccessMask:
       (# exit 7 #);
     Static:
       (# exit 16 #);
     InitOnly:
       (# exit 32 #);
     Literal:
       (# exit 64 #);
     NotSerialized:
       (# exit 128 #);
     HasFieldRVA:
       (# exit 256 #);
     SpecialName:
       (# exit 512 #);
     RTSpecialName:
       (# exit 1024 #);
     HasFieldMarshal:
       (# exit 4096 #);
     PinvokeImpl:
       (# exit 8192 #);
     HasDefault:
       (# exit 32768 #);
     ReservedMask:
       (# exit 38144 #);
  do 'valuetype [mscorlib]System.Reflection.FieldAttributes' -> className;
  INNER;
  #);

