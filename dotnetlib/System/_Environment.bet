ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet Environment class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See Environment.bet for members.
 *)
_Environment: ExternalClass
  (#
  do '[mscorlib]System.Environment' -> className;
  INNER;
  #);

