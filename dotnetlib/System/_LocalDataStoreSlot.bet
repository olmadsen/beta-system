ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet LocalDataStoreSlot class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See LocalDataStoreSlot.bet for members.
 *)
_LocalDataStoreSlot: ExternalClass
  (#
  do '[mscorlib]System.LocalDataStoreSlot' -> className;
  INNER;
  #);

