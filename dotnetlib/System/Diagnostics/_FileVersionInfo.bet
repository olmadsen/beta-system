ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet FileVersionInfo class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See FileVersionInfo.bet for members.
 *)
_FileVersionInfo: ExternalClass
  (#
  do '[System]System.Diagnostics.FileVersionInfo' -> className;
  INNER;
  #);

