ORIGIN '~beta/basiclib/betaenv';
--LIB: attributes--

(* Dotnet ISymbolWriter class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See ISymbolWriter.bet for members.
 *)
_ISymbolWriter: ExternalClass
  (#
  do '[mscorlib]System.Diagnostics.SymbolStore.ISymbolWriter' -> className;
  INNER;
  #);

