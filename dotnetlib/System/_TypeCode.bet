ORIGIN '~beta/basiclib/betaenv';
INCLUDE '~beta/dotnetlib/System/_Enum' (* Cannot use non-wrapper *);
--LIB: attributes--

(* Dotnet TypeCode class declaration.
 * This wrapper is needed to prevent circular fragment INCLUDE.
 * See TypeCode.bet for members.
 *)
_TypeCode: _Enum
  (#
  do '[mscorlib]System.TypeCode' -> className;
  INNER;
  #);

