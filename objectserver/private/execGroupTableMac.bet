ORIGIN 'execGroupTableBody';
LIB_ITEM 'execgrouptable';

--- segtePTtoInx:dopart ---
do (if len > 0 then
       0 -> low;
       len-1 -> high;
       loop:
		(for i: len repeat
			(if ((%getLongAt(ptListAdr+4*(i - 1)))) = ptAdr then
			(* Above line tos_converted from: (if (ptListAdr + 4*(i - 1) -> tos'%adrGetLong') = ptAdr then *)
				(i - 1) -> inx;
				leave loop;
			if);
		for);
    else
       -1 -> inx;
   if)
