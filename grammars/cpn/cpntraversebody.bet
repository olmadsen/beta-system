ORIGIN 'cpntraverse';

--LIB: attributes--

GrammarPath: (# exit '~beta/grammars/cpn/cpn' #);
BobsTab:     (# exit '~beta/grammars/cpn/cpn-parser.btab' #);

-- do_cpn_body: DoPart --
do c.getcpn_stagOpt -> do_cpn_stagOpt;
   c.getcpn_content -> do_cpn_content;
   c.getcpn_etagOpt -> do_cpn_etagOpt;

-- do_cpn_stagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_cpn_stag;
   if);

-- do_cpn_stag_body: DoPart --
do 

-- do_cpn_content_body: DoPart --
do c.newscan
   (#
   do current[] -> do_cpn_content_gen;
   #);

-- do_cpn_content_gen_body: DoPart --
do (if c.symbol
    // cpn.page then
       c[] -> do_page;
    // cpn.hierpage then
       c[] -> do_hierpage;
    // cpn.generator then
       c[] -> do_generator;
    if);

-- do_cpn_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_cpn_etag;
   if);

-- do_cpn_etag_body: DoPart --
do 

-- do_text_body: DoPart --
do t.getstring -> do_string;

-- do_page_body: DoPart --
do p.getatt_idOpt -> do_att_idOpt;
   p.getpage_content -> do_page_content;

-- do_page_content_body: DoPart --
do p.newscan
   (#
   do current[] -> do_page_content_gen;
   #);

-- do_page_content_gen_body: DoPart --
do (if p.symbol
    // cpn.page_text then
       p[] -> do_page_text;
       (* sub-alternation <pageobs>: *)
    // cpn.place then
       p[] -> do_place;
    // cpn.trans then
       p[] -> do_trans;
    // cpn.tempdecl then
       p[] -> do_tempdecl;
    // cpn.globdecl then
       p[] -> do_globdecl;
    // cpn.locdecl then
       p[] -> do_locdecl;
    // cpn.arc then
       p[] -> do_arc;
    // cpn.aux then
       p[] -> do_aux;
    // cpn.aux_conn then
       p[] -> do_aux_conn;
       (* End sub-alternation <pageobs> *)
    // cpn.page_shape then
       p[] -> do_page_shape;
    // cpn.pageattr then
       p[] -> do_pageattr;
    // cpn.fillattr then
       p[] -> do_fillattr;
    // cpn.lineattr then
       p[] -> do_lineattr;
    // cpn.posattr then
       p[] -> do_posattr;
    // cpn.winattr then
       p[] -> do_winattr;
    // cpn.modeattr then
       p[] -> do_modeattr;
    // cpn.mult then
       p[] -> do_mult;
    if);

-- do_page_text_body: DoPart --
do p.gettext -> do_text;

-- do_page_shape_body: DoPart --
do p.getshape -> do_shape;

-- do_att_idOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_att_id;
   if);

-- do_place_body: DoPart --
do p.getatt_idOpt -> do_att_idOpt;
   p.getplace_content -> do_place_content;

-- do_place_content_body: DoPart --
do p.newscan
   (#
   do current[] -> do_place_content_gen;
   #);

-- do_place_content_gen_body: DoPart --
do (if p.symbol
    // cpn.place_text then
       p[] -> do_place_text;
       (* sub-alternation <placeob>: *)
    // cpn.placeob_name then
       p[] -> do_placeob_name;
    // cpn.placeob_type then
       p[] -> do_placeob_type;
    // cpn.placeob_initmark then
       p[] -> do_placeob_initmark;
    // cpn.placeob_aux_endp then
       p[] -> do_placeob_aux_endp;
    // cpn.placeob_aux_std then
       p[] -> do_placeob_aux_std;
    // cpn.placeob_fus_key then
       p[] -> do_placeob_fus_key;
    // cpn.placeob_portkreg then
       p[] -> do_placeob_portkreg;
    // cpn.placeob_mark_key then
       p[] -> do_placeob_mark_key;
       (* End sub-alternation <placeob> *)
    // cpn.place_shape then
       p[] -> do_place_shape;
    // cpn.place_objatts then
       p[] -> do_place_objatts;
    // cpn.place_port then
       p[] -> do_place_port;
    if);

-- do_place_text_body: DoPart --
do p.gettext -> do_text;

-- do_place_shape_body: DoPart --
do p.getshape -> do_shape;

-- do_place_objatts_body: DoPart --
do p.getobjatts -> do_objatts;

-- do_place_port_body: DoPart --
do p.getport -> do_port;

-- do_name_body: DoPart --
do n.getatt_idOpt -> do_att_idOpt;
   n.getname_content -> do_name_content;

-- do_name_content_body: DoPart --
do n.newscan
   (#
   do current[] -> do_name_content_gen;
   #);

-- do_name_content_gen_body: DoPart --
do (if n.symbol
    // cpn.name_text then
       n[] -> do_name_text;
    // cpn.name_aux_std then
       n[] -> do_name_aux_std;
    // cpn.name_shape then
       n[] -> do_name_shape;
    // cpn.name_objatts then
       n[] -> do_name_objatts;
    // cpn.name_regattr then
       n[] -> do_name_regattr;
    if);

-- do_name_text_body: DoPart --
do n.gettext -> do_text;

-- do_name_shape_body: DoPart --
do n.getshape -> do_shape;

-- do_name_aux_std_body: DoPart --
do n.getaux_std -> do_aux_std;

-- do_name_objatts_body: DoPart --
do n.getobjatts -> do_objatts;

-- do_name_regattr_body: DoPart --
do n.getregattr -> do_regattr;

-- do_type_body: DoPart --
do t.getatt_idOpt -> do_att_idOpt;
   t.gettype_content -> do_type_content;

-- do_type_content_body: DoPart --
do t.newscan
   (#
   do current[] -> do_type_content_gen;
   #);

-- do_type_content_gen_body: DoPart --
do (if t.symbol
    // cpn.type_text then
       t[] -> do_type_text;
    // cpn.type_aux_std then
       t[] -> do_type_aux_std;
    // cpn.type_shape then
       t[] -> do_type_shape;
    // cpn.type_objatts then
       t[] -> do_type_objatts;
    // cpn.type_regattr then
       t[] -> do_type_regattr;
    if);

-- do_type_text_body: DoPart --
do t.gettext -> do_text;

-- do_type_shape_body: DoPart --
do t.getshape -> do_shape;

-- do_type_aux_std_body: DoPart --
do t.getaux_std -> do_aux_std;

-- do_type_objatts_body: DoPart --
do t.getobjatts -> do_objatts;

-- do_type_regattr_body: DoPart --
do t.getregattr -> do_regattr;

-- do_initmark_body: DoPart --
do i.getatt_idOpt -> do_att_idOpt;
   i.getinitmark_content -> do_initmark_content;

-- do_initmark_content_body: DoPart --
do i.newscan
   (#
   do current[] -> do_initmark_content_gen;
   #);

-- do_initmark_content_gen_body: DoPart --
do (if i.symbol
    // cpn.initmark_text then
       i[] -> do_initmark_text;
    // cpn.initmark_aux_std then
       i[] -> do_initmark_aux_std;
    // cpn.initmark_shape then
       i[] -> do_initmark_shape;
    // cpn.initmark_objatts then
       i[] -> do_initmark_objatts;
    // cpn.initmark_regattr then
       i[] -> do_initmark_regattr;
    if);

-- do_initmark_text_body: DoPart --
do i.gettext -> do_text;

-- do_initmark_shape_body: DoPart --
do i.getshape -> do_shape;

-- do_initmark_aux_std_body: DoPart --
do i.getaux_std -> do_aux_std;

-- do_initmark_objatts_body: DoPart --
do i.getobjatts -> do_objatts;

-- do_initmark_regattr_body: DoPart --
do i.getregattr -> do_regattr;

-- do_trans_body: DoPart --
do t.getatt_idOpt -> do_att_idOpt;
   t.gettrans_content -> do_trans_content;

-- do_trans_content_body: DoPart --
do t.newscan
   (#
   do current[] -> do_trans_content_gen;
   #);

-- do_trans_content_gen_body: DoPart --
do (if t.symbol
    // cpn.trans_text then
       t[] -> do_trans_text;
       (* sub-alternation <transob>: *)
    // cpn.transob_name then
       t[] -> do_transob_name;
    // cpn.transob_cond then
       t[] -> do_transob_cond;
    // cpn.transob_code_key then
       t[] -> do_transob_code_key;
    // cpn.transob_log_key then
       t[] -> do_transob_log_key;
    // cpn.transob_time then
       t[] -> do_transob_time;
    // cpn.transob_aux_endp then
       t[] -> do_transob_aux_endp;
    // cpn.transob_hier_key then
       t[] -> do_transob_hier_key;
    // cpn.transob_aux_enab then
       t[] -> do_transob_aux_enab;
       (* End sub-alternation <transob> *)
    // cpn.trans_shape then
       t[] -> do_trans_shape;
    // cpn.trans_objatts then
       t[] -> do_trans_objatts;
    // cpn.trans_modeattr then
       t[] -> do_trans_modeattr;
    // cpn.trans_subst then
       t[] -> do_trans_subst;
    if);

-- do_trans_text_body: DoPart --
do t.gettext -> do_text;

-- do_trans_shape_body: DoPart --
do t.getshape -> do_shape;

-- do_trans_objatts_body: DoPart --
do t.getobjatts -> do_objatts;

-- do_trans_modeattr_body: DoPart --
do t.getmodeattr -> do_modeattr;

-- do_trans_subst_body: DoPart --
do t.getsubst -> do_subst;

-- do_cond_body: DoPart --
do c.getatt_idOpt -> do_att_idOpt;
   c.getcond_content -> do_cond_content;

-- do_cond_content_body: DoPart --
do c.newscan
   (#
   do current[] -> do_cond_content_gen;
   #);

-- do_cond_content_gen_body: DoPart --
do (if c.symbol
    // cpn.cond_text then
       c[] -> do_cond_text;
    // cpn.cond_aux_std then
       c[] -> do_cond_aux_std;
    // cpn.cond_shape then
       c[] -> do_cond_shape;
    // cpn.cond_objatts then
       c[] -> do_cond_objatts;
    // cpn.cond_regattr then
       c[] -> do_cond_regattr;
    if);

-- do_cond_text_body: DoPart --
do c.gettext -> do_text;

-- do_cond_aux_std_body: DoPart --
do c.getaux_std -> do_aux_std;

-- do_cond_shape_body: DoPart --
do c.getshape -> do_shape;

-- do_cond_objatts_body: DoPart --
do c.getobjatts -> do_objatts;

-- do_cond_regattr_body: DoPart --
do c.getregattr -> do_regattr;

-- do_code_key_body: DoPart --
do c.getatt_idOpt -> do_att_idOpt;
   c.getcode_key_content -> do_code_key_content;

-- do_code_key_content_body: DoPart --
do c.newscan
   (#
   do current[] -> do_code_key_content_gen;
   #);

-- do_code_key_content_gen_body: DoPart --
do (if c.symbol
    // cpn.code_key_text then
       c[] -> do_code_key_text;
    // cpn.code_key_aux_std then
       c[] -> do_code_key_aux_std;
    // cpn.code_key_code then
       c[] -> do_code_key_code;
    // cpn.code_key_shape then
       c[] -> do_code_key_shape;
    // cpn.code_key_objatts then
       c[] -> do_code_key_objatts;
    // cpn.code_key_regattr then
       c[] -> do_code_key_regattr;
    if);

-- do_code_key_text_body: DoPart --
do c.gettext -> do_text;

-- do_code_key_aux_std_body: DoPart --
do c.getaux_std -> do_aux_std;

-- do_code_key_code_body: DoPart --
do c.getcpn_code -> do_cpn_code;

-- do_code_key_shape_body: DoPart --
do c.getshape -> do_shape;

-- do_code_key_objatts_body: DoPart --
do c.getobjatts -> do_objatts;

-- do_code_key_regattr_body: DoPart --
do c.getregattr -> do_regattr;

-- do_cpn_code_body: DoPart --
do c.getatt_idOpt -> do_att_idOpt;
   c.getcode_content -> do_code_content;

-- do_code_content_body: DoPart --
do c.newscan
   (#
   do current[] -> do_code_content_gen;
   #);

-- do_code_content_gen_body: DoPart --
do (if c.symbol
    // cpn.code_text then
       c[] -> do_code_text;
    // cpn.code_aux_std then
       c[] -> do_code_aux_std;
    // cpn.code_shape then
       c[] -> do_code_shape;
    // cpn.code_objatts then
       c[] -> do_code_objatts;
    // cpn.code_regattr then
       c[] -> do_code_regattr;
    if);

-- do_code_text_body: DoPart --
do c.gettext -> do_text;

-- do_code_shape_body: DoPart --
do c.getshape -> do_shape;

-- do_code_aux_std_body: DoPart --
do c.getaux_std -> do_aux_std;

-- do_code_objatts_body: DoPart --
do c.getobjatts -> do_objatts;

-- do_code_regattr_body: DoPart --
do c.getregattr -> do_regattr;

-- do_log_key_body: DoPart --
do l.getatt_idOpt -> do_att_idOpt;
   l.getlog_key_content -> do_log_key_content;

-- do_log_key_content_body: DoPart --
do l.newscan
   (#
   do current[] -> do_log_key_content_gen;
   #);

-- do_log_key_content_gen_body: DoPart --
do (if l.symbol
    // cpn.log_key_text then
       l[] -> do_log_key_text;
    // cpn.log_key_aux_std then
       l[] -> do_log_key_aux_std;
    // cpn.log_key_log then
       l[] -> do_log_key_log;
    // cpn.log_key_shape then
       l[] -> do_log_key_shape;
    // cpn.log_key_objatts then
       l[] -> do_log_key_objatts;
    // cpn.log_key_regattr then
       l[] -> do_log_key_regattr;
    if);

-- do_log_key_text_body: DoPart --
do l.gettext -> do_text;

-- do_log_key_shape_body: DoPart --
do l.getshape -> do_shape;

-- do_log_key_aux_std_body: DoPart --
do l.getaux_std -> do_aux_std;

-- do_log_key_log_body: DoPart --
do l.getlog -> do_log;

-- do_log_key_objatts_body: DoPart --
do l.getobjatts -> do_objatts;

-- do_log_key_regattr_body: DoPart --
do l.getregattr -> do_regattr;

-- do_log_body: DoPart --
do l.getatt_idOpt -> do_att_idOpt;
   l.getlog_content -> do_log_content;

-- do_log_content_body: DoPart --
do l.newscan
   (#
   do current[] -> do_log_content_gen;
   #);

-- do_log_content_gen_body: DoPart --
do (if l.symbol
    // cpn.log_text then
       l[] -> do_log_text;
    // cpn.log_aux_std then
       l[] -> do_log_aux_std;
    // cpn.log_shape then
       l[] -> do_log_shape;
    // cpn.log_objatts then
       l[] -> do_log_objatts;
    // cpn.log_regattr then
       l[] -> do_log_regattr;
    if);

-- do_log_text_body: DoPart --
do l.gettext -> do_text;

-- do_log_shape_body: DoPart --
do l.getshape -> do_shape;

-- do_log_aux_std_body: DoPart --
do l.getaux_std -> do_aux_std;

-- do_log_objatts_body: DoPart --
do l.getobjatts -> do_objatts;

-- do_log_regattr_body: DoPart --
do l.getregattr -> do_regattr;

-- do_time_body: DoPart --
do t.getatt_idOpt -> do_att_idOpt;
   t.gettime_content -> do_time_content;

-- do_time_content_body: DoPart --
do t.newscan
   (#
   do current[] -> do_time_content_gen;
   #);

-- do_time_content_gen_body: DoPart --
do (if t.symbol
    // cpn.time_text then
       t[] -> do_time_text;
    // cpn.time_aux_std then
       t[] -> do_time_aux_std;
    // cpn.time_shape then
       t[] -> do_time_shape;
    // cpn.time_objatts then
       t[] -> do_time_objatts;
    // cpn.time_regattr then
       t[] -> do_time_regattr;
    if);

-- do_time_text_body: DoPart --
do t.gettext -> do_text;

-- do_time_shape_body: DoPart --
do t.getshape -> do_shape;

-- do_time_aux_std_body: DoPart --
do t.getaux_std -> do_aux_std;

-- do_time_objatts_body: DoPart --
do t.getobjatts -> do_objatts;

-- do_time_regattr_body: DoPart --
do t.getregattr -> do_regattr;

-- do_hier_key_body: DoPart --
do h.getatt_idOpt -> do_att_idOpt;
   h.gethier_key_content -> do_hier_key_content;

-- do_hier_key_content_body: DoPart --
do h.newscan
   (#
   do current[] -> do_hier_key_content_gen;
   #);

-- do_hier_key_content_gen_body: DoPart --
do (if h.symbol
    // cpn.hier_key_text then
       h[] -> do_hier_key_text;
    // cpn.hier_key_aux_std then
       h[] -> do_hier_key_aux_std;
    // cpn.hier_key_hierarch then
       h[] -> do_hier_key_hierarch;
    // cpn.hier_key_shape then
       h[] -> do_hier_key_shape;
    // cpn.hier_key_objatts then
       h[] -> do_hier_key_objatts;
    // cpn.hier_key_regattr then
       h[] -> do_hier_key_regattr;
    if);

-- do_hier_key_text_body: DoPart --
do h.gettext -> do_text;

-- do_hier_key_shape_body: DoPart --
do h.getshape -> do_shape;

-- do_hier_key_aux_std_body: DoPart --
do h.getaux_std -> do_aux_std;

-- do_hier_key_hierarch_body: DoPart --
do h.gethierarch -> do_hierarch;

-- do_hier_key_objatts_body: DoPart --
do h.getobjatts -> do_objatts;

-- do_hier_key_regattr_body: DoPart --
do h.getregattr -> do_regattr;

-- do_hierarch_body: DoPart --
do h.getatt_idOpt -> do_att_idOpt;
   h.gethierarch_content -> do_hierarch_content;

-- do_hierarch_content_body: DoPart --
do h.newscan
   (#
   do current[] -> do_hierarch_content_gen;
   #);

-- do_hierarch_content_gen_body: DoPart --
do (if h.symbol
    // cpn.hierarch_text then
       h[] -> do_hierarch_text;
    // cpn.hierarch_aux_std then
       h[] -> do_hierarch_aux_std;
    // cpn.hierarch_shape then
       h[] -> do_hierarch_shape;
    // cpn.hierarch_objatts then
       h[] -> do_hierarch_objatts;
    // cpn.hierarch_regattr then
       h[] -> do_hierarch_regattr;
    if);

-- do_hierarch_text_body: DoPart --
do h.gettext -> do_text;

-- do_hierarch_shape_body: DoPart --
do h.getshape -> do_shape;

-- do_hierarch_aux_std_body: DoPart --
do h.getaux_std -> do_aux_std;

-- do_hierarch_objatts_body: DoPart --
do h.getobjatts -> do_objatts;

-- do_hierarch_regattr_body: DoPart --
do h.getregattr -> do_regattr;

-- do_fus_key_body: DoPart --
do f.getatt_idOpt -> do_att_idOpt;
   f.getfus_key_content -> do_fus_key_content;

-- do_fus_key_content_body: DoPart --
do f.newscan
   (#
   do current[] -> do_fus_key_content_gen;
   #);

-- do_fus_key_content_gen_body: DoPart --
do (if f.symbol
    // cpn.fus_key_text then
       f[] -> do_fus_key_text;
    // cpn.fus_key_aux_std then
       f[] -> do_fus_key_aux_std;
    // cpn.fus_key_fusion then
       f[] -> do_fus_key_fusion;
    // cpn.fus_key_shape then
       f[] -> do_fus_key_shape;
    // cpn.fus_key_objatts then
       f[] -> do_fus_key_objatts;
    // cpn.fus_key_regattr then
       f[] -> do_fus_key_regattr;
    if);

-- do_fus_key_text_body: DoPart --
do f.gettext -> do_text;

-- do_fus_key_shape_body: DoPart --
do f.getshape -> do_shape;

-- do_fus_key_aux_std_body: DoPart --
do f.getaux_std -> do_aux_std;

-- do_fus_key_fusion_body: DoPart --
do f.getfusion -> do_fusion;

-- do_fus_key_objatts_body: DoPart --
do f.getobjatts -> do_objatts;

-- do_fus_key_regattr_body: DoPart --
do f.getregattr -> do_regattr;

-- do_fusion_body: DoPart --
do f.getatt_fusion_gen -> do_att_fusion_gen;
   f.getfusion_content -> do_fusion_content;

-- do_fusion_content_body: DoPart --
do f.newscan
   (#
   do current[] -> do_fusion_content_gen;
   #);

-- do_fusion_content_gen_body: DoPart --
do (if f.symbol
    // cpn.fusion_text then
       f[] -> do_fusion_text;
    // cpn.fusion_aux_std then
       f[] -> do_fusion_aux_std;
    // cpn.fusion_shape then
       f[] -> do_fusion_shape;
    // cpn.fusion_objatts then
       f[] -> do_fusion_objatts;
    // cpn.fusion_regattr then
       f[] -> do_fusion_regattr;
    if);

-- do_att_fusion_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_fusion;
   #);

-- do_att_fusion_body: DoPart --
do (if a.symbol
    // cpn.fusion_att_id then
       a[] -> do_fusion_att_id;
    // cpn.fusion_att_type then
       a[] -> do_fusion_att_type;
    if);

-- do_fusion_att_id_body: DoPart --
do f.getatt_id -> do_att_id;

-- do_fusion_att_type_body: DoPart --
do f.getfustyps -> do_fustyps;

-- do_fusion_text_body: DoPart --
do f.gettext -> do_text;

-- do_fusion_shape_body: DoPart --
do f.getshape -> do_shape;

-- do_fusion_aux_std_body: DoPart --
do f.getaux_std -> do_aux_std;

-- do_fusion_objatts_body: DoPart --
do f.getobjatts -> do_objatts;

-- do_fusion_regattr_body: DoPart --
do f.getregattr -> do_regattr;

-- do_arc_body: DoPart --
do a.getarc_att_gen -> do_arc_att_gen;
   a.getarc_content -> do_arc_content;

-- do_arc_att_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_arc_att;
   #);

-- do_arc_att_body: DoPart --
do (if a.symbol
    // cpn.arc_att_id then
       a[] -> do_arc_att_id;
    // cpn.arc_att_orientation then
       a[] -> do_arc_att_orientation;
    if);

-- do_arc_content_body: DoPart --
do a.newscan
   (#
   do current[] -> do_arc_content_gen;
   #);

-- do_arc_content_gen_body: DoPart --
do (if a.symbol
    // cpn.arc_text then
       a[] -> do_arc_text;
    // cpn.arc_annot then
       a[] -> do_arc_annot;
    // cpn.arc_aux_std then
       a[] -> do_arc_aux_std;
    // cpn.arc_shape then
       a[] -> do_arc_shape;
    // cpn.arc_lineattr then
       a[] -> do_arc_lineattr;
    // cpn.arc_placeend then
       a[] -> do_arc_placeend;
    // cpn.arc_transend then
       a[] -> do_arc_transend;
    // cpn.arc_textattr then
       a[] -> do_arc_textattr;
    // cpn.arc_flags then
       a[] -> do_arc_flags;
    // cpn.arc_connattr then
       a[] -> do_arc_connattr;
    // cpn.arc_fillattr then
       a[] -> do_arc_fillattr;
    // cpn.arc_layering then
       a[] -> do_arc_layering;
    if);

-- do_arc_att_id_body: DoPart --
do a.getatt_id -> do_att_id;

-- do_arc_att_orientation_body: DoPart --
do a.getatt_orientation -> do_att_orientation;

-- do_arc_text_body: DoPart --
do a.gettext -> do_text;

-- do_arc_shape_body: DoPart --
do a.getshape -> do_shape;

-- do_arc_annot_body: DoPart --
do a.getannot -> do_annot;

-- do_arc_aux_std_body: DoPart --
do a.getaux_std -> do_aux_std;

-- do_arc_lineattr_body: DoPart --
do a.getlineattr -> do_lineattr;

-- do_arc_placeend_body: DoPart --
do a.getplaceend -> do_placeend;

-- do_arc_transend_body: DoPart --
do a.gettransend -> do_transend;

-- do_arc_textattr_body: DoPart --
do a.gettextattr -> do_textattr;

-- do_arc_flags_body: DoPart --
do a.getflags -> do_flags;

-- do_arc_connattr_body: DoPart --
do a.getconnattr -> do_connattr;

-- do_arc_fillattr_body: DoPart --
do a.getfillattr -> do_fillattr;

-- do_arc_layering_body: DoPart --
do a.getlayering -> do_layering;

-- do_annot_body: DoPart --
do a.getatt_idOpt -> do_att_idOpt;
   a.getannot_content -> do_annot_content;

-- do_annot_content_body: DoPart --
do a.newscan
   (#
   do current[] -> do_annot_content_gen;
   #);

-- do_annot_content_gen_body: DoPart --
do (if a.symbol
    // cpn.annot_text then
       a[] -> do_annot_text;
    // cpn.annot_aux_std then
       a[] -> do_annot_aux_std;
    // cpn.annot_shape then
       a[] -> do_annot_shape;
    // cpn.annot_objatts then
       a[] -> do_annot_objatts;
    // cpn.annot_regattr then
       a[] -> do_annot_regattr;
    if);

-- do_annot_text_body: DoPart --
do a.gettext -> do_text;

-- do_annot_shape_body: DoPart --
do a.getshape -> do_shape;

-- do_annot_aux_std_body: DoPart --
do a.getaux_std -> do_aux_std;

-- do_annot_objatts_body: DoPart --
do a.getobjatts -> do_objatts;

-- do_annot_regattr_body: DoPart --
do a.getregattr -> do_regattr;

-- do_locdecl_body: DoPart --
do l.getatt_idOpt -> do_att_idOpt;
   l.getlocdecl_content -> do_locdecl_content;

-- do_locdecl_content_body: DoPart --
do l.newscan
   (#
   do current[] -> do_locdecl_content_gen;
   #);

-- do_locdecl_content_gen_body: DoPart --
do (if l.symbol
    // cpn.locdecl_text then
       l[] -> do_locdecl_text;
    // cpn.locdecl_aux_endp then
       l[] -> do_locdecl_aux_endp;
    // cpn.locdecl_aux_std then
       l[] -> do_locdecl_aux_std;
    // cpn.locdecl_shape then
       l[] -> do_locdecl_shape;
    // cpn.locdecl_objatts then
       l[] -> do_locdecl_objatts;
    if);

-- do_locdecl_text_body: DoPart --
do l.gettext -> do_text;

-- do_locdecl_shape_body: DoPart --
do l.getshape -> do_shape;

-- do_locdecl_aux_endp_body: DoPart --
do l.getaux_endp -> do_aux_endp;

-- do_locdecl_aux_std_body: DoPart --
do l.getaux_std -> do_aux_std;

-- do_locdecl_objatts_body: DoPart --
do l.getobjatts -> do_objatts;

-- do_tempdecl_body: DoPart --
do t.getatt_idOpt -> do_att_idOpt;
   t.gettempdecl_content -> do_tempdecl_content;

-- do_tempdecl_content_body: DoPart --
do t.newscan
   (#
   do current[] -> do_tempdecl_content_gen;
   #);

-- do_tempdecl_content_gen_body: DoPart --
do (if t.symbol
    // cpn.tempdecl_text then
       t[] -> do_tempdecl_text;
    // cpn.tempdecl_aux_endp then
       t[] -> do_tempdecl_aux_endp;
    // cpn.tempdecl_aux_std then
       t[] -> do_tempdecl_aux_std;
    // cpn.tempdecl_shape then
       t[] -> do_tempdecl_shape;
    // cpn.tempdecl_objatts then
       t[] -> do_tempdecl_objatts;
    if);

-- do_tempdecl_text_body: DoPart --
do t.gettext -> do_text;

-- do_tempdecl_shape_body: DoPart --
do t.getshape -> do_shape;

-- do_tempdecl_aux_endp_body: DoPart --
do t.getaux_endp -> do_aux_endp;

-- do_tempdecl_aux_std_body: DoPart --
do t.getaux_std -> do_aux_std;

-- do_tempdecl_objatts_body: DoPart --
do t.getobjatts -> do_objatts;

-- do_globdecl_body: DoPart --
do g.getatt_idOpt -> do_att_idOpt;
   g.getglobdecl_content -> do_globdecl_content;

-- do_globdecl_content_body: DoPart --
do g.newscan
   (#
   do current[] -> do_globdecl_content_gen;
   #);

-- do_globdecl_content_gen_body: DoPart --
do (if g.symbol
    // cpn.globdecl_text then
       g[] -> do_globdecl_text;
    // cpn.globdecl_aux_endp then
       g[] -> do_globdecl_aux_endp;
    // cpn.globdecl_aux_std then
       g[] -> do_globdecl_aux_std;
    // cpn.globdecl_shape then
       g[] -> do_globdecl_shape;
    // cpn.globdecl_objatts then
       g[] -> do_globdecl_objatts;
    if);

-- do_globdecl_text_body: DoPart --
do g.gettext -> do_text;

-- do_globdecl_shape_body: DoPart --
do g.getshape -> do_shape;

-- do_globdecl_aux_endp_body: DoPart --
do g.getaux_endp -> do_aux_endp;

-- do_globdecl_aux_std_body: DoPart --
do g.getaux_std -> do_aux_std;

-- do_globdecl_objatts_body: DoPart --
do g.getobjatts -> do_objatts;

-- do_aux_body: DoPart --
do a.getatt_idOpt -> do_att_idOpt;
   a.getaux_content -> do_aux_content;

-- do_aux_content_body: DoPart --
do a.newscan
   (#
   do current[] -> do_aux_content_gen;
   #);

-- do_aux_content_gen_body: DoPart --
do (if a.symbol
    // cpn.aux_text then
       a[] -> do_aux_text;
    // cpn.aux_endp then
       a[] -> do_aux_endp;
    // cpn.aux_aux_std then
       a[] -> do_aux_aux_std;
    // cpn.aux_shape then
       a[] -> do_aux_shape;
    // cpn.aux_objatts then
       a[] -> do_aux_objatts;
    if);

-- do_aux_text_body: DoPart --
do a.gettext -> do_text;

-- do_aux_shape_body: DoPart --
do a.getshape -> do_shape;

-- do_aux_aux_std_body: DoPart --
do a.getaux_std -> do_aux_std;

-- do_aux_objatts_body: DoPart --
do a.getobjatts -> do_objatts;

-- do_aux_conn_body: DoPart --
do a.getaux_conn_att_gen -> do_aux_conn_att_gen;
   a.getaux_conn_content -> do_aux_conn_content;

-- do_aux_conn_att_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_aux_conn_att;
   #);

-- do_aux_conn_att_body: DoPart --
do (if a.symbol
    // cpn.aux_conn_att_id then
       a[] -> do_aux_conn_att_id;
    // cpn.aux_conn_att_orient then
       a[] -> do_aux_conn_att_orient;
    if);

-- do_aux_conn_content_body: DoPart --
do a.newscan
   (#
   do current[] -> do_aux_conn_content_gen;
   #);

-- do_aux_conn_content_gen_body: DoPart --
do (if a.symbol
    // cpn.aux_conn_text then
       a[] -> do_aux_conn_text;
    // cpn.aux_conn_aux_std then
       a[] -> do_aux_conn_aux_std;
    // cpn.aux_conn_shape then
       a[] -> do_aux_conn_shape;
    // cpn.aux_conn_lineattr then
       a[] -> do_aux_conn_lineattr;
    // cpn.aux_conn_node1 then
       a[] -> do_aux_conn_node1;
    // cpn.aux_conn_node2 then
       a[] -> do_aux_conn_node2;
    // cpn.aux_conn_textattr then
       a[] -> do_aux_conn_textattr;
    // cpn.aux_conn_flags then
       a[] -> do_aux_conn_flags;
    // cpn.aux_conn_connattr then
       a[] -> do_aux_conn_connattr;
    // cpn.aux_conn_fillattr then
       a[] -> do_aux_conn_fillattr;
    // cpn.aux_conn_layering then
       a[] -> do_aux_conn_layering;
    if);

-- do_aux_conn_att_id_body: DoPart --
do a.getatt_id -> do_att_id;

-- do_aux_conn_att_orient_body: DoPart --
do a.getatt_orient -> do_att_orient;

-- do_aux_conn_text_body: DoPart --
do a.gettext -> do_text;

-- do_aux_conn_shape_body: DoPart --
do a.getshape -> do_shape;

-- do_aux_conn_aux_std_body: DoPart --
do a.getaux_std -> do_aux_std;

-- do_aux_conn_lineattr_body: DoPart --
do a.getlineattr -> do_lineattr;

-- do_aux_conn_node1_body: DoPart --
do a.getnode1 -> do_node1;

-- do_aux_conn_node2_body: DoPart --
do a.getnode2 -> do_node2;

-- do_aux_conn_textattr_body: DoPart --
do a.gettextattr -> do_textattr;

-- do_aux_conn_flags_body: DoPart --
do a.getflags -> do_flags;

-- do_aux_conn_connattr_body: DoPart --
do a.getconnattr -> do_connattr;

-- do_aux_conn_fillattr_body: DoPart --
do a.getfillattr -> do_fillattr;

-- do_aux_conn_layering_body: DoPart --
do a.getlayering -> do_layering;

-- do_aux_std_body: DoPart --
do a.getatt_idOpt -> do_att_idOpt;
   a.getaux_std_content -> do_aux_std_content;

-- do_aux_std_content_body: DoPart --
do a.newscan
   (#
   do current[] -> do_aux_std_content_gen;
   #);

-- do_aux_std_content_gen_body: DoPart --
do (if a.symbol
    // cpn.aux_std_text then
       a[] -> do_aux_std_text;
    // cpn.aux_std then
       a[] -> do_aux_std;
    // cpn.aux_std_shape then
       a[] -> do_aux_std_shape;
    // cpn.aux_std_objatts then
       a[] -> do_aux_std_objatts;
    // cpn.aux_std_regattr then
       a[] -> do_aux_std_regattr;
    if);

-- do_aux_std_text_body: DoPart --
do a.gettext -> do_text;

-- do_aux_std_shape_body: DoPart --
do a.getshape -> do_shape;

-- do_aux_std_objatts_body: DoPart --
do a.getobjatts -> do_objatts;

-- do_aux_std_regattr_body: DoPart --
do a.getregattr -> do_regattr;

-- do_aux_endp_body: DoPart --
do a.getatt_idOpt -> do_att_idOpt;
   a.getaux_endp_content -> do_aux_endp_content;

-- do_aux_endp_content_body: DoPart --
do a.newscan
   (#
   do current[] -> do_aux_endp_content_gen;
   #);

-- do_aux_endp_content_gen_body: DoPart --
do (if a.symbol
    // cpn.aux_endp_text then
       a[] -> do_aux_endp_text;
    // cpn.aux_endp_aux_std then
       a[] -> do_aux_endp_aux_std;
    // cpn.aux_endp_shape then
       a[] -> do_aux_endp_shape;
    // cpn.aux_endp_objatts then
       a[] -> do_aux_endp_objatts;
    // cpn.aux_endp_regattr then
       a[] -> do_aux_endp_regattr;
    if);

-- do_aux_endp_text_body: DoPart --
do a.gettext -> do_text;

-- do_aux_endp_aux_std_body: DoPart --
do a.getaux_std -> do_aux_std;

-- do_aux_endp_shape_body: DoPart --
do a.getshape -> do_shape;

-- do_aux_endp_objatts_body: DoPart --
do a.getobjatts -> do_objatts;

-- do_aux_endp_regattr_body: DoPart --
do a.getregattr -> do_regattr;

-- do_portkreg_body: DoPart --
do p.getatt_idOpt -> do_att_idOpt;
   p.getportkreg_content -> do_portkreg_content;

-- do_portkreg_content_body: DoPart --
do p.newscan
   (#
   do current[] -> do_portkreg_content_gen;
   #);

-- do_portkreg_content_gen_body: DoPart --
do (if p.symbol
    // cpn.portkreg_text then
       p[] -> do_portkreg_text;
    // cpn.portkreg_aux_std then
       p[] -> do_portkreg_aux_std;
    // cpn.portkreg_portreg then
       p[] -> do_portkreg_portreg;
    // cpn.portkreg_shape then
       p[] -> do_portkreg_shape;
    // cpn.portkreg_objatts then
       p[] -> do_portkreg_objatts;
    // cpn.portkreg_regattr then
       p[] -> do_portkreg_regattr;
    if);

-- do_portkreg_text_body: DoPart --
do p.gettext -> do_text;

-- do_portkreg_shape_body: DoPart --
do p.getshape -> do_shape;

-- do_portkreg_aux_std_body: DoPart --
do p.getaux_std -> do_aux_std;

-- do_portkreg_portreg_body: DoPart --
do p.getportreg -> do_portreg;

-- do_portkreg_objatts_body: DoPart --
do p.getobjatts -> do_objatts;

-- do_portkreg_regattr_body: DoPart --
do p.getregattr -> do_regattr;

-- do_portreg_body: DoPart --
do p.getatt_idOpt -> do_att_idOpt;
   p.getportreg_content -> do_portreg_content;

-- do_portreg_content_body: DoPart --
do p.newscan
   (#
   do current[] -> do_portreg_content_gen;
   #);

-- do_portreg_content_gen_body: DoPart --
do (if p.symbol
    // cpn.portreg_text then
       p[] -> do_portreg_text;
    // cpn.portreg_aux_std then
       p[] -> do_portreg_aux_std;
    // cpn.portreg_shape then
       p[] -> do_portreg_shape;
    // cpn.portreg_objatts then
       p[] -> do_portreg_objatts;
    // cpn.portreg_regattr then
       p[] -> do_portreg_regattr;
    if);

-- do_portreg_text_body: DoPart --
do p.gettext -> do_text;

-- do_portreg_shape_body: DoPart --
do p.getshape -> do_shape;

-- do_portreg_aux_std_body: DoPart --
do p.getaux_std -> do_aux_std;

-- do_portreg_objatts_body: DoPart --
do p.getobjatts -> do_objatts;

-- do_portreg_regattr_body: DoPart --
do p.getregattr -> do_regattr;

-- do_mark_key_body: DoPart --
do m.getatt_idOpt -> do_att_idOpt;
   m.getmark_key_content -> do_mark_key_content;

-- do_mark_key_content_body: DoPart --
do m.newscan
   (#
   do current[] -> do_mark_key_content_gen;
   #);

-- do_mark_key_content_gen_body: DoPart --
do (if m.symbol
    // cpn.mark_key_text then
       m[] -> do_mark_key_text;
    // cpn.mark_key_aux_std then
       m[] -> do_mark_key_aux_std;
    // cpn.mark_key_mark then
       m[] -> do_mark_key_mark;
    // cpn.mark_key_shape then
       m[] -> do_mark_key_shape;
    // cpn.mark_key_objatts then
       m[] -> do_mark_key_objatts;
    // cpn.mark_key_regattr then
       m[] -> do_mark_key_regattr;
    if);

-- do_mark_key_text_body: DoPart --
do m.gettext -> do_text;

-- do_mark_key_shape_body: DoPart --
do m.getshape -> do_shape;

-- do_mark_key_aux_std_body: DoPart --
do m.getaux_std -> do_aux_std;

-- do_mark_key_mark_body: DoPart --
do m.getmark -> do_mark;

-- do_mark_key_objatts_body: DoPart --
do m.getobjatts -> do_objatts;

-- do_mark_key_regattr_body: DoPart --
do m.getregattr -> do_regattr;

-- do_mark_body: DoPart --
do m.getatt_idOpt -> do_att_idOpt;
   m.getmark_content -> do_mark_content;

-- do_mark_content_body: DoPart --
do m.newscan
   (#
   do current[] -> do_mark_content_gen;
   #);

-- do_mark_content_gen_body: DoPart --
do (if m.symbol
    // cpn.mark_text then
       m[] -> do_mark_text;
    // cpn.mark_aux_std then
       m[] -> do_mark_aux_std;
    // cpn.mark_shape then
       m[] -> do_mark_shape;
    // cpn.mark_objatts then
       m[] -> do_mark_objatts;
    // cpn.mark_regattr then
       m[] -> do_mark_regattr;
    if);

-- do_mark_text_body: DoPart --
do m.gettext -> do_text;

-- do_mark_shape_body: DoPart --
do m.getshape -> do_shape;

-- do_mark_aux_std_body: DoPart --
do m.getaux_std -> do_aux_std;

-- do_mark_objatts_body: DoPart --
do m.getobjatts -> do_objatts;

-- do_mark_regattr_body: DoPart --
do m.getregattr -> do_regattr;

-- do_aux_enab_body: DoPart --
do a.getatt_idOpt -> do_att_idOpt;
   a.getaux_enab_content -> do_aux_enab_content;

-- do_aux_enab_content_body: DoPart --
do a.newscan
   (#
   do current[] -> do_aux_enab_content_gen;
   #);

-- do_aux_enab_content_gen_body: DoPart --
do (if a.symbol
    // cpn.aux_enab_text then
       a[] -> do_aux_enab_text;
    // cpn.aux_enab_shape then
       a[] -> do_aux_enab_shape;
    // cpn.aux_enab_objatts then
       a[] -> do_aux_enab_objatts;
    // cpn.aux_enab_regattr then
       a[] -> do_aux_enab_regattr;
    if);

-- do_aux_enab_text_body: DoPart --
do a.gettext -> do_text;

-- do_aux_enab_shape_body: DoPart --
do a.getshape -> do_shape;

-- do_aux_enab_objatts_body: DoPart --
do a.getobjatts -> do_objatts;

-- do_aux_enab_regattr_body: DoPart --
do a.getregattr -> do_regattr;

-- do_hierpage_body: DoPart --
do h.getatt_idOpt -> do_att_idOpt;
   h.gethierpage_content -> do_hierpage_content;

-- do_hierpage_content_body: DoPart --
do h.newscan
   (#
   do current[] -> do_hierpage_content_gen;
   #);

-- do_hierpage_content_gen_body: DoPart --
do (if h.symbol
    // cpn.hierpage_text then
       h[] -> do_hierpage_text;
    // cpn.hierpage_aux then
       h[] -> do_hierpage_aux;
    // cpn.hierpage_aux_conn then
       h[] -> do_hierpage_aux_conn;
    // cpn.hierpage_pagenode then
       h[] -> do_hierpage_pagenode;
    // cpn.hierpage_substcon then
       h[] -> do_hierpage_substcon;
    // cpn.hierpage_shape then
       h[] -> do_hierpage_shape;
    // cpn.hierpage_pageattr then
       h[] -> do_hierpage_pageattr;
    // cpn.hierpage_fillattr then
       h[] -> do_hierpage_fillattr;
    // cpn.hierpage_lineattr then
       h[] -> do_hierpage_lineattr;
    // cpn.hierpage_posattr then
       h[] -> do_hierpage_posattr;
    // cpn.hierpage_winattr then
       h[] -> do_hierpage_winattr;
    if);

-- do_hierpage_text_body: DoPart --
do h.gettext -> do_text;

-- do_hierpage_shape_body: DoPart --
do h.getshape -> do_shape;

-- do_hierpage_aux_body: DoPart --
do h.getaux -> do_aux;

-- do_hierpage_aux_conn_body: DoPart --
do h.getaux_conn -> do_aux_conn;

-- do_hierpage_pagenode_body: DoPart --
do h.getpagenode -> do_pagenode;

-- do_hierpage_substcon_body: DoPart --
do h.getsubstcon -> do_substcon;

-- do_hierpage_pageattr_body: DoPart --
do h.getpageattr -> do_pageattr;

-- do_hierpage_fillattr_body: DoPart --
do h.getfillattr -> do_fillattr;

-- do_hierpage_lineattr_body: DoPart --
do h.getlineattr -> do_lineattr;

-- do_hierpage_posattr_body: DoPart --
do h.getposattr -> do_posattr;

-- do_hierpage_winattr_body: DoPart --
do h.getwinattr -> do_winattr;

-- do_pagenode_body: DoPart --
do p.getpagenode_att_gen -> do_pagenode_att_gen;
   p.getpagenode_content -> do_pagenode_content;

-- do_pagenode_att_gen_body: DoPart --
do p.newscan
   (#
   do current[] -> do_pagenode_att;
   #);

-- do_pagenode_att_body: DoPart --
do (if p.symbol
    // cpn.pagenode_att_id then
       p[] -> do_pagenode_att_id;
    // cpn.pagenode_att_pageref then
       p[] -> do_pagenode_att_pageref;
    if);

-- do_pagenode_content_body: DoPart --
do p.newscan
   (#
   do current[] -> do_pagenode_content_gen;
   #);

-- do_pagenode_content_gen_body: DoPart --
do (if p.symbol
    // cpn.pagenode_text then
       p[] -> do_pagenode_text;
    // cpn.pagenode_aux_std then
       p[] -> do_pagenode_aux_std;
    // cpn.pagenode_pgmodkey then
       p[] -> do_pagenode_pgmodkey;
    // cpn.pagenode_shape then
       p[] -> do_pagenode_shape;
    // cpn.pagenode_objatts then
       p[] -> do_pagenode_objatts;
    if);

-- do_pagenode_att_id_body: DoPart --
do p.getatt_id -> do_att_id;

-- do_pagenode_att_pageref_body: DoPart --
do p.getatt_pageref -> do_att_pageref;

-- do_pagenode_text_body: DoPart --
do p.gettext -> do_text;

-- do_pagenode_shape_body: DoPart --
do p.getshape -> do_shape;

-- do_pagenode_aux_std_body: DoPart --
do p.getaux_std -> do_aux_std;

-- do_pagenode_pgmodkey_body: DoPart --
do p.getpgmodkey -> do_pgmodkey;

-- do_pagenode_objatts_body: DoPart --
do p.getobjatts -> do_objatts;

-- do_idref_body: DoPart --
do i.getnameAppl -> do_nameAppl;

-- do_id_body: DoPart --
do i.getnameAppl -> do_nameAppl;

-- do_pgmodkey_body: DoPart --
do p.getatt_idOpt -> do_att_idOpt;
   p.getpgmodkey_content -> do_pgmodkey_content;

-- do_pgmodkey_content_body: DoPart --
do p.newscan
   (#
   do current[] -> do_pgmodkey_content_gen;
   #);

-- do_pgmodkey_content_gen_body: DoPart --
do (if p.symbol
    // cpn.pgmodkey_text then
       p[] -> do_pgmodkey_text;
    // cpn.pgmodkey_pagemode then
       p[] -> do_pgmodkey_pagemode;
    // cpn.pgmodkey_aux_std then
       p[] -> do_pgmodkey_aux_std;
    // cpn.pgmodkey_shape then
       p[] -> do_pgmodkey_shape;
    // cpn.pgmodkey_objatts then
       p[] -> do_pgmodkey_objatts;
    // cpn.pgmodkey_regattr then
       p[] -> do_pgmodkey_regattr;
    if);

-- do_pgmodkey_text_body: DoPart --
do p.gettext -> do_text;

-- do_pgmodkey_shape_body: DoPart --
do p.getshape -> do_shape;

-- do_pgmodkey_pagemode_body: DoPart --
do p.getpagemode -> do_pagemode;

-- do_pgmodkey_aux_std_body: DoPart --
do p.getaux_std -> do_aux_std;

-- do_pgmodkey_objatts_body: DoPart --
do p.getobjatts -> do_objatts;

-- do_pgmodkey_regattr_body: DoPart --
do p.getregattr -> do_regattr;

-- do_pagemode_body: DoPart --
do p.getatt_idOpt -> do_att_idOpt;
   p.getpagemode_content -> do_pagemode_content;

-- do_pagemode_content_body: DoPart --
do p.newscan
   (#
   do current[] -> do_pagemode_content_gen;
   #);

-- do_pagemode_content_gen_body: DoPart --
do (if p.symbol
    // cpn.pagemode_text then
       p[] -> do_pagemode_text;
    // cpn.pagemode_aux_std then
       p[] -> do_pagemode_aux_std;
    // cpn.pagemode_shape then
       p[] -> do_pagemode_shape;
    // cpn.pagemode_objatts then
       p[] -> do_pagemode_objatts;
    // cpn.pagemode_regattr then
       p[] -> do_pagemode_regattr;
    if);

-- do_pagemode_text_body: DoPart --
do p.gettext -> do_text;

-- do_pagemode_shape_body: DoPart --
do p.getshape -> do_shape;

-- do_pagemode_aux_std_body: DoPart --
do p.getaux_std -> do_aux_std;

-- do_pagemode_objatts_body: DoPart --
do p.getobjatts -> do_objatts;

-- do_pagemode_regattr_body: DoPart --
do p.getregattr -> do_regattr;

-- do_substcon_body: DoPart --
do s.getsubstcon_att_gen -> do_substcon_att_gen;
   s.getsubstcon_content -> do_substcon_content;

-- do_substcon_att_gen_body: DoPart --
do s.newscan
   (#
   do current[] -> do_substcon_att;
   #);

-- do_substcon_att_body: DoPart --
do (if s.symbol
    // cpn.substcon_att_id then
       s[] -> do_substcon_att_id;
    // cpn.substcon_att_orient then
       s[] -> do_substcon_att_orient;
    if);

-- do_substcon_content_body: DoPart --
do s.newscan
   (#
   do current[] -> do_substcon_content_gen;
   #);

-- do_substcon_content_gen_body: DoPart --
do (if s.symbol
    // cpn.substcon_text then
       s[] -> do_substcon_text;
    // cpn.substcon_aux_std then
       s[] -> do_substcon_aux_std;
    // cpn.substcon_substtag then
       s[] -> do_substcon_substtag;
    // cpn.substcon_shape then
       s[] -> do_substcon_shape;
    // cpn.substcon_lineattr then
       s[] -> do_substcon_lineattr;
    // cpn.substcon_sup_page then
       s[] -> do_substcon_sup_page;
    // cpn.substcon_sub_page then
       s[] -> do_substcon_sub_page;
    // cpn.substcon_textattr then
       s[] -> do_substcon_textattr;
    // cpn.substcon_flags then
       s[] -> do_substcon_flags;
    // cpn.substcon_connattr then
       s[] -> do_substcon_connattr;
    // cpn.substcon_fillattr then
       s[] -> do_substcon_fillattr;
    // cpn.substcon_layering then
       s[] -> do_substcon_layering;
    if);

-- do_substcon_att_id_body: DoPart --
do s.getatt_id -> do_att_id;

-- do_substcon_att_orient_body: DoPart --
do s.getatt_orient -> do_att_orient;

-- do_substcon_text_body: DoPart --
do s.gettext -> do_text;

-- do_substcon_shape_body: DoPart --
do s.getshape -> do_shape;

-- do_substcon_aux_std_body: DoPart --
do s.getaux_std -> do_aux_std;

-- do_substcon_substtag_body: DoPart --
do s.getsubsttag -> do_substtag;

-- do_substcon_lineattr_body: DoPart --
do s.getlineattr -> do_lineattr;

-- do_substcon_sup_page_body: DoPart --
do s.getsup_page -> do_sup_page;

-- do_substcon_sub_page_body: DoPart --
do s.getsub_page -> do_sub_page;

-- do_substcon_textattr_body: DoPart --
do s.gettextattr -> do_textattr;

-- do_substcon_flags_body: DoPart --
do s.getflags -> do_flags;

-- do_substcon_connattr_body: DoPart --
do s.getconnattr -> do_connattr;

-- do_substcon_fillattr_body: DoPart --
do s.getfillattr -> do_fillattr;

-- do_substcon_layering_body: DoPart --
do s.getlayering -> do_layering;

-- do_substtag_body: DoPart --
do s.getsubsttag_att_gen -> do_substtag_att_gen;
   s.getsubsttag_content -> do_substtag_content;

-- do_substtag_att_gen_body: DoPart --
do s.newscan
   (#
   do current[] -> do_substtag_att;
   #);

-- do_substtag_att_body: DoPart --
do (if s.symbol
    // cpn.att_id then
       s[] -> do_att_id;
    // cpn.att_transref then
       s[] -> do_att_transref;
    if);

-- do_substtag_content_body: DoPart --
do s.newscan
   (#
   do current[] -> do_substtag_content_gen;
   #);

-- do_substtag_content_gen_body: DoPart --
do (if s.symbol
    // cpn.substtag_text then
       s[] -> do_substtag_text;
    // cpn.substtag_aux_std then
       s[] -> do_substtag_aux_std;
    // cpn.substtag_shape then
       s[] -> do_substtag_shape;
    // cpn.substtag_objatts then
       s[] -> do_substtag_objatts;
    // cpn.substtag_regattr then
       s[] -> do_substtag_regattr;
    if);

-- do_substtag_text_body: DoPart --
do s.gettext -> do_text;

-- do_substtag_shape_body: DoPart --
do s.getshape -> do_shape;

-- do_substtag_aux_std_body: DoPart --
do s.getaux_std -> do_aux_std;

-- do_substtag_objatts_body: DoPart --
do s.getobjatts -> do_objatts;

-- do_substtag_regattr_body: DoPart --
do s.getregattr -> do_regattr;

-- do_generator_body: DoPart --
do g.getgenerator_att_gen -> do_generator_att_gen;
   g.getgenerator_etagOpt -> do_generator_etagOpt;

-- do_generator_att_gen_body: DoPart --
do g.newscan
   (#
   do current[] -> do_generator_att;
   #);

-- do_generator_att_body: DoPart --
do (if g.symbol
    // cpn.att_tool then
       g[] -> do_att_tool;
    // cpn.att_version then
       g[] -> do_att_version;
    if);

-- do_generator_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_generator_etag;
   if);

-- do_generator_etag_body: DoPart --
do 

-- do_coordinate_body: DoPart --
do c.getminusOpt -> do_minusOpt;
   c.getconst -> do_const;

-- do_dimension_body: DoPart --
do d.getcoordinate -> do_coordinate;

-- do_number_body: DoPart --
do n.getconst -> do_const;

-- do_minusOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_minus;
   if);

-- do_minus_body: DoPart --
do 

-- do_points_body: DoPart --
do p.newscan
   (#
   do current[] -> do_point;
   #);

-- do_point_body: DoPart --
do p.getx -> do_coordinate;
   p.gety -> do_coordinate;

-- do_cpn_boolean_body: DoPart --
do (if c.symbol
    // cpn.cpn_true then
       c[] -> do_cpn_true;
    // cpn.cpn_false then
       c[] -> do_cpn_false;
    if);

-- do_cpn_true_body: DoPart --
do 

-- do_cpn_false_body: DoPart --
do 

-- do_att_id_body: DoPart --
do a.getid -> do_id;

-- do_att_orientation_body: DoPart --
do a.getarcors -> do_arcors;

-- do_att_orient_body: DoPart --
do a.getorients -> do_orients;

-- do_att_pageref_body: DoPart --
do a.getidref -> do_idref;

-- do_att_transref_body: DoPart --
do a.getidref -> do_const;

-- do_att_tool_body: DoPart --
do a.getstring -> do_string;

-- do_att_version_body: DoPart --
do a.getstring -> do_string;

-- do_att_points_body: DoPart --
do a.getpoints -> do_points;

-- do_att_x_body: DoPart --
do a.getcoordinate -> do_coordinate;

-- do_att_y_body: DoPart --
do a.getcoordinate -> do_coordinate;

-- do_att_h_body: DoPart --
do a.getdimension -> do_dimension;

-- do_att_w_body: DoPart --
do a.getdimension -> do_dimension;

-- do_att_stangle_body: DoPart --
do a.getnumber -> do_number;

-- do_att_endangle_body: DoPart --
do a.getnumber -> do_number;

-- do_att_curv_body: DoPart --
do a.getnumber -> do_number;

-- do_att_pattern_body: DoPart --
do a.getpats -> do_pats;

-- do_att_colour_body: DoPart --
do a.getcols -> do_cols;

-- do_att_type_body: DoPart --
do a.getlintyps -> do_lintyps;

-- do_att_thick_body: DoPart --
do a.getnumber -> do_number;

-- do_att_font_body: DoPart --
do a.getfonts -> do_fonts;

-- do_att_just_body: DoPart --
do a.getjusts -> do_justs;

-- do_att_bold_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_italic_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_underlin_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_outline_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_shadow_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_condense_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_extend_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_scbar_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_size_body: DoPart --
do a.getnumber -> do_number;

-- do_att_mask_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_omit_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_nobound_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_nosizing_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_visible_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_idref_body: DoPart --
do a.getidref -> do_idref;

-- do_att_endpoint_body: DoPart --
do a.getidref -> do_idref;

-- do_att_name_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_number_body: DoPart --
do a.getnumber -> do_number;

-- do_att_visbor_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_colorap_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_sizewp_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_hoffset_body: DoPart --
do a.getdimension -> do_dimension;

-- do_att_voffset_body: DoPart --
do a.getdimension -> do_dimension;

-- do_att_hdwidth_body: DoPart --
do a.getdimension -> do_dimension;

-- do_att_hdheight_body: DoPart --
do a.getdimension -> do_dimension;

-- do_att_txtwidth_body: DoPart --
do a.getdimension -> do_dimension;

-- do_att_txtheight_body: DoPart --
do a.getdimension -> do_dimension;

-- do_att_open_body: DoPart --
do a.getcpn_boolean -> do_cpn_boolean;

-- do_att_width_body: DoPart --
do a.getnumber -> do_number;

-- do_att_height_body: DoPart --
do a.getnumber -> do_number;

-- do_att_xpos_body: DoPart --
do a.getnumber -> do_number;

-- do_att_ypos_body: DoPart --
do a.getnumber -> do_number;

-- do_att_siminc_body: DoPart --
do a.getstring -> do_string;

-- do_att_observe_body: DoPart --
do a.getstring -> do_string;

-- do_att_propocc_body: DoPart --
do a.getstring -> do_string;

-- do_att_interact_body: DoPart --
do a.getstring -> do_string;

-- do_att_execcode_body: DoPart --
do a.getstring -> do_string;

-- do_att_subpage_body: DoPart --
do a.getidref -> do_idref;

-- do_att_portsock_body: DoPart --
do a.getportsocks -> do_portsocks;

-- do_att_porttype_body: DoPart --
do a.getprttyps -> do_prttyps;

-- do_att_insts_body: DoPart --
do a.getnumber -> do_number;

-- do_att_laytype_body: DoPart --
do a.getlaytyps -> do_laytyps;

-- do_att_xy_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_xy;
   #);

-- do_att_xy_body: DoPart --
do (if a.symbol
    // cpn.att_x then
       a[] -> do_att_x;
    // cpn.att_y then
       a[] -> do_att_y;
    if);

-- do_att_hw_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_hw;
   #);

-- do_att_hw_body: DoPart --
do (if a.symbol
    // cpn.att_h then
       a[] -> do_att_h;
    // cpn.att_w then
       a[] -> do_att_w;
    if);

-- do_posattr_body: DoPart --
do p.getatt_xy_gen -> do_att_xy_gen;
   p.getposattr_etagOpt -> do_posattr_etagOpt;

-- do_posattr_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_posattr_etag;
   if);

-- do_posattr_etag_body: DoPart --
do 

-- do_box_body: DoPart --
do b.getatt_hw_gen -> do_att_hw_gen;
   b.getbox_etagOpt -> do_box_etagOpt;

-- do_box_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_box_etag;
   if);

-- do_box_etag_body: DoPart --
do 

-- do_ellipse_body: DoPart --
do e.getatt_hw_gen -> do_att_hw_gen;
   e.getellipse_etagOpt -> do_ellipse_etagOpt;

-- do_ellipse_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_ellipse_etag;
   if);

-- do_ellipse_etag_body: DoPart --
do 

-- do_poly_body: DoPart --
do p.getatt_points -> do_att_points;
   p.getpoly_etagOpt -> do_poly_etagOpt;

-- do_poly_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_poly_etag;
   if);

-- do_poly_etag_body: DoPart --
do 

-- do_line_body: DoPart --
do l.getatt_points -> do_att_points;
   l.getline_etagOpt -> do_line_etagOpt;

-- do_line_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_line_etag;
   if);

-- do_line_etag_body: DoPart --
do 

-- do_label_body: DoPart --
do l.getlabel_etagOpt -> do_label_etagOpt;

-- do_label_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_label_etag;
   if);

-- do_label_etag_body: DoPart --
do 

-- do_wedge_body: DoPart --
do w.getatt_hwse_gen -> do_att_hwse_gen;
   w.getwedge_etagOpt -> do_wedge_etagOpt;

-- do_wedge_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_wedge_etag;
   if);

-- do_wedge_etag_body: DoPart --
do 

-- do_att_hwse_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_hwse;
   #);

-- do_att_hwse_body: DoPart --
do (if a.symbol
    // cpn.wedge_att_hw then
       a[] -> do_wedge_att_hw;
    // cpn.att_stangle then
       a[] -> do_att_stangle;
    // cpn.att_endangle then
       a[] -> do_att_endangle;
    if);

-- do_wedge_att_hw_body: DoPart --
do w.getatt_hw -> do_att_hw;

-- do_rbox_body: DoPart --
do r.getatt_hwc_gen -> do_att_hwc_gen;
   r.getrbox_etagOpt -> do_rbox_etagOpt;

-- do_rbox_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_rbox_etag;
   if);

-- do_rbox_etag_body: DoPart --
do 

-- do_att_hwc_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_hwc;
   #);

-- do_att_hwc_body: DoPart --
do (if a.symbol
    // cpn.rbox_att_hw then
       a[] -> do_rbox_att_hw;
    // cpn.rbox_att_curv then
       a[] -> do_rbox_att_curv;
    if);

-- do_rbox_att_curv_body: DoPart --
do r.getatt_curv -> do_att_curv;

-- do_rbox_att_hw_body: DoPart --
do r.getatt_hw -> do_att_hw;

-- do_seg_conn_body: DoPart --
do s.getatt_cp_gen -> do_att_cp_gen;
   s.getseg_conn_etagOpt -> do_seg_conn_etagOpt;

-- do_seg_conn_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_seg_conn_etag;
   if);

-- do_seg_conn_etag_body: DoPart --
do 

-- do_att_cp_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_cp;
   #);

-- do_att_cp_body: DoPart --
do (if a.symbol
    // cpn.att_curv then
       a[] -> do_att_curv;
    // cpn.att_points then
       a[] -> do_att_points;
    if);

-- do_fillattr_body: DoPart --
do f.getatt_pc_gen -> do_att_pc_gen;
   f.getfillattr_etagOpt -> do_fillattr_etagOpt;

-- do_fillattr_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_fillattr_etag;
   if);

-- do_fillattr_etag_body: DoPart --
do 

-- do_att_pc_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_pc;
   #);

-- do_att_pc_body: DoPart --
do (if a.symbol
    // cpn.fillattr_att_pattern then
       a[] -> do_fillattr_att_pattern;
    // cpn.fillattr_att_colour then
       a[] -> do_fillattr_att_colour;
    if);

-- do_fillattr_att_pattern_body: DoPart --
do f.getatt_pattern -> do_att_pattern;

-- do_fillattr_att_colour_body: DoPart --
do f.getatt_colour -> do_att_colour;

-- do_lineattr_body: DoPart --
do l.getatt_ttc_gen -> do_att_ttc_gen;
   l.getlineattr_etagOpt -> do_lineattr_etagOpt;

-- do_lineattr_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_lineattr_etag;
   if);

-- do_lineattr_etag_body: DoPart --
do 

-- do_att_ttc_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_ttc;
   #);

-- do_att_ttc_body: DoPart --
do (if a.symbol
    // cpn.att_type then
       a[] -> do_att_type;
    // cpn.att_thick then
       a[] -> do_att_thick;
    // cpn.lineattr_att_colour then
       a[] -> do_lineattr_att_colour;
    if);

-- do_lineattr_att_colour_body: DoPart --
do l.getatt_colour -> do_att_colour;

-- do_textattr_body: DoPart --
do t.getatt_textattr_gen -> do_att_textattr_gen;
   t.gettextattr_etagOpt -> do_textattr_etagOpt;

-- do_textattr_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_textattr_etag;
   if);

-- do_textattr_etag_body: DoPart --
do 

-- do_att_textattr_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_textattr;
   #);

-- do_att_textattr_body: DoPart --
do (if a.symbol
    // cpn.att_font then
       a[] -> do_att_font;
    // cpn.att_just then
       a[] -> do_att_just;
    // cpn.att_colour then
       a[] -> do_att_colour;
    // cpn.att_bold then
       a[] -> do_att_bold;
    // cpn.att_italic then
       a[] -> do_att_italic;
    // cpn.att_underlin then
       a[] -> do_att_underlin;
    // cpn.att_outline then
       a[] -> do_att_outline;
    // cpn.att_shadow then
       a[] -> do_att_shadow;
    // cpn.att_condense then
       a[] -> do_att_condense;
    // cpn.att_extend then
       a[] -> do_att_extend;
    // cpn.att_scbar then
       a[] -> do_att_scbar;
    // cpn.att_size then
       a[] -> do_att_size;
    if);

-- do_flags_body: DoPart --
do f.getatt_flags_gen -> do_att_flags_gen;
   f.getflags_etagOpt -> do_flags_etagOpt;

-- do_flags_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_flags_etag;
   if);

-- do_flags_etag_body: DoPart --
do 

-- do_att_flags_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_flags;
   #);

-- do_att_flags_body: DoPart --
do (if a.symbol
    // cpn.att_mask then
       a[] -> do_att_mask;
    // cpn.att_omit then
       a[] -> do_att_omit;
    // cpn.att_nobound then
       a[] -> do_att_nobound;
    // cpn.att_nosizing then
       a[] -> do_att_nosizing;
    // cpn.att_visible then
       a[] -> do_att_visible;
    if);

-- do_placeend_body: DoPart --
do p.getatt_placeend_gen -> do_att_placeend_gen;
   p.getplaceend_etagOpt -> do_placeend_etagOpt;

-- do_placeend_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_placeend_etag;
   if);

-- do_placeend_etag_body: DoPart --
do 

-- do_att_placeend_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_placeend;
   #);

-- do_att_placeend_body: DoPart --
do (if a.symbol
    // cpn.att_idref then
       a[] -> do_att_idref;
    // cpn.att_endpoint then
       a[] -> do_att_endpoint;
    if);

-- do_transend_body: DoPart --
do t.getatt_placeend_gen -> do_att_placeend_gen;
   t.gettransend_etagOpt -> do_transend_etagOpt;

-- do_transend_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_transend_etag;
   if);

-- do_transend_etag_body: DoPart --
do 

-- do_node1_body: DoPart --
do n.getatt_idOpt -> do_att_idOpt;
   n.getnode1_etagOpt -> do_node1_etagOpt;

-- do_node1_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_node1_etag;
   if);

-- do_node1_etag_body: DoPart --
do 

-- do_node2_body: DoPart --
do n.getatt_idOpt -> do_att_idOpt;
   n.getnode2_etagOpt -> do_node2_etagOpt;

-- do_node2_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_node2_etag;
   if);

-- do_node2_etag_body: DoPart --
do 

-- do_pageattr_body: DoPart --
do p.getatt_pageattr_gen -> do_att_pageattr_gen;
   p.getpageattr_etagOpt -> do_pageattr_etagOpt;

-- do_pageattr_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_pageattr_etag;
   if);

-- do_pageattr_etag_body: DoPart --
do 

-- do_att_pageattr_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_pageattr;
   #);

-- do_att_pageattr_body: DoPart --
do (if a.symbol
    // cpn.att_name then
       a[] -> do_att_name;
    // cpn.att_number then
       a[] -> do_att_number;
    // cpn.att_visbor then
       a[] -> do_att_visbor;
    if);

-- do_regattr_body: DoPart --
do r.getatt_regattr_gen -> do_att_regattr_gen;
   r.getregattr_etagOpt -> do_regattr_etagOpt;

-- do_regattr_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_regattr_etag;
   if);

-- do_regattr_etag_body: DoPart --
do 

-- do_att_regattr_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_regattr;
   #);

-- do_att_regattr_body: DoPart --
do (if a.symbol
    // cpn.att_colorap then
       a[] -> do_att_colorap;
    // cpn.att_sizewp then
       a[] -> do_att_sizewp;
    // cpn.att_hoffset then
       a[] -> do_att_hoffset;
    // cpn.att_voffset then
       a[] -> do_att_voffset;
    if);

-- do_connattr_body: DoPart --
do c.getatt_connattr_gen -> do_att_connattr_gen;
   c.getconnattr_etagOpt -> do_connattr_etagOpt;

-- do_connattr_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_connattr_etag;
   if);

-- do_connattr_etag_body: DoPart --
do 

-- do_att_connattr_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_connattr;
   #);

-- do_att_connattr_body: DoPart --
do (if a.symbol
    // cpn.att_hdwidth then
       a[] -> do_att_hdwidth;
    // cpn.att_hdheight then
       a[] -> do_att_hdheight;
    // cpn.att_txtwidth then
       a[] -> do_att_txtwidth;
    // cpn.att_txtheight then
       a[] -> do_att_txtheight;
    if);

-- do_winattr_body: DoPart --
do w.getatt_winattr_gen -> do_att_winattr_gen;
   w.getwinattr_etagOpt -> do_winattr_etagOpt;

-- do_winattr_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_winattr_etag;
   if);

-- do_winattr_etag_body: DoPart --
do 

-- do_att_winattr_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_winattr;
   #);

-- do_att_winattr_body: DoPart --
do (if a.symbol
    // cpn.att_open then
       a[] -> do_att_open;
    // cpn.att_width then
       a[] -> do_att_width;
    // cpn.att_height then
       a[] -> do_att_height;
    // cpn.att_xpos then
       a[] -> do_att_xpos;
    // cpn.att_ypos then
       a[] -> do_att_ypos;
    if);

-- do_modeattr_body: DoPart --
do m.getatt_modeattr_gen -> do_att_modeattr_gen;
   m.getmodeattr_etagOpt -> do_modeattr_etagOpt;

-- do_modeattr_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_modeattr_etag;
   if);

-- do_modeattr_etag_body: DoPart --
do 

-- do_att_modeattr_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_modeattr;
   #);

-- do_att_modeattr_body: DoPart --
do (if a.symbol
    // cpn.att_siminc then
       a[] -> do_att_siminc;
    // cpn.att_observe then
       a[] -> do_att_observe;
    // cpn.att_propocc then
       a[] -> do_att_propocc;
    // cpn.att_interact then
       a[] -> do_att_interact;
    // cpn.att_execcode then
       a[] -> do_att_execcode;
    if);

-- do_subst_body: DoPart --
do s.getatt_subst_gen -> do_att_subst_gen;
   s.getsubst_etagOpt -> do_subst_etagOpt;

-- do_subst_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_subst_etag;
   if);

-- do_subst_etag_body: DoPart --
do 

-- do_att_subst_gen_body: DoPart --
do a.newscan
   (#
   do current[] -> do_att_subst;
   #);

-- do_att_subst_body: DoPart --
do (if a.symbol
    // cpn.att_subpage then
       a[] -> do_att_subpage;
    // cpn.att_portsock then
       a[] -> do_att_portsock;
    if);

-- do_portsocks_body: DoPart --
do p.newscan
   (#
   do current[] -> do_portsock;
   #);

-- do_portsock_body: DoPart --
do p.getport -> do_string;
   p.getsocket -> do_string;

-- do_port_body: DoPart --
do p.getatt_porttypeOpt -> do_att_porttypeOpt;
   p.getport_etagOpt -> do_port_etagOpt;

-- do_port_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_port_etag;
   if);

-- do_port_etag_body: DoPart --
do 

-- do_att_porttypeOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_att_porttype;
   if);

-- do_sup_page_body: DoPart --
do s.getatt_sup_pageOpt -> do_att_sup_pageOpt;
   s.getsup_page_etagOpt -> do_sup_page_etagOpt;

-- do_sup_page_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_sup_page_etag;
   if);

-- do_sup_page_etag_body: DoPart --
do 

-- do_att_sup_pageOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_att_idref;
   if);

-- do_sub_page_body: DoPart --
do s.getatt_sub_pageOpt -> do_att_sub_pageOpt;
   s.getsub_page_etagOpt -> do_sub_page_etagOpt;

-- do_sub_page_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_sub_page_etag;
   if);

-- do_sub_page_etag_body: DoPart --
do 

-- do_att_sub_pageOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_att_idref;
   if);

-- do_mult_body: DoPart --
do m.getatt_instOpt -> do_att_instOpt;
   m.getmult_etagOpt -> do_mult_etagOpt;

-- do_mult_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_mult_etag;
   if);

-- do_mult_etag_body: DoPart --
do 

-- do_att_instOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_att_insts;
   if);

-- do_layering_body: DoPart --
do l.getatt_laytypeOpt -> do_att_laytypeOpt;
   l.getlayering_etagOpt -> do_layering_etagOpt;

-- do_layering_etagOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_layering_etag;
   if);

-- do_layering_etag_body: DoPart --
do 

-- do_att_laytypeOpt_body: DoPart --
do (if a.kind<>ast.kinds.optional then
       a[] -> do_att_laytype;
   if);

-- do_shape_body: DoPart --
do (if s.symbol
    // cpn.box then
       s[] -> do_box;
    // cpn.ellipse then
       s[] -> do_ellipse;
    // cpn.poly then
       s[] -> do_poly;
    // cpn.line then
       s[] -> do_line;
    // cpn.label then
       s[] -> do_label;
    // cpn.wedge then
       s[] -> do_wedge;
    // cpn.rbox then
       s[] -> do_rbox;
    // cpn.seg_conn then
       s[] -> do_seg_conn;
    if);

-- do_lintyps_body: DoPart --
do (if l.symbol
    // cpn.lintyp_Solid then
       l[] -> do_lintyp_Solid;
    // cpn.Dashed then
       l[] -> do_Dashed;
    // cpn.DotDash then
       l[] -> do_DotDash;
    // cpn.GroupInd then
       l[] -> do_GroupInd;
    // cpn.FlashInd then
       l[] -> do_FlashInd;
    // cpn.lintyp_None then
       l[] -> do_lintyp_None;
    // cpn.Null then
       l[] -> do_Null;
    // cpn.Dotted then
       l[] -> do_Dotted;
    if);

-- do_lintyp_Solid_body: DoPart --
do l.getSolid -> do_Solid;

-- do_lintyp_None_body: DoPart --
do l.getcpn_None -> do_cpn_None;

-- do_Solid_body: DoPart --
do 

-- do_Dashed_body: DoPart --
do 

-- do_DotDash_body: DoPart --
do 

-- do_GroupInd_body: DoPart --
do 

-- do_FlashInd_body: DoPart --
do 

-- do_cpn_None_body: DoPart --
do 

-- do_Null_body: DoPart --
do 

-- do_Dotted_body: DoPart --
do 

-- do_cols_body: DoPart --
do (if c.symbol
    // cpn.Black then
       c[] -> do_Black;
    // cpn.Maroon then
       c[] -> do_Maroon;
    // cpn.Gray then
       c[] -> do_Gray;
    // cpn.Olive then
       c[] -> do_Olive;
    // cpn.Purple then
       c[] -> do_Purple;
    // cpn.Silver then
       c[] -> do_Silver;
    // cpn.Red then
       c[] -> do_Red;
    // cpn.Teal then
       c[] -> do_Teal;
    // cpn.Navy then
       c[] -> do_Navy;
    // cpn.Fucia then
       c[] -> do_Fucia;
    // cpn.Blue then
       c[] -> do_Blue;
    // cpn.Aqua then
       c[] -> do_Aqua;
    // cpn.Green then
       c[] -> do_Green;
    // cpn.Lime then
       c[] -> do_Lime;
    // cpn.Yellow then
       c[] -> do_Yellow;
    // cpn.White then
       c[] -> do_White;
    if);

-- do_Black_body: DoPart --
do 

-- do_Maroon_body: DoPart --
do 

-- do_Gray_body: DoPart --
do 

-- do_Olive_body: DoPart --
do 

-- do_Purple_body: DoPart --
do 

-- do_Silver_body: DoPart --
do 

-- do_Red_body: DoPart --
do 

-- do_Teal_body: DoPart --
do 

-- do_Navy_body: DoPart --
do 

-- do_Fucia_body: DoPart --
do 

-- do_Blue_body: DoPart --
do 

-- do_Aqua_body: DoPart --
do 

-- do_Green_body: DoPart --
do 

-- do_Lime_body: DoPart --
do 

-- do_Yellow_body: DoPart --
do 

-- do_White_body: DoPart --
do 

-- do_arcors_body: DoPart --
do (if a.symbol
    // cpn.arcors_BOTHDIR then
       a[] -> do_arcors_BOTHDIR;
    // cpn.arcors_NODIR then
       a[] -> do_arcors_NODIR;
    // cpn.PtoT then
       a[] -> do_PtoT;
    // cpn.TtoP then
       a[] -> do_TtoP;
    if);

-- do_arcors_BOTHDIR_body: DoPart --
do a.getBOTHDIR -> do_BOTHDIR;

-- do_arcors_NODIR_body: DoPart --
do a.getNODIR -> do_NODIR;

-- do_BOTHDIR_body: DoPart --
do 

-- do_NODIR_body: DoPart --
do 

-- do_PtoT_body: DoPart --
do 

-- do_TtoP_body: DoPart --
do 

-- do_fustyps_body: DoPart --
do (if f.symbol
    // cpn.fustyp_Global then
       f[] -> do_fustyp_Global;
    // cpn.fustyp_Page then
       f[] -> do_fustyp_Page;
    // cpn.fustyp_Instance then
       f[] -> do_fustyp_Instance;
    if);

-- do_fustyp_Global_body: DoPart --
do 

-- do_fustyp_Page_body: DoPart --
do 

-- do_fustyp_Instance_body: DoPart --
do 

-- do_orients_body: DoPart --
do (if o.symbol
    // cpn.BOTHDIR then
       o[] -> do_BOTHDIR;
    // cpn.NODIR then
       o[] -> do_NODIR;
    // cpn.one_to_two then
       o[] -> do_one_to_two;
    // cpn.two_to_one then
       o[] -> do_two_to_one;
    if);

-- do_one_to_two_body: DoPart --
do 

-- do_two_to_one_body: DoPart --
do 

-- do_pats_body: DoPart --
do (if p.symbol
    // cpn.cpn_None then
       p[] -> do_cpn_None;
    // cpn.Solid then
       p[] -> do_Solid;
    // cpn.AlmostSolid then
       p[] -> do_AlmostSolid;
    // cpn.InverseHeavyDotted then
       p[] -> do_InverseHeavyDotted;
    // cpn.Chessboard then
       p[] -> do_Chessboard;
    // cpn.InverseOrderedDotted then
       p[] -> do_InverseOrderedDotted;
    // cpn.NarrowVertical then
       p[] -> do_NarrowVertical;
    // cpn.FilledDiagonals then
       p[] -> do_FilledDiagonals;
    // cpn.WideVertical then
       p[] -> do_WideVertical;
    // cpn.Speckled then
       p[] -> do_Speckled;
    // cpn.RandomDotted then
       p[] -> do_RandomDotted;
    // cpn.NarrowGrid then
       p[] -> do_NarrowGrid;
    // cpn.Bricks then
       p[] -> do_Bricks;
    // cpn.SparselyDotted then
       p[] -> do_SparselyDotted;
    // cpn.ShortDiagonals then
       p[] -> do_ShortDiagonals;
    // cpn.Lightbubbles then
       p[] -> do_Lightbubbles;
    // cpn.PieTop then
       p[] -> do_PieTop;
    // cpn.Buttercups then
       p[] -> do_Buttercups;
    // cpn.Waffles then
       p[] -> do_Waffles;
    // cpn.LightDotted then
       p[] -> do_LightDotted;
    // cpn.MediumDotted then
       p[] -> do_MediumDotted;
    // cpn.LightGray then
       p[] -> do_LightGray;
    // cpn.OrderedDotted then
       p[] -> do_OrderedDotted;
    // cpn.NarrowHorizontal then
       p[] -> do_NarrowHorizontal;
    // cpn.NarrowDiagonal then
       p[] -> do_NarrowDiagonal;
    // cpn.WideHorizontal then
       p[] -> do_WideHorizontal;
    // cpn.WideDiagonal then
       p[] -> do_WideDiagonal;
    // cpn.DottedBoxes then
       p[] -> do_DottedBoxes;
    // cpn.WideGrid then
       p[] -> do_WideGrid;
    // cpn.DiagonalBricks then
       p[] -> do_DiagonalBricks;
    // cpn.Holiday then
       p[] -> do_Holiday;
    // cpn.Carrots then
       p[] -> do_Carrots;
    // cpn.DiagonalScales then
       p[] -> do_DiagonalScales;
    // cpn.RoundScales then
       p[] -> do_RoundScales;
    // cpn.Mountains then
       p[] -> do_Mountains;
    // cpn.HeavyBubbles then
       p[] -> do_HeavyBubbles;
    // cpn.Diamonds then
       p[] -> do_Diamonds;
    if);

-- do_AlmostSolid_body: DoPart --
do 

-- do_InverseHeavyDotted_body: DoPart --
do 

-- do_Chessboard_body: DoPart --
do 

-- do_InverseOrderedDotted_body: DoPart --
do 

-- do_NarrowVertical_body: DoPart --
do 

-- do_FilledDiagonals_body: DoPart --
do 

-- do_WideVertical_body: DoPart --
do 

-- do_Speckled_body: DoPart --
do 

-- do_RandomDotted_body: DoPart --
do 

-- do_NarrowGrid_body: DoPart --
do 

-- do_Bricks_body: DoPart --
do 

-- do_SparselyDotted_body: DoPart --
do 

-- do_ShortDiagonals_body: DoPart --
do 

-- do_Lightbubbles_body: DoPart --
do 

-- do_PieTop_body: DoPart --
do 

-- do_Buttercups_body: DoPart --
do 

-- do_Waffles_body: DoPart --
do 

-- do_LightDotted_body: DoPart --
do 

-- do_MediumDotted_body: DoPart --
do 

-- do_LightGray_body: DoPart --
do 

-- do_OrderedDotted_body: DoPart --
do 

-- do_NarrowHorizontal_body: DoPart --
do 

-- do_NarrowDiagonal_body: DoPart --
do 

-- do_WideHorizontal_body: DoPart --
do 

-- do_WideDiagonal_body: DoPart --
do 

-- do_DottedBoxes_body: DoPart --
do 

-- do_WideGrid_body: DoPart --
do 

-- do_DiagonalBricks_body: DoPart --
do 

-- do_Holiday_body: DoPart --
do 

-- do_Carrots_body: DoPart --
do 

-- do_DiagonalScales_body: DoPart --
do 

-- do_RoundScales_body: DoPart --
do 

-- do_Mountains_body: DoPart --
do 

-- do_HeavyBubbles_body: DoPart --
do 

-- do_Diamonds_body: DoPart --
do 

-- do_fonts_body: DoPart --
do (if f.symbol
    // cpn.Chicago then
       f[] -> do_Chicago;
    // cpn.Courier then
       f[] -> do_Courier;
    // cpn.Geneva then
       f[] -> do_Geneva;
    // cpn.Helvetica then
       f[] -> do_Helvetica;
    // cpn.Monaco then
       f[] -> do_Monaco;
    // cpn.Times then
       f[] -> do_Times;
    // cpn.Symbol then
       f[] -> do_Symbol;
    if);

-- do_Chicago_body: DoPart --
do 

-- do_Courier_body: DoPart --
do 

-- do_Geneva_body: DoPart --
do 

-- do_Helvetica_body: DoPart --
do 

-- do_Monaco_body: DoPart --
do 

-- do_Times_body: DoPart --
do 

-- do_Symbol_body: DoPart --
do 

-- do_justs_body: DoPart --
do (if j.symbol
    // cpn.Left then
       j[] -> do_Left;
    // cpn.Centered then
       j[] -> do_Centered;
    // cpn.Right then
       j[] -> do_Right;
    if);

-- do_Left_body: DoPart --
do 

-- do_Centered_body: DoPart --
do 

-- do_Right_body: DoPart --
do 

-- do_prttyps_body: DoPart --
do (if p.symbol
    // cpn.In then
       p[] -> do_In;
    // cpn.Out then
       p[] -> do_Out;
    // cpn.InOut then
       p[] -> do_InOut;
    // cpn.General then
       p[] -> do_General;
    if);

-- do_In_body: DoPart --
do 

-- do_Out_body: DoPart --
do 

-- do_InOut_body: DoPart --
do 

-- do_General_body: DoPart --
do 

-- do_laytyps_body: DoPart --
do (if l.symbol
    // cpn.Copy then
       l[] -> do_Copy;
    // cpn.IOR then
       l[] -> do_IOR;
    // cpn.cpn_XOR then
       l[] -> do_cpn_XOR;
    // cpn.INV then
       l[] -> do_INV;
    if);

-- do_Copy_body: DoPart --
do 

-- do_IOR_body: DoPart --
do 

-- do_cpn_XOR_body: DoPart --
do 

-- do_INV_body: DoPart --
do 

-- do_objatts_body: DoPart --
do (if o.symbol
    // cpn.objatts_posattr then
       o[] -> do_objatts_posattr;
    // cpn.objatts_fillattr then
       o[] -> do_objatts_fillattr;
    // cpn.objatts_lineattr then
       o[] -> do_objatts_lineattr;
    // cpn.objatts_textattr then
       o[] -> do_objatts_textattr;
    // cpn.objatts_flags then
       o[] -> do_objatts_flags;
    // cpn.objatts_layering then
       o[] -> do_objatts_layering;
    if);

-- do_objatts_posattr_body: DoPart --
do o.getposattr -> do_posattr;

-- do_objatts_fillattr_body: DoPart --
do o.getfillattr -> do_fillattr;

-- do_objatts_lineattr_body: DoPart --
do o.getlineattr -> do_lineattr;

-- do_objatts_textattr_body: DoPart --
do o.gettextattr -> do_textattr;

-- do_objatts_flags_body: DoPart --
do o.getflags -> do_flags;

-- do_objatts_layering_body: DoPart --
do o.getlayering -> do_layering;

-- do_pageobs_body: DoPart --
do (if p.symbol
    // cpn.place then
       p[] -> do_place;
    // cpn.trans then
       p[] -> do_trans;
    // cpn.tempdecl then
       p[] -> do_tempdecl;
    // cpn.globdecl then
       p[] -> do_globdecl;
    // cpn.locdecl then
       p[] -> do_locdecl;
    // cpn.arc then
       p[] -> do_arc;
    // cpn.aux then
       p[] -> do_aux;
    // cpn.aux_conn then
       p[] -> do_aux_conn;
    if);

-- do_placeob_body: DoPart --
do (if p.symbol
    // cpn.placeob_name then
       p[] -> do_placeob_name;
    // cpn.placeob_type then
       p[] -> do_placeob_type;
    // cpn.placeob_initmark then
       p[] -> do_placeob_initmark;
    // cpn.placeob_aux_endp then
       p[] -> do_placeob_aux_endp;
    // cpn.placeob_aux_std then
       p[] -> do_placeob_aux_std;
    // cpn.placeob_fus_key then
       p[] -> do_placeob_fus_key;
    // cpn.placeob_portkreg then
       p[] -> do_placeob_portkreg;
    // cpn.placeob_mark_key then
       p[] -> do_placeob_mark_key;
    if);

-- do_placeob_name_body: DoPart --
do p.getname -> do_name;

-- do_placeob_aux_endp_body: DoPart --
do p.getaux_endp -> do_aux_endp;

-- do_placeob_type_body: DoPart --
do p.gettype -> do_type;

-- do_placeob_initmark_body: DoPart --
do p.getinitmark -> do_initmark;

-- do_placeob_aux_std_body: DoPart --
do p.getaux_std -> do_aux_std;

-- do_placeob_fus_key_body: DoPart --
do p.getfus_key -> do_fus_key;

-- do_placeob_portkreg_body: DoPart --
do p.getportkreg -> do_portkreg;

-- do_placeob_mark_key_body: DoPart --
do p.getmark_key -> do_mark_key;

-- do_transob_body: DoPart --
do (if t.symbol
    // cpn.transob_name then
       t[] -> do_transob_name;
    // cpn.transob_cond then
       t[] -> do_transob_cond;
    // cpn.transob_code_key then
       t[] -> do_transob_code_key;
    // cpn.transob_log_key then
       t[] -> do_transob_log_key;
    // cpn.transob_time then
       t[] -> do_transob_time;
    // cpn.transob_aux_endp then
       t[] -> do_transob_aux_endp;
    // cpn.transob_hier_key then
       t[] -> do_transob_hier_key;
    // cpn.transob_aux_enab then
       t[] -> do_transob_aux_enab;
    if);

-- do_transob_name_body: DoPart --
do t.getname -> do_name;

-- do_transob_cond_body: DoPart --
do t.getcond -> do_cond;

-- do_transob_code_key_body: DoPart --
do t.getcode_key -> do_code_key;

-- do_transob_log_key_body: DoPart --
do t.getlog_key -> do_log_key;

-- do_transob_time_body: DoPart --
do t.gettime -> do_time;

-- do_transob_aux_endp_body: DoPart --
do t.getaux_endp -> do_aux_endp;

-- do_transob_hier_key_body: DoPart --
do t.gethier_key -> do_hier_key;

-- do_transob_aux_enab_body: DoPart --
do t.getaux_enab -> do_aux_enab;

-- cpn_init_body: DoPart --
do ast.astLevelInit;
   (* Alternative to using findGrammar: *)
   ('cpn', '.int', GrammarPath->ast.expandToFullPath)
     -> ast.registergrammar;
   cpn.init; 
   BobsTab -> ast.expandToFullPath -> cpn.parser.initialize
   (# dashNames::(# do true->value #);
   #);
