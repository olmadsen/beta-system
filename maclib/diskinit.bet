ORIGIN 'mactypes';

-- lib: attributes --
DILoad: external
	(# 
	do 'DILOAD' -> callPascal;
	#);
DIBadMount: external
	(# where: @integer;
		evtMessage: @integer;
		error: @shortInt;
	enter (where,evtMessage)
	do 'DIBADMOUNT' -> callPascal;
	exit error
	#);

